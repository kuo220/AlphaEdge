# Byte-compiled / optimized / DLL files
__pycache__/
*pyc

# Environments
.venv/
.env


# db
*.db

# Shioaji
shioaji.log

*.csv
*.png
*.log

# 回歸 baseline 例外：這是引擎重構期間唯一的行為護欄，不進版控等於沒有護欄
!tests/backtest/snapshots/*.csv

# Downloads Content
Downloads/*

# MkDocs
site/
.cache/

# broker_trading_metadata
broker_trading_metadata.json

# Build & Coverage
# egg-info 由 `pip install -e .` 產生；.coverage 由 pytest --cov 產生。
# ruff 與 pytest 的 cache 目錄會自行寫入 .gitignore 自我排除，不需列在這裡
*.egg-info/
.coverage
