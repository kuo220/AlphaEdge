# Backlog 索引

本檔案彙整 `backlog/` 內**所有待辦事項文件**的優先級與完成狀態，作為單一入口。

- 新增待辦 `.md` 時：在下表新增一列。
- 實作進度變動時：同步更新該列的「狀態」與「進度」。
- 項目完成並整份移出 `backlog/`（見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）時：刪除該列。

**狀態圖例**：⬜ 未開始（僅規劃）｜🔄 進行中｜✅ 完成｜⛔ 中斷（需在該文件註明中斷點與恢復下一步）｜⏸ 暫緩

> 文件結構與狀態標記規範見 [`manage-backlog` skill](../.claude/skills/manage-backlog/SKILL.md)。

**優先級（Priority）**：`P0` 最高（阻塞其他項目或已在進行）→ `P3` 最低（長期架構規劃）。各文件內部的階段編號一律寫成 `Phase1-1`，不縮寫成 `P1-1`，避免與優先級混淆。

> **2026-08-15：「梯次」欄已移除**（連同 `排程與相依.md`）。該欄回答「什麼時候能動工」，
> 用來標示前置相依尚未解除的工作。護欄、API 介面收斂、baseline 換代、引擎重構四個閘門
> 全數結清後，**剩餘每一份工作都是現在就能開工**，該欄所有列的值變得相同、不再帶資訊。
> 跨文件的相依與「建議同批施作」的提示，一律改寫在各列的「相依」欄。

---

## 待辦清單

| 優先級 | 狀態 | 檔案 | 說明 | 進度 | 相依 |
|:------:|:----:|------|------|------|------|
| P1 | ⏸ | [爬蟲缺口回補與非交易日批次清理.md](爬蟲缺口回補與非交易日批次清理.md) | 全 ETL 涵蓋盤點後的收斂（S6 券商分點已依使用者裁示移除，不做 FinMind 相關工作）：三張日頻表的非交易日與單一市場批次、財報三表缺漏年季、tick 停 16 個月、期貨三大法人籌碼起點太晚、連續契約只有 TX、股期與期貨 tick 未開工 | 8 / 9 項 ✅（S1~S5、S7、S9、S10）；S8 ⏸ | **無前置相依**。**2026-09-15 S9 裁示不做、S8 確認暫不補爬**：S9 期貨 tick 庫內無資料，回測不支援期貨 tick、唯一的期貨策略用日線，且 ETL 寫入已不再使用的 DolphinDB，故只留書面結論；S8 目前沒有任何策略交易股票期貨，解除條件改為「開始開發股期策略」，屆時建議先跑前 10 檔 × 2020 起（約 2.5 天）。僅剩 S8 暫緩，故整份仍標 ⏸。**2026-09-15 S10 完成**：TPEX 的 HTML 回應恢復後，`price` `2026-09-07~09` 補回 TPEX 半邊、`2017-06-03` 經 `extra_dates` 自動補回，護欄三條通過、清理腳本三表 0 天；途中 `chip` `2026-09-15` 因 TWSE 尚未公布（`NO_DATA`）只入庫 TPEX 半邊，已刪除，程式已同日修正（見 [ETL 入庫約定 §3.5](../docs/pipeline/etl-ingestion.md)；一邊查無資料的日子改為整天不入庫，重跑時 `chip` 09-15 被正確擋下）；`chip`／`margin` 的 09-15 已記 `incomplete`，兩市場公布後重跑即補上。剩 S8、S9 暫緩，故整份標 ⏸。**2026-09-14 S7 裁示不做**：往後不再使用 DolphinDB，台股 tick 不回補；S9 的期貨 tick ETL 同樣寫入 DolphinDB，失去落地目標，待決定改落地或不做（專案內 DolphinDB 相關程式未移除，屬另一份工作）。**2026-09-14 S10 複測：原判斷「TPEX 封鎖本機 IP」有誤**——同 IP 同時段下，TPEX 收盤行情、三大法人、融資融券的 `response=html` 都在傳輸途中斷線，改 `response=json` 則完整取得（收盤行情 1,014 列）；TWSE、MOPS、TAIFEX 正常。恢復方向改為 TPEX crawler 改抓 JSON 或等站方修好 HTML；庫內狀態不變、未執行 `--target price`，與另一份已結案的「半份資料入庫防線」實跑驗證合併。**2026-09-13 S2、S3 完成、S8 暫緩、S10 中斷**：S2 三表 52／52 年季齊備，收尾補的是三個**只入庫單一市場**的年季（`balance_sheet` 2021Q1 0 → 1,702、`comprehensive_income` 2021Q1 946 → 1,702、`cash_flow` 2024Q1 833 → 1,858），成因是財報 crawler 單一市場失敗只 `continue`、年季層 resume 之後不回頭（未修）。S3 `chip`／`margin` 追到 2026-09-11、月營收追到 2026/08，並補回只有 26 檔的殘缺月份 2026/04（→ 1,976）。S8 經估算前 20 檔 × 2015 起約 8~9 天，**使用者裁示暫緩**；另記下開跑前須先修的風險——股期上市晚於起點會觸發保險絲，而 `update()` 未逐商品隔離例外，一檔中止整批（**2026-09-13 已修正**：逐商品隔離例外，S8 只剩使用者選定 N 與起點）。S10 的 `chip` 11 天已補回，但 **TPEX 封鎖本機 IP**，`price` 的 `2017-06-03` 與本日新產生的 `2026-09-07~09`（僅 TWSE）未補，護欄正確地失敗在這 3 天；恢復步驟與兩個程式面發現（`price` updater 補不回週六補行交易日、TPEX 被擋時仍入庫 TWSE 半邊）寫在該文件。2026-09-04 建立，依據是當日以資料庫實際內容為準的涵蓋盤點（快照文件已刪除，重跑查詢附在該文件末尾）。**2026-09-05 S5 完成**：`--target futures_continuous` 一次跑完 3 秒，七檔齊備、42,891 列，零程式碼變更；TX 重建後 sha256 位元級相同（證明重建冪等），TX/MTX、TE/ZEF、TF/ZFF 三組大小型契約的日報酬相關係數 0.9957~0.9997。**2026-09-04 S1 完成**：刪除 29,782 列非交易日與停滯批次（`chip` 20 個日期、`margin` 6 個、`price` 1 個；刪除前逐列匯出 CSV 備份，可還原）＋ 重爬 6 個真實交易日（列數全部回到與鄰日同量級）；新增 `tests/test_trading_calendar_guard.py` 兩條護欄，**以清理前資料實測過會失敗**。**意外發現 `2017-12-18` 是停滯區塊的來源而非受害者**——它重爬後與刪除前 100% 相同，是站方把它的內容回給了後來 11 次請求。**S3 的封鎖已解除**。護欄有已知盲區（部分受污染的真實交易日抓不到，理由見測試 docstring），另立 **S10** 複查 22 個列數偏低但不含停滯區塊的交易日。原始成因：清洗端蓋的是**請求參數的日期**而非來源頁面內的日期，而 TWSE 那條路徑當時沒有「休市」判斷（TPEX 有）。**爬蟲端已由 `f278160` 修好**（2026-09-04 對 TWSE 實測 2026-08-29 週六回 `NO_DATA`），最後一批壞資料寫於 2026-06-03、早於修復，**故 S1 不含程式修改，只剩資料清理與護欄**。**但範圍比原估大**——除 21 個非交易日批次外，另有 **6 個真實交易日**（`2017-12-18`、`2023-05-12`、`2024-02-16`、`2025-01-22`、`2025-03-24`、`2025-04-23`）的 TWSE 籌碼被同一組停滯內容取代（列數 1,735~1,754 vs 鄰日約 2,050），**這 6 天要重爬不能刪**；另有 22 個交易日列數偏低待查。原始描述：**S1 是唯一的新發現且最該先做**：`chip` 有 **14 個非交易日批次**（13 個週六日 ＋ 1 個春節休市日）、`margin` 6 個（全為颱風停市日）、`price` 1 個（`2019-02-06`），每批列數都只有正常日的四分之一到一半；其中 `chip` 的 `2024-09-07` ＝ `2025-02-08` ＝ `2025-07-13` 與 `2025-07-20` ＝ `2025-07-27` 是**位元級相同的重複內容**，且不等於鄰近任何真實交易日——與 commit `154f78c`（PCR 清洗沒改到來源日期）同類，當時只修了 PCR。**缺資料讓回測少算幾天，多資料讓回測多算幾天，而後者沒有任何統計行會示警**；S1 阻塞 S3（`chip` 成因未明前續跑只會再寫進新的壞批次），且會改動歷史資料、回歸基準需重產。**明確不含**：爬蟲效能優化（並行爬取與批次寫入已評估為不做，理由見 [ETL 入庫約定](../docs/pipeline/etl-ingestion.md)）、權益變動表（歷史回補已於 2026-09-11 完成、2026-09-13 結案移出 `backlog/`，涵蓋範圍見 [權益變動表](../docs/pipeline/equity-change.md)）、指數期貨日行情（七檔目標商品已於 2026-09-04 全數補齊、零缺口，該文件已移出 `backlog/`）。S6 券商分點與 [券商分點NO_DATA的metadata語意.md](券商分點NO_DATA的metadata語意.md) 共用 FinMind quota 預算，建議同批評估 |
| P3 | ⬜ | [券商分點NO_DATA的metadata語意.md](券商分點NO_DATA的metadata語意.md) | 券商分點 metadata 增加 `last_attempted_date`，已確認無資料的區間不再重複請求 FinMind | 0 / 2 項（S1 必做、S2 延遲重試可選） | **無前置相依，純效率問題**。方案已於 2026-08-01 選定（做法一），原以選型紀錄放在 `docs/pipeline/`，2026-09-13 依「`docs/` 只放現行說明」移回 `backlog/`。**解除條件**：確認要省 FinMind quota，或 NO_DATA 重打開始影響日更時間。並行爬取已評估為不做（瓶頸是 quota 而非網路延遲），本項是券商分點剩下唯一能省 quota 的手段 |
| P3 | ⬜ | [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) | 由 SQLite3 遷移到 PostgreSQL 的分階段實作計畫 | 0 / 16 項（Phase0-1~Phase5-3） | **無硬前置**，但影響面廣，建議在其他重構收斂後再動；`core/api/` 具名查詢收斂完成後，其改動面已可收斂到該層 |
| P3 | 🔄 | [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | 美股平行模組擴充：市場分層、provider 抽象、回測核心拆分 | 1 / 9 項 ✅（Phase3-3，2026-09-02）；**Phase1-1 可直接開工** | **無前置相依**。**2026-09-02 Phase3-3 結案**：它不是被美股這條線做掉的，而是由命名軸線收斂＋台期貨 Phase5-3＋多市場回測引擎三件工作分別完成——四個市場軸目錄（`pipeline`／`api`／`adapters`／`backtest/datafeed`）已全部只剩 `tw/`，而 `models`／`strategies`／`managers` 依定案承載的是**軸 B（商品類別）**，本來就不該有 `us/`（市場軸由 `BaseStrategy.market` 與 factory 的 `(market, instrument_type)` 分派鍵表達）。**Phase1-1 可直接平行新增 `us/`**；美股策略則放進既有的 `core/strategies/stock/`，不需要新目錄。欄位語言亦已定案——美股一律英文欄名（見 [ETL 入庫約定 §3.4](../docs/pipeline/etl-ingestion.md)） |
| P3 | ⬜ | [台股新聞情緒溫度計篩選工具.md](台股新聞情緒溫度計篩選工具.md) | 每日爬取台股財經新聞，萃取個股利多/利空情緒並生成可篩選的溫度計指標 | 0 / 7 項（S1~S6 ⬜、S7 ⏸） | 與回測引擎無關，**無外部相依**；但 S1 新聞來源授權檢查阻塞其餘所有步驟，須先做 |

---

## 維護規則

1. 每次**新增**待辦 `.md`，同步在上表新增一列，並填寫優先級、狀態、說明與相依。
2. 每次**實作**推進（子任務完成、狀態欄變更），同步更新該列的「狀態」與「進度」。
3. 項目完成並整份移出 `backlog/` 時，刪除本表對應那一列（移出方式見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）。
4. 優先級為當下研判結果，可隨需求調整，但調整後必須讓本表與各文件內的優先序敘述一致。
5. **跨文件的相依寫在「相依」欄**：包含前置條件、與哪些步驟建議同批施作、以及該判斷的日期與理由。
6. 本檔只放**通則**（狀態圖例、優先級、待辦清單、維護規則）；各文件內部的施作紀錄與中斷點寫在該文件自身，不要回流到本檔。
