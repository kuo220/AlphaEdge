# Backlog 索引

本檔案彙整 `backlog/` 內**所有待辦事項文件**的優先級與完成狀態，作為單一入口。

- 新增待辦 `.md` 時：在下表新增一列。
- 實作進度變動時：同步更新該列的「狀態」與「進度」。
- 項目完成並整份移出 `backlog/`（見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）時：刪除該列。

**狀態圖例**：⬜ 未開始（僅規劃）｜🔄 進行中｜✅ 完成｜⛔ 中斷（需在該文件註明中斷點與恢復下一步）｜⏸ 暫緩

> 文件結構與狀態標記規範見 [`manage-backlog` skill](../.claude/skills/manage-backlog/SKILL.md)。

**優先級**：`P0` 最高（阻塞其他項目或已在進行）→ `P3` 最低（長期架構規劃）。此處的 `P` 指 Priority；各文件內部的階段編號一律寫成 `Phase1-1`，不縮寫成 `P1-1`，避免與優先級混淆。

---

## 執行梯次（Critical Path）

**優先級回答「多重要」，梯次回答「什麼時候能動工」，兩者不同**——高優先級的工作可能因為前置未完成而不能先做。

梯次的排序依據**不是**重要性，而是「誰會讓 `test_long_regression_snapshot`（915 筆 × 13 欄）失效」。這條 baseline 是全 `backlog/` 的共用資產：多市場抽象的 17 個步驟、部位控管、策略層欄位抽象、執行真實度，全部拿它當驗收條件。**會換掉 baseline 的工作若排在後面，前面每一次「回歸逐筆相同」的驗證都只是暫時有效。**

| 梯次 | 名稱 | 判準 |
|:----:|------|------|
| **G1** | 護欄 | 不改任何行為，但讓後續每一步的錯誤能被抓到（第二條回歸線、一鍵回歸腳本、靜默失效防呆） |
| **G2** | API 介面收斂 | 先把 `core/api/` 的查詢介面收乾淨，G3 的還原價才有地方掛 |
| **G3** | baseline 換代 | **會讓 915 筆失效**。兩份工作應合併排程、**只重產一次 baseline**，避免兩輪逐筆歸因 |
| **G4** | 引擎重構 | 需要 G1~G3 的回歸線都穩定後才動工 |
| **G5** | `FillModel` 掛點 | 掛點由 G4 定案，提前做會搬第二次 |
| **G6** | 下游 | 相依 G4 完成（多市場、遷移、端到端策略驗收） |
| **∥** | 可全程平行 | 不碰回測引擎與 baseline，任何時候都能插隊 |

```
G1  多市場 Phase0-1（含一鍵回歸腳本）✅ ＋ 真實度 S1/S4   ← 不改行為
 ↓
G2  策略層資料欄位抽象化（S1~S6）
 ↓
G3  股價還原 S1~S3 ＋ LONG 成本口徑 S1~S3
    → 合併重產 baseline（單獨 commit，只做一次）
 ↓
G4  多市場抽象 Phase1~Phase4（Phase4-2 併入部位控管）
 ↓
G5  滑價 ＋ 真實度 S5/S6 ＋ 放空約束 S2  → 同批進 FillModel
 ↓
G6  期貨／美股／PostgreSQL／放空當沖策略

∥   專案工程基建補齊（本次排程刻意延後，見下）、FinMind、逐日權益、當沖順序 S2/S3、新聞情緒
```

### 關於「專案工程基建補齊」的延後決定（2026-08-07）

該份原列為 G1（`pyproject.toml` → `ruff` → CI）。經確認 **G2~G5 對它沒有任何硬相依**——三處宣稱的依賴（`策略層資料欄位抽象化` S6 防護測試、`部位控管` S2 測試、Phase0-1 快照納入 CI）全是「有了更好」而非「沒有做不了」——故改列 `∥`，**不阻塞任何梯次**。

延後的取捨：

- **失去**：引擎重構 17 步的回歸驗證失去自動兜底，全靠人工記得跑。
- **替代**：改由多市場抽象 **Phase0-1 產出一鍵回歸腳本**（`scripts/run_regression.sh`，同時跑 SHORT 秒級快照與 LONG 915 筆），成本 10 分鐘，不需 `pyproject.toml` 與 GitHub Actions。**G1 的護欄性質因此仍然成立。**
- **附帶好處**：S2 的全專案格式化會動到大量檔案，若在重構前做，多市場抽象又會重寫同一批檔案，`git blame` 等於被洗兩次；放到重構後只洗一次。
- **建議落點**：不晚於 G6 動工前（期貨／美股會新增大量檔案，屆時沒有 `ruff` 與 CI 的代價會開始放大）。

> 此處正是「優先級 ≠ 梯次」的實例：該份仍是 **P1**（重要性未變），但梯次為 `∥`（本次不排進關鍵路徑）。

### 已知的排程衝突

1. **`策略層資料欄位抽象化` S3 與 `股價還原` S3 改同一批方法**（皆為 `core/api/stock_price_api.py` 新增查詢方法）。**必須先做前者**，讓還原價在乾淨的具名介面上加 adjusted 變體；反向施作會讓還原邏輯散進 raw `DataFrame` 的取值。此即 G2 先於 G3 的理由。
2. **除權息 ETL 是一份資料、兩個問題**：`股價還原` S1 與 `放空回測市場約束補齊` S3 需要同一份資料源，只做一次，先動工者負責，另一方直接取用。
3. **`FinMind` S8 應提前於 S5／S6**：S5 並行爬取與 S6 批次寫入都是往那兩個檔案再加程式碼，先拆再加。

---

## 待辦清單

| 優先級 | 狀態 | 梯次 | 檔案 | 說明 | 進度 | 相依 |
|:------:|:----:|:----:|------|------|------|------|
| P0 | 🔄 | **G1** ✅／**G4** | [回測引擎多市場抽象.md](回測引擎多市場抽象.md) | 把 `Backtester` 收斂為**單一引擎 ＋ 可插拔 model**（`InstrumentSpec`／`FillModel`／`CostModel`／`SettlementModel`／`DataFeed`），並完成 `core/` 解耦收尾 | 9 / 17 項（Phase 0～1 ＋ Phase2-1~2-5 ✅；Phase2-6~Phase4-4 ⬜） | **最高優先，阻塞期貨與美股的回測落地**；行為零改變，驗收為 LONG 915 筆 ＋ SHORT 快照逐筆相同。**G1（Phase0-1 護欄）已於 2026-08-07 完成**——回歸雙線與 `scripts/run_regression.sh` 就緒；G4 已開工，`core/models/base/` 與 `symbol` 欄位就位。Phase2-7 一併收斂策略側自建 API 與連線生命週期 |
| P1 | ⬜ | ∥ | [專案工程基建補齊.md](專案工程基建補齊.md) | 補上 `pyproject.toml`／`ruff` 設定／GitHub Actions CI，移除 `config.py` 的 import 副作用，並建立測試覆蓋率基線 | 0 / 6 項（S1~S6，S5 為可選） | **2026-08-07 由 G1 改列 ∥**：經確認 G2~G5 對本份無硬相依，護欄職責改由多市場抽象 Phase0-1 的一鍵回歸腳本承擔（理由與取捨見上方〈延後決定〉）。仍為 P1，建議不晚於 G6 動工前完成 |
| P1 | ⬜ | **G1**／**G5**／∥ | [回測引擎執行真實度補強.md](回測引擎執行真實度補強.md) | `ShortConstraint` 三個限制欄位有定義無呼叫端（設了不生效也不報錯）、停券回補計數掛錯桶、停牌／下市部位無出場、`Scale.MIX` 空實作、缺少成交量上限與部分成交，以及當沖轉留倉未補徵證交稅 | 0 / 7 項（S1~S7） | **S1、S4 屬 G1**（十餘行、零行為改變，但直接影響對所有回測結果的判讀）；S5、S6 屬 G5，建議與「回測滑價與執行係數」同批進 `FillModel`；S2、S3 可平行。**S7 於 2026-08-07 由多市場抽象 Phase0-1 的 SHORT 快照發現**，會改變回測結果，建議排在其 Phase2-3 之後 |
| P1 | ⬜ | **G2** | [策略層資料欄位抽象化.md](策略層資料欄位抽象化.md) | 6 支策略直接對 raw `DataFrame` 取中文欄位（`"收盤價"`、`"成交股數"`），改由 API 提供具名查詢方法 | 0 / 6 項（S1~S6） | 成本低、風險高（換資料源時會**靜默**少開倉）；**必須先於「股價還原與除權息調整」**（見上方衝突 ①），亦建議早於「PostgreSQL遷移計畫」與「美股ETL與回測架構規劃」 |
| P1 | ⬜ | **G3** | [股價還原與除權息調整.md](股價還原與除權息調整.md) | `price` 表為原始成交價未做還原，除權息跳空被回測當成真實漲跌——SHORT 憑空獲利、LONG 憑空虧損，所有策略的歷史回測都受污染 | 0 / 5 項（S1~S5） | **會讓 LONG baseline 失效**，須與「LONG成本模型口徑收斂」合併排程、只重產一次；S1 除權息 ETL 與「放空回測市場約束補齊」S3 共用同一份資料源 |
| P1 | ⬜ | **G3** | [LONG成本模型口徑收斂.md](LONG成本模型口徑收斂.md) | `StockPositionManager` 的 LONG 分支仍走舊 `StockUtils`，與 SHORT 的 `StockCostModel` 兩套口徑並存 | 0 / 4 項（S1~S4） | **會讓 LONG baseline 失效**，與「股價還原與除權息調整」合併重產；阻擋滑價係數落地 |
| P1 | ⬜ | ∥ | [回測權益曲線改用逐日權益.md](回測權益曲線改用逐日權益.md) | 報表四張圖仍以已實現損益為軸，未接上 `snapshot_daily_equity` 產出的逐日權益，導致 MDD 被低估 | 0 / 5 項（S1~S5） | 只動 `reporter`／`analyzer`，不碰 baseline 與引擎流程，**可全程平行**；工作量小、直接修掉指標失真 |
| P1 | ⬜ | **G5** | [回測滑價與執行係數.md](回測滑價與執行係數.md) | 不大改 `core` 架構下新增滑價等執行係數：係數存放、`StockUtils` 調價、`Backtester` 掛點 | 0 / 5 項（S1~S5，S4 為可選） | 掛點由「回測引擎多市場抽象」Phase2-2 定案為 `FillModel`，提前做會搬第二次；建議與「回測引擎執行真實度補強」S5／S6 同批 |
| P1 | ⬜ | **G6** | [放空策略_外資大賣強勢股當沖.md](放空策略_外資大賣強勢股當沖.md) | 外資大賣 + 強勢股隔日開盤放空、尾盤回補（現股先賣後買當沖） | 0 / 6 項（S1~S5 ⬜、S6 ⏸） | **整條鏈的最下游**：同時依賴券源檢核（放空約束 S2）、當沖執行順序、滑價假設三者；是端到端驗收用例，不是起點 |
| P2 | ⬜ | **G4** | [部位控管下沉引擎.md](部位控管下沉引擎.md) | `max_holdings` 被 `Backtester` 讀入卻從未使用，實際檔數上限與等權資金切分由 4 支策略各寫一遍；抽出 `PositionSizer` 並改為引擎側硬上限 | 0 / 5 項（S1~S5） | 建議與「回測引擎多市場抽象」Phase4-2（`BasePositionManager` 抽取）合併施作，可省一次回歸驗證成本 |
| P2 | 🔄 | ∥ | [回測引擎當沖執行順序重構.md](回測引擎當沖執行順序重構.md) | 當沖／日內策略的開倉、平倉順序政策化（對齊業界 bar policy / event loop） | 1 / 4 項（S1 ✅、S2~S3 ⬜、S4 ⏸） | S1 經 2026-08-07 複查確認已由放空框架 Phase3-4 完成（18 項測試通過，含 LONG 回歸 915 筆）；剩餘 S2、S3 不碰 baseline，可平行 |
| P2 | 🔄 | ∥ | [FinMind爬蟲清洗儲存流程優化.md](FinMind爬蟲清洗儲存流程優化.md) | FinMind 爬蟲／清洗／儲存流程優化計畫；S8 拆分全專案最大的兩個檔案（1102 ＋ 859 行） | 4 / 8 項 ✅（S1、S2、S4、S7） | 與回測完全無關，**整條可平行**；S8 建議排在 S5／S6 之前（見上方衝突 ③） |
| P2 | ⏸ | ∥ | [券商分點NO_DATA處理.md](券商分點NO_DATA處理.md) | 券商分點 API 回傳 NO_DATA 時的 metadata 處理（建議做法一：`last_attempted_date`） | S1 選型 ✅；S2~S5 ⏸ 僅紀錄做法，暫不實作 | 解除條件：確認要省 API 額度或重複請求開始影響日更時間 |
| P2 | 🔄 | **G3**／**G5** | [放空回測市場約束補齊.md](放空回測市場約束補齊.md) | 融券餘額 ETL／券源檢核、停券日與股利補償、融資槓桿、同標的雙向持倉 | 1 / 6 項（S1 ✅、S2~S3 ⬜、S4~S6 ⏸）；歷史回補待執行 | **S3 除權息 ETL 屬 G3**（與「股價還原與除權息調整」S1 共用資料源，只做一次）；**S2 券源檢核屬 G5**（落點為 `FillModel`）。S1 已完成，資料缺口解除 |
| P3 | ⬜ | **G6** | [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) | 由 SQLite3 遷移到 PostgreSQL 的分階段實作計畫 | 0 / 16 項（Phase0-1~Phase5-3） | 影響面廣，建議在其他重構收斂後再動；G2 完成後其改動面可收斂到 `core/api/` 一層 |
| P3 | ⬜ | **G6** | [台期貨ETL與回測架構規劃.md](台期貨ETL與回測架構規劃.md) | 台期貨平行模組擴充：ETL、保證金/換月/日曆、回測與策略分支 | 0 / 18 項（Phase1-1~Phase6-2，Phase5-3 ⏸） | Phase1-6 相依「回測引擎多市場抽象」（G4）全部完成；與「美股ETL與回測架構規劃」共用核心拆分原則 |
| P3 | ⬜ | **G6** | [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | 美股平行模組擴充：市場分層、provider 抽象、回測核心拆分 | 0 / 9 項（Phase1-1~Phase3-3，Phase3-3 ⏸） | 同上；引擎層共用介面由「回測引擎多市場抽象」提前完成；兩者的目錄收斂步驟建議一起做，避免兩次重工 |
| P3 | ⬜ | ∥ | [台股新聞情緒溫度計篩選工具.md](台股新聞情緒溫度計篩選工具.md) | 每日爬取台股財經新聞，萃取個股利多/利空情緒並生成可篩選的溫度計指標 | 0 / 7 項（S1~S6 ⬜、S7 ⏸） | 與回測引擎無關，可平行；S1 新聞來源授權檢查阻塞其餘所有步驟 |

---

## 維護規則

1. 每次**新增**待辦 `.md`，同步在上表新增一列，並填寫優先級、狀態、梯次、說明與相依。
2. 每次**實作**推進（子任務完成、狀態欄變更），同步更新該列的「狀態」與「進度」。
3. 項目完成並整份移出 `backlog/` 時，刪除本表對應那一列（移出方式見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）。
4. 優先級為當下研判結果，可隨需求調整，但調整後必須讓本表與各文件內的優先序敘述一致。
5. **梯次為相依關係的推導結果，不可單獨調整**：若要改動某列的梯次，必須先確認該文件的前置相依確實改變，並同步更新〈執行梯次〉一節的流程圖與衝突清單。一份文件的不同步驟可分屬不同梯次（以 `／` 分隔），此時須在「相依」欄寫明哪些步驟屬哪一梯。
