# Backlog 索引

本檔案彙整 `backlog/` 內**所有待辦事項文件**的優先級與完成狀態，作為單一入口。

- 新增待辦 `.md` 時：在下表新增一列。
- 實作進度變動時：同步更新該列的「狀態」與「進度」。
- 項目完成並整份移出 `backlog/`（見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）時：刪除該列。

**狀態圖例**：⬜ 未開始（僅規劃）｜🔄 進行中｜✅ 完成｜⛔ 中斷（需在該文件註明中斷點與恢復下一步）｜⏸ 暫緩

> 文件結構與狀態標記規範見 [`manage-backlog` skill](../.claude/skills/manage-backlog/SKILL.md)。

**優先級（Priority）**：`P0` 最高（阻塞其他項目或已在進行）→ `P3` 最低（長期架構規劃）。各文件內部的階段編號一律寫成 `Phase1-1`，不縮寫成 `P1-1`，避免與優先級混淆。

> **2026-08-15：「梯次」欄已移除**（連同 `排程與相依.md`）。該欄回答「什麼時候能動工」，
> 用來標示前置相依尚未解除的工作。護欄、API 介面收斂、baseline 換代、引擎重構四個閘門
> 全數結清後，**剩餘每一份工作都是現在就能開工**，該欄所有列的值變得相同、不再帶資訊。
> 跨文件的相依與「建議同批施作」的提示，一律改寫在各列的「相依」欄。

---

## 待辦清單

| 優先級 | 狀態 | 檔案 | 說明 | 進度 | 相依 |
|:------:|:----:|------|------|------|------|
| P1 | 🔄 | [爬蟲缺口回補與非交易日批次清理.md](爬蟲缺口回補與非交易日批次清理.md) | 全 ETL 涵蓋盤點後的十項收斂：三張日頻表的 21 個非交易日批次、財報三表 15 個年季、券商分點 5/1007 家、tick 停 16 個月、期貨三大法人籌碼起點太晚、連續契約只有 TX、股期與期貨 tick 未開工 | 2 / 10 項 ✅（S1、S5）；**新增 S10** | **無前置相依**。2026-09-04 建立，依據是[爬蟲涵蓋範圍與中斷點盤點](../docs/dev/crawler-coverage-2026-09.md)（以資料庫實際內容為準，非文件自述進度）。**2026-09-05 S5 完成**：`--target futures_continuous` 一次跑完 3 秒，七檔齊備、42,891 列，零程式碼變更；TX 重建後 sha256 位元級相同（證明重建冪等），TX/MTX、TE/ZEF、TF/ZFF 三組大小型契約的日報酬相關係數 0.9957~0.9997。**2026-09-04 S1 完成**：刪除 29,782 列非交易日與停滯批次（`chip` 20 個日期、`margin` 6 個、`price` 1 個；刪除前逐列匯出 CSV 備份，可還原）＋ 重爬 6 個真實交易日（列數全部回到與鄰日同量級）；新增 `tests/test_trading_calendar_guard.py` 兩條護欄，**以清理前資料實測過會失敗**。**意外發現 `2017-12-18` 是停滯區塊的來源而非受害者**——它重爬後與刪除前 100% 相同，是站方把它的內容回給了後來 11 次請求。**S3 的封鎖已解除**。護欄有已知盲區（部分受污染的真實交易日抓不到，理由見測試 docstring），另立 **S10** 複查 22 個列數偏低但不含停滯區塊的交易日。原始成因：清洗端蓋的是**請求參數的日期**而非來源頁面內的日期，而 TWSE 那條路徑當時沒有「休市」判斷（TPEX 有）。**爬蟲端已由 `f278160` 修好**（2026-09-04 對 TWSE 實測 2026-08-29 週六回 `NO_DATA`），最後一批壞資料寫於 2026-06-03、早於修復，**故 S1 不含程式修改，只剩資料清理與護欄**。**但範圍比原估大**——除 21 個非交易日批次外，另有 **6 個真實交易日**（`2017-12-18`、`2023-05-12`、`2024-02-16`、`2025-01-22`、`2025-03-24`、`2025-04-23`）的 TWSE 籌碼被同一組停滯內容取代（列數 1,735~1,754 vs 鄰日約 2,050），**這 6 天要重爬不能刪**；另有 22 個交易日列數偏低待查。原始描述：**S1 是唯一的新發現且最該先做**：`chip` 有 **14 個非交易日批次**（13 個週六日 ＋ 1 個春節休市日）、`margin` 6 個（全為颱風停市日）、`price` 1 個（`2019-02-06`），每批列數都只有正常日的四分之一到一半；其中 `chip` 的 `2024-09-07` ＝ `2025-02-08` ＝ `2025-07-13` 與 `2025-07-20` ＝ `2025-07-27` 是**位元級相同的重複內容**，且不等於鄰近任何真實交易日——與 commit `154f78c`（PCR 清洗沒改到來源日期）同類，當時只修了 PCR。**缺資料讓回測少算幾天，多資料讓回測多算幾天，而後者沒有任何統計行會示警**；S1 阻塞 S3（`chip` 成因未明前續跑只會再寫進新的壞批次），且會改動歷史資料、回歸基準需重產。**明確不含**：爬蟲效能優化（屬 [FinMind爬蟲清洗儲存流程優化.md](FinMind爬蟲清洗儲存流程優化.md) S5／S6）、權益變動表（[權益變動表ETL補完.md](權益變動表ETL補完.md) 已在進行）、指數期貨日行情（七檔目標商品已於 2026-09-04 全數補齊、零缺口，該文件已移出 `backlog/`）。S6 券商分點與 [FinMind爬蟲清洗儲存流程優化.md](FinMind爬蟲清洗儲存流程優化.md) S5／S6 共用 FinMind quota 預算，建議同批評估 |
| P1 | 🔄 | [還原價缺非除權息公司行動.md](還原價缺非除權息公司行動.md) | 還原價只還原除權息：`dividend` 表來源是 TWSE／TPEx 除權息公告（`權息別` 只有息／權／權息），**減資、面額變更、股票分割、ETF 受益權分割、槓桿反向 ETF 的反向分割全部不在裡面** | 1 / 4 項 ✅（S1） | **無前置相依**。2026-09-04 由 [健檢殘留項目收斂] S2 從 0050 一拆四發現，**2026-09-05 S1 調查後範圍被推翻並更名**（原名「還原價缺分割事件」）：分割只有 6 檔，真正的大宗是**減資**——上市 `reducation/TWTAUU` 有 360 筆／243 檔，其中 355 筆可算倍率而**沒有任何一筆出現在 `dividend`**（305 筆倍率偏離 1 超過 5%、200 筆超過 20%，最極端南亞科 2014-09-09 為 9.99×）；上櫃 `bulletin/revivt` 另有 283 筆／193 檔。兩邊端點都直接給「停止買賣前收盤價 ＋ 恢復買賣參考價」，**資料源齊備、確定走 ETL**。**唯一的例外是 0050 那類 ETF 分割**：查遍 TWTB8U 與 OpenAPI 143 個端點都沒有，只能由行情偵測補（台股 ±10% 漲跌幅，非除權息日單日變動 > 15% 必為公司行動）。S3 完成後**回歸雙線基準需重產**——還原價是策略訊號的輸入，這點與先前幾份「零數值變動」的工作不同 |
| P2 | 🔄 | [健檢第三輪收斂.md](健檢第三輪收斂.md) | 第三輪掃描的 5 條：analyzer 四個交易統計沒防零除、`core/api` 九個死介面、報告 `.docx` 在版控、文件路徑漂移、四條無主的 C 級 | 4 / 5 項 ✅（S1、S2、S3、S5）；**S4 🔄 剩 10 處** | **無前置相依**。2026-09-05 建立，五條全部實證過；**2026-09-09 一天內做完四條**，全程 `pytest -m "not slow"` 由 891 增為 922、回歸雙線零數值變動。**S1（唯一的功能性 bug）完成**：五個統計改回 `Optional[float]`，分母為零回 `None` 而不是 `0.0`——勝率 0% 與「沒有交易可算勝率」必須分得開，`profit_factor = 0` 與「從來沒虧過」更是相反的意思；`core/` 內零呼叫（正式報表走 reporter），唯一呼叫端是一條測試，故改型別無風險。**S2 完成**：九個介面全數選「補測試」而非刪除（形狀都合理，且其中兩個原本就被文件指名為正式路徑），15 條離線測試 ＋ 掃描腳本 `scripts/check_api_orphan_methods.py`。**S3 完成**：實際是**三個** `.docx` 不是兩個——`tech_new_high_continuation` 那個是本輪掃描漏掉的同樣式殘留。**S5 完成**：F-029 讓 `FinMindAPI` 覆蓋率由 **0% 升到 98%** 並補上共用連線注入，F-011 刪掉被覆蓋的 import 並補兩份 `OrderState` 的一致性測試；**F-010 與 F-023 實查後判定不必做——它們早就被修掉了，只是 `code-quality.md` 沒回填**。⚠️ 這推翻了前兩輪「標為已分流、實際無人接手」的歸因：至少一半是**做了沒回填**，下一輪處理 C 級應先逐條實查現況，不要照處置欄重做一遍。**S4 🔄 剩 10 處**：`docs/futures/tw-futures-platform.md` 9 處與本表另一列的 [權益變動表ETL補完](權益變動表ETL補完.md) 1 處，兩檔在 2026-09-09 仍有未提交變更（回補行程仍在跑），改了會把對方未完成的內容一併 commit；已列進 `scripts/check_doc_paths.py` 的 `_PENDING` 附理由與日期，**解除封鎖時要連同那兩個條目一起刪掉**，否則檢查會對那兩檔永久失明。本輪共留下三支可重跑的檢查腳本（`check_api_orphan_methods.py`、`check_doc_paths.py`、既有的 `check_layer_deps.py`），下一輪健檢不必再手寫一次性掃描。文件末尾另附「本輪確認健康」對照表（`zip()` strict、無斷言測試、金鑰處理、覆蓋率、`print()` 擴散、`logs/` 等 11 項），下次健檢可直接跳過 |
| P1 | 🔄 | [測試護欄與本機CI容器一致性.md](測試護欄與本機CI容器一致性.md) | 回歸腳本假綠燈、CI 不跑 slow 與回歸線、容器缺相依與 `data/`、`run.py` 退出碼、一次性腳本清理、`tests/manual_*` 搬家、環境變數與相依檔一致 | 2 / 6 項 ✅（S1、S3） | 無前置相依。**2026-09-10 S3 完成**：`run.py` 的兩條失敗路徑不再回 0——策略名找不到 0 → 2 且訊息改走 stderr、`--mode live` 由 `pass`（0、零輸出）改為 `NotImplementedError`（1）；`live` 保留在 `choices` 但 help 講明未實作。8 條 subprocess 測試，實測修正前 7 條會失敗。**S5 的 `tests/conftest.py` loguru 隔離經實查已經做掉了**（`pytest_sessionstart` 的 no-op 已在），S5 剩 F-074 護欄（不存在）與 F-009 的 18 處 `sys.path.insert`。⚠️ **S6 目前擋住**：它要改 `requirements.txt`，該檔在權益變動表那條線手上且未提交。**2026-09-05 S1 完成**：`run_regression.sh` 的假綠燈修掉（skip 即結束碼 3 並指名是哪一條），CI 新增「分層相依檢查」與「SHORT 回歸線」兩步，ruff 由 `>=0.6` 釘為 `==0.16.3` 與 pre-commit 對齊。`test_long_regression.py` 未改——`skipif` 是對的，錯的是腳本把 skip 當通過。驗證改用 `ALPHAEDGE_DATA_DIR` 指向空目錄模擬無 DB 機器（原訂的「改名 `tw_stock.db`」會炸掉正在跑的權益變動表回補） |
| P2 | 🔄 | [FinMind爬蟲清洗儲存流程優化.md](FinMind爬蟲清洗儲存流程優化.md) | FinMind 爬蟲／清洗／儲存流程優化計畫 | 6 / 9 項 ✅（S1、S2、S4、S7、S8、S9） | 與回測完全無關，**整條無相依**。**2026-09-01 S8 完成**：`finmind_updater.py`／`finmind_loader.py` 按資料集拆進 `updaters/finmind/`／`loaders/finmind/`，最大單檔 1097 → 483 行，公開介面零改變。過程中發現**既有的 `tests/test_broker_trading_updater.py` 永遠不會失敗**（整段包在 try/except 且 `return False`），已改用新增的 `tests/test_finmind_broker_trading_batch.py`（4 條真斷言）＋ 逐方法與 `HEAD` 的 diff 作為驗收。**2026-09-01 S9 亦完成**：三支 85% 重複的 `load_*` 合併為 `reference_table_loader`（275 → 131 行共用實作 ＋ 三份 spec），同一組 11 條測試在合併前後結果相同。**剩下 S3（cleaner 可選不寫 CSV）／S5（並行爬取）／S6（批次寫入）**，S5／S6 需要打 FinMind API 實測 quota |
| P3 | 🔄 | [權益變動表ETL補完.md](權益變動表ETL補完.md) | 權益變動表 ETL 半成品補完；2026-08-22 決定走補完路線，cleaner／loader／updater 三件套完成，`equity_change` 攤平成長表 | 6 / 7 項 ✅（S1~S4、S6~S7）；**S5 的執行追蹤已移交 [權益變動表回補續跑.md](權益變動表回補續跑.md)** | **無前置相依**；程式面已完成。**2026-08-22 完成 2020Q1 全市場回補**：230,163 列 / 1,743 檔（另 343 檔當時尚未上市），`unreachable` 0、入庫失敗 0，抽樣 6 檔 1,993 列比對 mismatch = 0。過程中修掉一個會**靜默漏抓 323 檔**的早退 bug（見 [ETL 入庫約定 §4.5](../docs/pipeline/etl-ingestion.md)）。**2026-08-28 放寬節流**（每檔 0.5~1.5 秒、每 50 檔睡 15 秒，權益變動表專用常數，其他三張報表不受影響）：由 6 秒/檔降為推估 1.6 秒/檔，**一個年季約 0.9 小時、剩餘 55 個年季約 50 小時**；新值尚未實測，下一季跑完要看 `unreachable` 統計行確認沒被擋。逐檔 resume 已就緒，可隨時中斷，重跑只補差集。**2026-08-29 已把長期參考內容抽到 [`docs/pipeline/equity-change.md`](../docs/pipeline/equity-change.md)**（資料形狀、涵蓋範圍、已知限制、節流），本文件 S5 跑完即可**整份刪除**，不需再搬內容。**2026-09-03 的 2020Q2 梯次節流過關（2,087 次請求、0 unreachable）卻入庫 0 列**：`select_equity_changes_period_table()` 只認「第N季」，而 Q2／Q3／Q4 的實際標籤是「上半年度」「前3季」「年度」——照那個版本跑完整段回補，54 個年季只有 14 個 Q1 會有資料，其餘 40 季白打兩千次請求卻退出碼 0。**2026-09-04 S6 修正並以 2330 逐季實查驗證**（四季全中，且與同頁的去年同期表確實分開），統計行補上 `cleaned empty`。**同日 S7 完成中斷韌性**：Ctrl+C／SIGTERM 改為「本檔跑完才收工」（手上那批先入庫、進度存檔），「查無資料」寫進進度檔不再重打（省約 15% 請求），已落地未入庫的 CSV 會自動對帳，cleaner 例外與單批入庫失敗都不再中止整段回補；離線 32 項測試 ＋ 實際送 SIGINT 的實測皆通過 |
| P3 | 🔄 | [權益變動表回補續跑.md](權益變動表回補續跑.md) | `equity_change` 2013Q1 起的歷史回補續跑：續跑整段、判定 19 筆 `incomplete` 的性質、抽樣稽核、結案 | 0 / 4 項；S1 🔄 已完成 18 / 51 個年季，2026-09-09 20:18 重啟續跑中 | **2026-09-09 20:18 已重啟續跑**，實測 2,000+ 檔/小時（健康值 1,750），一個年季不到 1 小時。**節流額度自己回滿了，沒有換 IP**——同一組 10 檔對照組 09-05 23:20 是 178 秒、09-09 是 1.7 秒，停打三天就回填；09-06 判斷的「MOPS 對本 IP 設了長期低速率上限」屬過度解讀，下次遇節流先評估「停幾天再跑」，但別只停幾小時（09-05 那晚連續 10 小時都沒鬆開）。舊 log 留檔 `backfill_equity_change.20260904-0906.log`，新輪從空檔開始。**新增唯讀進度腳本 `scripts/watch_equity_change_progress.sh`**：一條指令印完行程存活、目前爬到哪、各季收尾統計、實測節流速率（取最後 20 個 throttle 區間換算，不用「每小時行數」以免不足整小時被低估成節流）、異常訊號（`cleaned empty` 非 0 季數、逾時行數、有無別的 MOPS 爬蟲）、資料表年季數／列數、進度檔 `incomplete`。⚠️ **S2 的原訂做法已被推翻**：原以為「S1 跑完再跑一次同指令」就能補掉 19 筆 `incomplete`，但 resume 每次啟動都從頭算差集、**會回頭補已完成年季**（09-09 第一遍 24 秒內就重打了那 19 筆），而且**全數再度失敗且原因是 `No tables found` 不是逾時**（當時額度回滿、其他季正常跑 2,000+ 檔/小時，排除站方過載）。離線查 DB：19 筆只落在 6 檔（1107、1606、2381、2396、3426、6457），失敗年季全部早於各檔第一筆成功年季；前四檔 `price` 表一列都沒有（已下市，本就在既有限制內）且連 2015 年後也只有 Q2／Q4（只申報半年報／年報），後兩檔（3426、6457）的失敗年季在掛牌前。S2 因此改為**判定性質**：抓一次原始 HTML，若站方明示查無資料就記為已知限制、不算缺口；若是錯誤頁則屬爬蟲分類邏輯問題，**回 [權益變動表ETL補完.md](權益變動表ETL補完.md) 開步驟，不在本文件範圍**。⚠️ **4 次請求就足以打壞主回補**：09-09 起跑後為查那 19 筆而另開一支只有 4 次請求的 MOPS 探測，主回補當場開始 `Read timed out`，停掉探測才恢復——「同一時間只能有一支 MOPS 爬蟲」連臨時查詢都算，任何對 MOPS 的查詢都要等 S1 跑完。**無前置相依，純資料回補、不改 `core/` 程式**（程式面見 [權益變動表ETL補完.md](權益變動表ETL補完.md)）。**2026-09-06 09:15 人為中斷，待換 IP 後重啟**：2013Q1~2017Q2 共 18 季完成、2017Q3 進行中（已入庫 671 檔 ＋ 查無資料 80 檔，待補 1,336 檔），累計 5,328,996 列 / 32,784 檔次；收工統計 `2017Q3 done: 751 requested / 671 ok / 80 no data / 0 unreachable / 0 cleaned empty`，結束碼 0、`incomplete` 未增加。**中斷原因**：MOPS 對本機 IP 的節流連續 10 小時沒回填，速率壓在健康值的 8%（100~200 檔/小時 vs 1,750），依此估剩餘 33 季要 380 小時（16 天）。**恢復步驟**（見該文件 S1 章節末）：換 IP → 跑 10 檔對照組驗證（10/10 且 5 秒內才算回滿）→ 確認沒有其他 MOPS 爬蟲 → 重啟 → 補 `caffeinate`。**2026-09-05 07:40~17:00 因機器休眠空轉 9.3 小時**（log 滿是 `Failed to resolve`，那是本機沒網路；同時段「IP 已被證交所封鎖」是十次建 session 失敗的固定文案，該情境下屬誤導），已補 `caffeinate -i -w <pid>` 綁住回補行程；同日 17:30 確認**對外網路（WAN）斷線**：閘道 ping 正常、DNS 解得出 IP，但 `8.8.8.8` 100% 丟包、連 google 都連不上，**不是被 MOPS 擋**；2017Q2 自 07:40 起停在 500 檔、十小時零進度。**22:30 kill 舊行程並重啟**（收工統計 `2017Q2 1255 requested / 1146 ok / 104 no data / 5 unreachable / 0 cleaned empty`，資料無損；resume 正確跳過 17 個完成年季、接上 2017Q2 剩 837 檔）。⚠️ **當時「舊行程退化」的判定站不住**：同一組 10 檔對照組 18:50 是 10/10／4.2 秒，23:20 變成 10/10／178 秒（慢 42 倍），程式一行沒改——**對照組量的是當下 IP 還剩多少節流額度，不是行程健不健康**；判定行程只能看長時間平均速率（每小時 throttle 行數 × 50，健康值約 35 行／1,750 檔）。判定停擺成因固定走四步：`ping 閘道` → `ping 8.8.8.8` → `curl 任一外站` → `curl 目標端點`；判斷「還在爬嗎」只能看 log 的 `throttle`／`add_to_db`，`ps` 有 PID 與 DB 列數都會騙人（列數每 100 檔才落地一批）。⚠️ **同一時間只能有一支 MOPS 爬蟲**：2026-09-05 22:36 另一個工作階段起跑 `backfill_fs.py`（財報三表），33 分鐘內本回補幾乎全逾時，23:09 停掉後恢復；四張報表共用同一份 log，靠端點分辨（權益變動表是 `ajax_t164sb06`）。**續跑不需指定起點**——resume 逐年季查資料表與進度檔算差集，已完成的年季零次請求即跳過；中斷用 `kill <pid>`，手上那批會先入庫、進度存檔才離開。⚠️ 進度檔另有 19 筆暫時性失敗待補（S2），**必須等 S1 跑完再做**，不可併行——兩個行程同時寫同一份進度檔會互相覆蓋 |
| P3 | ⬜ | [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) | 由 SQLite3 遷移到 PostgreSQL 的分階段實作計畫 | 0 / 16 項（Phase0-1~Phase5-3） | **無硬前置**，但影響面廣，建議在其他重構收斂後再動；`core/api/` 具名查詢收斂完成後，其改動面已可收斂到該層 |
| P3 | 🔄 | [台期貨保證金ETL.md](台期貨保證金ETL.md) | 台期貨保證金歷史序列 ETL：指數類（每口金額）與股票類（適用比例）分兩張表，並讓 `FuturesMarginConfig` 改為查表 | 5 / 7 項 ✅（S1~S5）；S6、S7 ⏸ | **無硬前置**，且**不擋台期貨主線**（保證金影響資金效率與可開口數，不影響 PnL）。2026-09-01 已完成來源調查並把四個端點寫進 `url_manager.py` 實測打通。**關鍵發現**：保證金是**每日計算、達門檻就調**（TX 在 2015~2026 調整 62 次，間隔最短 2 天、最長 372 天、中位數 34 天），且 TAIFEX 明載**調整後溯及既往**（未沖銷部位一併適用），所以現行 `FuturesMarginConfig` 的固定 10% 比率跨年回測會系統性偏掉。**歷史只補得到 2020/03 起**——那之後的調整公告附 CSV（含調整前／後，44 筆），2015~2019 的 16 筆是**掃描影像**（無字型、可抽文字 0 字），需 OCR 故列為 S6 ⏸。[台期貨平台](../docs/futures/tw-futures-platform.md) Phase2-2 的另一半（槓桿／部位控管）相依本文件。**2026-09-04 新增 S7 ⏸（價差部位保證金）**：由回測端健檢 F-058 反推出的第三張費率表缺口，交易所對同商品跨月份的價差部位只收單邊或按專屬費率收取，本專案無該資料源，回測端故讓兩腿各繳全額（保守）。同契約的雙向持倉已在 `FuturesPositionManager.open_position()` 直接拒單，錯誤狀態不會發生；**解除條件是出現價差／對沖策略需求**。**2026-09-01 S1~S2 完成**：`--target futures_margin` 上線，指數類 7 個商品入庫、重跑 0 新增；實作時抓到「乘數比例檢查只在同一標的指數家族內成立」（加權 3,505／電子 244.50／金融 158.00），已改為分家族比對。**S4 完成**：歷史回補至 2020/03（金額 1,101 列、比例 1,027 列），TX 45 筆變動、鏈式驗證 0 斷點。過程中抓到兩類會靜默寫錯歷史的坑——**附件用固定檔名會被站方覆寫**（今天下載 2022 的公告拿到 2026 的內容），以及**不能用標題判斷公告類型**（措辭改過、有換行空白、春節用「回調」不用「調整」，連續被咬四次），最後定案「取回所有提到保證金的公告，由附件結構收斂」。**S5 完成**：`FuturesMarginAPI` 上線，`FuturesMarginConfig` 改為雙模式（無 API 用比率、有 API 查表且查不到即 raise）。實資料量化出固定 10% 比率的誤差為 **2020 年 +143% 到 2026 年 −38%**——不但大且**跨年份會變號**，前段高估保證金、後段低估。**S3 完成**：股票類一份 CSV 有四段，逐段實查後把**分表依據修正為「金額 vs 比例」**——ETF 股期給的也是每口金額（不是比例），故與指數期貨同表；切掉選擇權後 **320 檔與標的池雙向完全吻合** |
| P3 | 🔄 | [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | 美股平行模組擴充：市場分層、provider 抽象、回測核心拆分 | 1 / 9 項 ✅（Phase3-3，2026-09-02）；**Phase1-1 可直接開工** | **無前置相依**。**2026-09-02 Phase3-3 結案**：它不是被美股這條線做掉的，而是由命名軸線收斂＋台期貨 Phase5-3＋多市場回測引擎三件工作分別完成——四個市場軸目錄（`pipeline`／`api`／`adapters`／`backtest/datafeed`）已全部只剩 `tw/`，而 `models`／`strategies`／`managers` 依定案承載的是**軸 B（商品類別）**，本來就不該有 `us/`（市場軸由 `BaseStrategy.market` 與 factory 的 `(market, instrument_type)` 分派鍵表達）。**Phase1-1 可直接平行新增 `us/`**；美股策略則放進既有的 `core/strategies/stock/`，不需要新目錄。欄位語言亦已定案——美股一律英文欄名（見 [ETL 入庫約定 §3.4](../docs/pipeline/etl-ingestion.md)） |
| P3 | ⬜ | [台股新聞情緒溫度計篩選工具.md](台股新聞情緒溫度計篩選工具.md) | 每日爬取台股財經新聞，萃取個股利多/利空情緒並生成可篩選的溫度計指標 | 0 / 7 項（S1~S6 ⬜、S7 ⏸） | 與回測引擎無關，**無外部相依**；但 S1 新聞來源授權檢查阻塞其餘所有步驟，須先做 |

---

## 維護規則

1. 每次**新增**待辦 `.md`，同步在上表新增一列，並填寫優先級、狀態、說明與相依。
2. 每次**實作**推進（子任務完成、狀態欄變更），同步更新該列的「狀態」與「進度」。
3. 項目完成並整份移出 `backlog/` 時，刪除本表對應那一列（移出方式見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）。
4. 優先級為當下研判結果，可隨需求調整，但調整後必須讓本表與各文件內的優先序敘述一致。
5. **跨文件的相依寫在「相依」欄**：包含前置條件、與哪些步驟建議同批施作、以及該判斷的日期與理由。
6. 本檔只放**通則**（狀態圖例、優先級、待辦清單、維護規則）；各文件內部的施作紀錄與中斷點寫在該文件自身，不要回流到本檔。
