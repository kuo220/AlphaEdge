# Backlog 索引

本檔案彙整 `backlog/` 內**所有待辦事項文件**的優先級與完成狀態，作為單一入口。

- 新增待辦 `.md` 時：在下表新增一列。
- 實作進度變動時：同步更新該列的「狀態」與「進度」。
- 項目完成並整份移出 `backlog/`（見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）時：刪除該列。

**狀態圖例**：⬜ 未開始（僅規劃）｜🔄 進行中｜✅ 完成｜⛔ 中斷（需在該文件註明中斷點與恢復下一步）｜⏸ 暫緩

> 文件結構與狀態標記規範見 [`manage-backlog` skill](../.claude/skills/manage-backlog/SKILL.md)。

**優先級（Priority）**：`P0` 最高（阻塞其他項目或已在進行）→ `P3` 最低（長期架構規劃）。各文件內部的階段編號一律寫成 `Phase1-1`，不縮寫成 `P1-1`，避免與優先級混淆。

> **2026-08-15：「梯次」欄已移除**（連同 `排程與相依.md`）。該欄回答「什麼時候能動工」，
> 用來標示前置相依尚未解除的工作。護欄、API 介面收斂、baseline 換代、引擎重構四個閘門
> 全數結清後，**剩餘每一份工作都是現在就能開工**，該欄所有列的值變得相同、不再帶資訊。
> 跨文件的相依與「建議同批施作」的提示，一律改寫在各列的「相依」欄。

---

## 待辦清單

| 優先級 | 狀態 | 檔案 | 說明 | 進度 | 相依 |
|:------:|:----:|------|------|------|------|
| P1 | ⬜ | [放空策略_外資大賣強勢股當沖.md](放空策略_外資大賣強勢股當沖.md) | 外資大賣 + 強勢股隔日開盤放空、尾盤回補（現股先賣後買當沖） | 5 / 6 項 ✅（S1~S5）；S6 ⏸ | **整條鏈的最下游**：三項前置（券源檢核、滑價假設、當沖執行順序 S2／S3）**已於 2026-08-15 全數完成**（券源檢核於 2026-08-16 `margin` 回補完成後才真正生效），前置已清空。是端到端驗收用例，不是起點；唯 S4 的平盤下放空／可當沖清單資料源仍缺（見本表〈放空回測市場約束補齊〉S7 ⏸），該步須以已知限制處理，不阻塞 S5 跑通回測 |
| P2 | 🔄 | [FinMind爬蟲清洗儲存流程優化.md](FinMind爬蟲清洗儲存流程優化.md) | FinMind 爬蟲／清洗／儲存流程優化計畫 | 6 / 9 項 ✅（S1、S2、S4、S7、S8、S9） | 與回測完全無關，**整條無相依**。**2026-09-01 S8 完成**：`finmind_updater.py`／`finmind_loader.py` 按資料集拆進 `updaters/finmind/`／`loaders/finmind/`，最大單檔 1097 → 483 行，公開介面零改變。過程中發現**既有的 `tests/test_broker_trading_updater.py` 永遠不會失敗**（整段包在 try/except 且 `return False`），已改用新增的 `tests/test_finmind_broker_trading_batch.py`（4 條真斷言）＋ 逐方法與 `HEAD` 的 diff 作為驗收。**2026-09-01 S9 亦完成**：三支 85% 重複的 `load_*` 合併為 `reference_table_loader`（275 → 131 行共用實作 ＋ 三份 spec），同一組 11 條測試在合併前後結果相同。**剩下 S3（cleaner 可選不寫 CSV）／S5（並行爬取）／S6（批次寫入）**，S5／S6 需要打 FinMind API 實測 quota |
| P2 | 🔄 | [放空回測市場約束補齊.md](放空回測市場約束補齊.md) | 融券餘額 ETL／券源檢核、停券日與股利補償、融資槓桿、同標的雙向持倉、平盤下放空與可當沖清單（S7，2026-08-15 自已移出的「真實度補強」承接） | 3 / 7 項（S1~S3 ✅、S4~S7 ⏸） | **可做的部分已全數完成**：S3 停券強制回補與股利補償於 2026-08-22 接進 `SettlementModel`（停券日由 `dividend` 表推導為除權息交易日前 4 個營業日，只對 `MARGIN` 生效；`SBL` 改扣股利補償）。剩餘 S4~S7 皆為明確暫緩（S4、S5 無策略需求；S6、S7 卡資料源），**不再有可推進的工作**。移出 `backlog/` 前須先替 S4~S7 安排追蹤去向，做法見該文件〈移出規劃〉。已知限制：股東會停券仍無資料源、上市權息並存拆不出現金股利（記入 `dividend_compensation_unknown`，不猜 0） |
| P3 | 🔄 | [權益變動表ETL補完.md](權益變動表ETL補完.md) | 權益變動表 ETL 半成品補完；2026-08-22 決定走補完路線，cleaner／loader／updater 三件套完成，`equity_change` 攤平成長表 | 4 / 5 項 ✅（S1~S4）；S5 🔄 | **無前置相依**；程式面已完成。**2026-08-22 完成 2020Q1 全市場回補**：230,163 列 / 1,743 檔（另 343 檔當時尚未上市），`unreachable` 0、入庫失敗 0，抽樣 6 檔 1,993 列比對 mismatch = 0。過程中修掉一個會**靜默漏抓 323 檔**的早退 bug（見 [ETL 入庫約定 §4.5](../docs/pipeline/etl-ingestion.md)）。**2026-08-28 放寬節流**（每檔 0.5~1.5 秒、每 50 檔睡 15 秒，權益變動表專用常數，其他三張報表不受影響）：由 6 秒/檔降為推估 1.6 秒/檔，**一個年季約 0.9 小時、剩餘 55 個年季約 50 小時**；新值尚未實測，下一季跑完要看 `unreachable` 統計行確認沒被擋。逐檔 resume 已就緒，可隨時中斷，重跑只補差集。**2026-08-29 已把長期參考內容抽到 [`docs/pipeline/equity-change.md`](../docs/pipeline/equity-change.md)**（資料形狀、涵蓋範圍、已知限制、節流），本文件 S5 跑完即可**整份刪除**，不需再搬內容 |
| P3 | ⬜ | [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) | 由 SQLite3 遷移到 PostgreSQL 的分階段實作計畫 | 0 / 16 項（Phase0-1~Phase5-3） | **無硬前置**，但影響面廣，建議在其他重構收斂後再動；`core/api/` 具名查詢收斂完成後，其改動面已可收斂到該層 |
| P3 | 🔄 | [台期貨ETL與回測架構規劃.md](台期貨ETL與回測架構規劃.md) | 台期貨平行模組擴充：ETL、保證金/換月/日曆、回測與策略分支 | 18 / 21 項 ✅（Phase0-1、Phase1-1、Phase1-2、Phase1-3a、Phase1-3b、Phase1-4、Phase1-5、Phase1-6、Phase1-7、Phase2-1、Phase2-2、Phase2-3、Phase2-4、Phase3-1、Phase4-1、Phase4-2、Phase6-1、**Phase6-2**）；**Phase5-3 🔄**（`pipeline/` 已收斂） | **阻塞已於 2026-08-07 解除**：多市場回測引擎架構 17 步全數完成，Phase1-6 改為「實作期貨 model 組」，引擎 0 行改動即可接入。**2026-08-22 新增 Phase0-1**（`downloads/` 收斂為 `tw_stock/`／`tw_futures/`）並定案目錄策略「資料先分、程式碼後分」（見該文件 §3.0）——**Phase0-1（downloads 歸位）與 Phase1-1（期貨常數）已於 2026-08-22 完成**，261 項測試通過、零行為改變；Phase5-3 範圍因此縮小為只剩程式碼。**2026-08-29 Phase1-2 完成**：TAIFEX 來源已接上（`TAIFEX_FUTURES_PRICE_URL`，POST 端點）、`tw_futures.db` 已建、`--target futures_price` 可跑，TX 端對端驗過且續跑零重爬。**歷史回補已於 2026-09-01 開跑**（TX，起點 2015-01-01，3,052 個候選日、約 6,100 次請求，估 7 小時）——過程中修掉一個會讓回補靜默中止的坑：**TAIFEX 擋流量回的是 HTTP 200 ＋ 無表格頁面，被誤判成「查無資料」**而誤觸保險絲（首次跑到 2015-03-30 就中止，那 20 天事後查證全部有資料）。已改為「空產出必再試一次、等待隨連續空產出遞增」並放慢節流；另補上 `update(resume=False)`，否則日常路徑的 resume 會讓整段歷史補不到。已定案：起點 **2015-01-01**（2026-08-29 使用者決定；來源實測可回溯至 TX 上市日 1998-07-21，取 2015 是刻意收窄非資料限制）、先只做大台 TX、日夜盤分開存、日線走 TAIFEX／Tick 走 Shioaji。**2026-08-29 Phase6-1 完成**：股期標的池 ETL 上線（`--target futures_stock_universe`），資料源改用 TAIFEX 標的證券一覽表（GET，整份一次回傳）而非行情頁下拉，320 檔入庫（個股 249／小型個股 47／ETF 21／小型 ETF 3）、標的代號 270/270 對得上現股 `price` 表；表為 `(snapshot_date, product_id)` 快照序列，掛牌／下市／乘數異動皆由差分推得（來源無這些欄位）。順帶查證了原本待確認的「股期是否有夜盤」——320 檔中只有 6 檔有。股期商品清單改由 `get_active_products()` 提供，**不再需要為每一檔手動指定**。流動性前 N 檔篩選需要成交量，已移入 Phase6-2。**2026-09-01 Phase1-3a 完成**：`FuturesPriceAPI` 上線（17 條測試 ＋ 實資料 smoke test），**不做換月／不挑近月**，當日所有到期月原樣回傳。順帶發現原 Phase1-3 的**相依方向寫反**——`FuturesQuoteAdapter` 要產出的 `FuturesQuote` 屬 Phase1-4，故拆為 1-3a（API，已完成）與 1-3b（adapter，相依 1-4，建議同批）。**同日 Phase1-3b ＋ Phase1-4 亦完成**：`FuturesQuoteAdapter`、`models/futures`、`managers/futures` 已就位（33 條測試）。**逐日盯市已實作**——`settle_daily()` 不再是 no-op，損益每日進出帳戶、`position.price` 重設為結算價，開倉價另存 `entry_price`。成本與保證金為刻意的簡化（費率預設 0、保證金以契約價值 × 比率近似），**不填未查證的數字**，完整版分屬 Phase2-1／Phase2-2。下一步為策略層 Phase1-5。**2026-09-02 Phase1-6 完成**：期貨 model 組（`TwFuturesSpec`／`TwFuturesFillModel`／`TwFuturesCostModel`／`TwFuturesSettlementModel`／`TwFuturesDataFeed`／`FuturesBacktestReporter`）接進**既有的單一引擎**，`--strategy MomentumFuturesStrategy` 端對端跑完 2024 全年並產出報表；台股 LONG 915 筆與 SHORT 快照逐筆相同。**原規格的「引擎 0 行改動」修正為一處**——`snapshot_daily_equity()` 的「做多部位價值＝市價 × 計價單位」是現金帳戶語意而非市場無關骨架（期貨只凍結保證金，契約價值不佔用資金），已下沉為 `SettlementModel.mark_position()`，預設實作逐字不動。另補了兩件規格沒寫但不做會壞的事：期貨專屬報表（股票報表的借券費／融券利息／0050 對標在期貨都不存在）與**到期契約的權宜出場**（到期後沒有報價，策略下不出平倉單，實測會凍結 79 萬保證金；正式換月屬 Phase2-4）。**2026-09-02 Phase2-1 完成**：期交稅（法規值、**買賣各課一次**、稅基為契約價值）、每口手續費（市場常見值，可逐商品指定）、滑價改以**跳動點**表達（基點在不同價位會換算成不同檔數）。順帶把 `FuturesCostConfig` 從 `managers/` 移到 `cost_model.py`，費率不再有第二份。**2026-09-02 Phase2-2 完成**：保證金**查表成為預設**（API 由 DataFeed 注入策略與部位管理層共用的同一份設定）、追繳以「權益 vs 維持保證金」判斷且砍到足額為止、可開口數隨生效日改變（實測 2024 年 TX 調整 11 次，每口由 167,000 到 338,000）。**2026-09-02 Phase2-3 完成**：期貨交易日曆上線（交易日取自行情表故涵蓋臨時休市；結算日為第三個星期三遇休市順延，以 140 個已到期 TX 月契約全量比對 140/140 相同；夜盤跨日與 2017-05-15 上線日皆處理）。**2026-09-02 Phase1-7 完成**：`--target futures_continuous` 上線（衍生表，不連網路），TX 2015~2026 建出 2,842 個交易日、140 次換月、8,526 列；三種調整方式全做（原規格只要一種），換月規則與 Phase2-4 共用同一份實作。實作時抓到**逆向調整方向寫反**——序列一樣連續、還原一樣通過，只有「換月日變動 ＝ 新契約自身變動」這條檢查抓得到，已固化為測試。**2026-09-02 Phase2-4 完成**：換月規則接進回測（策略挑合約與結算模型轉倉共用同一份設定），回測換月接點與 `futures_continuous` 的 `roll_flag` 完全一致。實測示範策略績效由 −11.0% 變 −24.1%——**舊行為在掩蓋真實虧損**（到期部位被權宜出場，等於在結算日把曝險憑空關掉），轉倉後才誠實。**2026-09-02 Phase4-1 完成（程式面）**：目標商品擴為 7 檔，六檔新商品實測可爬可清可入庫且**crawler／updater 一行都沒改**；歷史回補背景進行中（約 40 小時）。**2026-09-02 Phase4-2 完成**：日夜盤整併做在報價層（`session=COMBINED`），夜盤取**前一交易日**（週一要取到週五）、open 取夜盤故跨盤別跳空留在 bar 內（實測 2024-03-04 TX 近月的 183 點隔夜跳空原本完全看不見）。**2026-09-02 Phase3-1 完成**：籌碼 ETL 上線（三大法人／大額交易人／PCR 三張表，一天三次請求涵蓋全市場），**前視偏差對齊做在 API**（`get_available()` 取「資料日 < 查詢日」，那個等號就是前視）。踩到三個不會報錯的來源格式坑：非交易日回 HTML、PCR 尾逗號讓 pandas 整列位移、檔尾說明文字被當成資料列。**2026-09-02 Phase6-2 完成**：股期行情 ETL 上線（`--target futures_stock_price`，清單取自標的池、預設流動性前 20 檔），**股期乘數改為逐日查契約單位**（除權息會調整它，原本 adapter 直接 KeyError＝股期根本不能回測）；定案「無雙重調整」——台股走還原價、股期走調整契約單位，擇一即可。與「美股ETL與回測架構規劃」共用核心拆分原則，兩者的程式碼目錄收斂建議一起做 |
| P3 | 🔄 | [台期貨保證金ETL.md](台期貨保證金ETL.md) | 台期貨保證金歷史序列 ETL：指數類（每口金額）與股票類（適用比例）分兩張表，並讓 `FuturesMarginConfig` 改為查表 | 5 / 6 項 ✅（S1~S5）；S6 ⏸ | **無硬前置**，且**不擋台期貨主線**（保證金影響資金效率與可開口數，不影響 PnL）。2026-09-01 已完成來源調查並把四個端點寫進 `url_manager.py` 實測打通。**關鍵發現**：保證金是**每日計算、達門檻就調**（TX 在 2015~2026 調整 62 次，間隔最短 2 天、最長 372 天、中位數 34 天），且 TAIFEX 明載**調整後溯及既往**（未沖銷部位一併適用），所以現行 `FuturesMarginConfig` 的固定 10% 比率跨年回測會系統性偏掉。**歷史只補得到 2020/03 起**——那之後的調整公告附 CSV（含調整前／後，44 筆），2015~2019 的 16 筆是**掃描影像**（無字型、可抽文字 0 字），需 OCR 故列為 S6 ⏸。[台期貨ETL與回測架構規劃](台期貨ETL與回測架構規劃.md) Phase2-2 的另一半（槓桿／部位控管）相依本文件。**2026-09-01 S1~S2 完成**：`--target futures_margin` 上線，指數類 7 個商品入庫、重跑 0 新增；實作時抓到「乘數比例檢查只在同一標的指數家族內成立」（加權 3,505／電子 244.50／金融 158.00），已改為分家族比對。**S4 完成**：歷史回補至 2020/03（金額 1,101 列、比例 1,027 列），TX 45 筆變動、鏈式驗證 0 斷點。過程中抓到兩類會靜默寫錯歷史的坑——**附件用固定檔名會被站方覆寫**（今天下載 2022 的公告拿到 2026 的內容），以及**不能用標題判斷公告類型**（措辭改過、有換行空白、春節用「回調」不用「調整」，連續被咬四次），最後定案「取回所有提到保證金的公告，由附件結構收斂」。**S5 完成**：`FuturesMarginAPI` 上線，`FuturesMarginConfig` 改為雙模式（無 API 用比率、有 API 查表且查不到即 raise）。實資料量化出固定 10% 比率的誤差為 **2020 年 +143% 到 2026 年 −38%**——不但大且**跨年份會變號**，前段高估保證金、後段低估。**S3 完成**：股票類一份 CSV 有四段，逐段實查後把**分表依據修正為「金額 vs 比例」**——ETF 股期給的也是每口金額（不是比例），故與指數期貨同表；切掉選擇權後 **320 檔與標的池雙向完全吻合** |
| P3 | 🔄 | [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | 美股平行模組擴充：市場分層、provider 抽象、回測核心拆分 | 0 / 9 項（Phase3-3 🔄：`pipeline/tw/` 已歸位） | **阻塞已於 2026-08-07 解除**（同上）；引擎層共用介面已完成。**2026-08-31：`core/pipeline/` 的 `tw/` 歸位已由命名軸線收斂工作完成**（軸線定案見 [命名軸線](../docs/dev/naming-axes.md)），`us/` 可直接平行新增；Phase3-3 剩 `api/`／`models/`／`strategies/`／`managers/`。**Phase1-3 的隱性阻塞亦已解除**：factory 分派鍵已是 `(market, instrument_type)`。**2026-09-01：欄位語言已定案**——美股一律用英文欄名，規則為「欄位語言跟著資料來源走」（見 [ETL 入庫約定 §3.4](../docs/pipeline/etl-ingestion.md)），Phase1-2 可依此開工 |
| P3 | ⬜ | [台股新聞情緒溫度計篩選工具.md](台股新聞情緒溫度計篩選工具.md) | 每日爬取台股財經新聞，萃取個股利多/利空情緒並生成可篩選的溫度計指標 | 0 / 7 項（S1~S6 ⬜、S7 ⏸） | 與回測引擎無關，**無外部相依**；但 S1 新聞來源授權檢查阻塞其餘所有步驟，須先做 |

---

## 維護規則

1. 每次**新增**待辦 `.md`，同步在上表新增一列，並填寫優先級、狀態、說明與相依。
2. 每次**實作**推進（子任務完成、狀態欄變更），同步更新該列的「狀態」與「進度」。
3. 項目完成並整份移出 `backlog/` 時，刪除本表對應那一列（移出方式見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）。
4. 優先級為當下研判結果，可隨需求調整，但調整後必須讓本表與各文件內的優先序敘述一致。
5. **跨文件的相依寫在「相依」欄**：包含前置條件、與哪些步驟建議同批施作、以及該判斷的日期與理由。
6. 本檔只放**通則**（狀態圖例、優先級、待辦清單、維護規則）；各文件內部的施作紀錄與中斷點寫在該文件自身，不要回流到本檔。
