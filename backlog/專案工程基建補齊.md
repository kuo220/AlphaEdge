# 專案工程基建補齊

## Abstract

- **背景／問題**：專案的**程式碼品質規範完整**（`CLAUDE.md` §2 有 10 節 coding style、`.cursor/rules/` 有對應規則），但**沒有任何自動化在執行這些規範**：無 `pyproject.toml`（`import core` 依賴當前工作目錄，`tests/backtest/conftest.py:8-9` 必須手動 `sys.path.insert`）、無 `ruff.toml`／`pre-commit`（`CLAUDE.md` §2.10 寫明行寬 88／雙引號但無設定檔可執行）、無 CI（沒有 `.github/`）。`requirements.txt` 把 105 個 transitive dependency 全部 pin 死，等於當 lock file 用，但沒有 direct dependency 的來源可追。此外 `core/config.py` 有 import 時副作用（`load_dotenv()` 於 :9、`LOGS_DIR_PATH.mkdir()` 於 :32），只要 import 就會寫檔案系統。
- **目標**：讓 `CLAUDE.md` 的規範由工具自動執行而非人工記憶；讓專案可 `pip install -e .` 安裝；讓每次 push 自動跑 lint ＋ 快速測試。**這件事與所有重構正交**，做完之後所有重構的信心度都會上升。
- **範圍界線**：**不改任何 `core/` 的業務邏輯**。**不做**：切換套件管理器到 Poetry／uv（維持 pip ＋ venv）、不做 Docker 映像優化（`docker-compose.yml` 現況可用）、不做測試覆蓋率提升（只建立基線與報告，不追指標）、不做 `dev/env/*.yml`（conda 環境檔）的整併。
- **驗收標準**：`pip install -e .` 後於任意目錄可 `import core`；`ruff check .` 與 `ruff format --check .` 通過；GitHub Actions 於 push 時自動跑 `ruff` ＋ `pytest -m "not slow"` 並全綠；`tests/backtest/conftest.py` 的 `sys.path.insert` 可刪除。

> **時機（2026-08-07 修訂）**：原建議 S1~S3 在 [多市場回測引擎架構](../docs/backtest/multi-market-engine.md) 動工**之前**完成。經確認 **其餘各份工作對本份沒有任何硬相依**——三處宣稱的依賴（`策略層資料欄位抽象化` S6 的防護測試、`部位控管下沉引擎` S2 的純記憶體測試、Phase0-1 快照納入 CI）全都是「有了更好」而非「沒有做不了」——故本份**不阻塞任何工作**，任何時候都能開工。
>
> - **護欄職責的替代**：引擎重構的回歸把關改由多市場抽象 **Phase0-1 產出的一鍵回歸腳本**（`scripts/run_regression.sh`）承擔，成本 10 分鐘，不需 `pyproject.toml` 與 GitHub Actions。
> - **延後 S2 反而更合理**：全專案格式化會動到大量檔案，若在重構前做，多市場抽象又會重寫同一批檔案，`git blame` 等於被洗兩次；放到重構後只洗一次。
> - **建議落點**：不晚於期貨／美股／PostgreSQL 動工前。那些工作會新增大量檔案，屆時沒有 `ruff` 與 CI 的代價會開始放大。
> - 本份仍為 **P1**——重要性未變，只是本次排程不進關鍵路徑。

---

## 進度追蹤表

| 編號 | 步驟名稱 | 產出檔案 | 驗證方式 | 狀態 | 備註／中斷點 |
|------|----------|----------|----------|:----:|--------------|
| S1 | 新增 `pyproject.toml` 與可編輯安裝 | `pyproject.toml`、`requirements.txt`、`requirements-lock.txt`、`core/Dockerfile`、`tests/backtest/conftest.py`、`make_baseline.py`、`make_short_baseline.py` | 新 venv 內 `pip install -e .` 後任意目錄可 `import core` | ✅ | 2026-08-16；乾淨 venv 實測通過。**發現 `core.managers` 等目錄無 `__init__.py`，打包需開 namespaces** |
| S2 | 新增 `ruff` 設定並全專案套用 | `pyproject.toml`（`[tool.ruff]`）＋ 全專案格式化 | `ruff check .`、`ruff format --check .` 皆通過 | ✅ | 2026-08-16；1495 → 0。**格式化仍未 commit，請單獨一個 commit** |
| S3 | 建立 GitHub Actions CI | `.github/workflows/ci.yml`、4 個測試檔補標 `slow` | push 後 workflow 全綠 | ✅ | 2026-08-16；已用「移除 DB 與金鑰的副本」在本機模擬為全綠，**實際 workflow 需 push 後才能確認** |
| S4 | 移除 `core/config.py` 的 import 副作用 | `core/config.py` | 全專案 import 掃描；LONG 回歸逐筆相同 | ✅ | 2026-08-16；四項驗收全過。`LogManager` 早已有惰性 `mkdir`，不需修改 |
| S5 | 新增 `pre-commit` 掛鉤 | `.pre-commit-config.yaml`、`README.md` | `pre-commit run --all-files` 通過 | ✅ | 2026-08-16；設定檔已建立並驗證 YAML。**`pre-commit install` 會裝 git hook，留給使用者自己執行** |
| S6 | 測試覆蓋率基線 | `pyproject.toml`（`[tool.coverage]`）、本文件 §覆蓋率基線 | `pytest --cov` 產出報告，數字記入本文件 | ✅ | 2026-08-16；整體 **40%**，分模組數字見 §覆蓋率基線 |

---

## 現況盤點

（下表為 2026-08-07 動工前的狀態，2026-08-16 施作後的結果記於「處置」欄）

| 項目 | 動工前現況 | 處置 |
|------|------|------|
| 套件定義 | 無 `pyproject.toml` / `setup.py` | ✅ S1 已新增；`conftest.py` 與兩支 `make_*baseline.py` 的 `sys.path.insert` 皆已刪除 |
| 相依管理 | `requirements.txt` pin 了 105 個套件（含 transitive） | ✅ S1 已改為 `requirements-lock.txt`；直接相依由 AST 掃描 import 產出，共 11 個 ＋ 4 組 optional extras |
| Lint／格式 | `CLAUDE.md` §2.10 有規範，無設定檔 | ✅ S2 已加 `[tool.ruff]`，`ruff check .` 全綠 |
| CI | 無 | ✅ S3 已加 `.github/workflows/ci.yml` |
| 測試設定 | `pytest.ini`（僅定義 `slow` marker） | ✅ S1 已併入 `pyproject.toml`，`pytest.ini` 已刪除 |
| import 副作用 | `config.py:9` `load_dotenv()`、`config.py:32` `mkdir()` | ✅ S4 已移除 `mkdir`，並把 `DDB_PORT` 的 `int()` 改為 `get_int_env()` 防護 |
| repo 衛生 | `kuo_test.ipynb` 仍被追蹤 | ⬜ **未處理**（本份未動）；仍待與死碼清除一併處理 |

---

## S1. `pyproject.toml` 與可編輯安裝 ✅

- **目的**：讓 `core` 成為真正的 package，解除「只能從 repo root 執行」與「測試要手動改 `sys.path`」兩個限制。
- **做法**：
  1. 新增 `pyproject.toml`，使用 setuptools backend（不引入新套件管理器）：
     ```toml
     [project]
     name = "alphaedge"
     version = "0.1.0"
     requires-python = ">=3.12"
     dependencies = [
         "pandas>=3.0",
         "numpy>=2.4",
         "loguru>=0.7",
         "python-dotenv>=1.2",
         # …只列「直接 import 的套件」
     ]

     [project.optional-dependencies]
     dev = ["pytest>=9.0", "pytest-timeout>=2.4", "ruff", "pytest-cov"]
     ```
  2. **相依清單以實際 import 為準**：用 `grep -rhoE '^(import|from) [a-z_]+' --include="*.py" core/ tasks/ frontend/ | sort -u` 產出直接相依清單，不要照抄 `requirements.txt`。
  3. 現行 `requirements.txt` 改名為 `requirements-lock.txt` 保留（既有 Docker build 與 conda 環境仍可用），`requirements.txt` 改為一行 `-e .`。
  4. 把 `pytest.ini` 的內容併入 `pyproject.toml` 的 `[tool.pytest.ini_options]`，刪除 `pytest.ini`。
  5. 刪除 `tests/backtest/conftest.py:8-9` 的 `sys.path.insert`。
- **產出**：`pyproject.toml`、`requirements.txt`、`requirements-lock.txt`、`tests/backtest/conftest.py`；`pytest.ini` 刪除。
- **驗證方式**：建立乾淨 venv → `pip install -e ".[dev]"` → 於 `/tmp` 下執行 `python -c "import core; print(core.__file__)"` 成功；`pytest -m "not slow"` 全數通過。
- **相依**：無。須確認 `core/Dockerfile`、`frontend/Dockerfile` 的安裝步驟同步調整。

## S2. `ruff` 設定並全專案套用 ✅

- **目的**：把 `CLAUDE.md` §2.5（import 排序）、§2.10（行寬 88、雙引號、magic trailing comma）從文字規範變成可執行檢查。
- **做法**：
  1. 於 `pyproject.toml` 新增設定，對齊既有風格：
     ```toml
     [tool.ruff]
     line-length = 88
     target-version = "py312"

     [tool.ruff.lint]
     select = ["E", "F", "I", "UP", "B", "BLE", "TRY"]
     # UP006/UP035：專案規範要求 typing.List/Dict（CLAUDE.md §2.4.3），必須關閉
     ignore = ["UP006", "UP035"]

     [tool.ruff.format]
     quote-style = "double"
     ```

     - **`BLE`（blind-except）與 `TRY` 是刻意加的**：`core/` 目前 108 個 `except` 中有 81 個是 `except Exception` 或裸 `except`（**75%**），而 `core/pipeline/utils/exceptions.py` 已經定義了自訂例外卻沒有普及。ETL 場景下這會把「網路逾時」（該重試）與「資料 schema 變了」（該中止）混為一談。
     - 本步驟**只負責量化，不負責修**：預期這兩條規則會噴出大量告警，先以 `--statistics` 產出分佈（哪些模組最集中），把數字記入本文件 §例外處理現況，再決定是「逐一修」還是「另開 backlog」。若數量過大擋住 S3 的 CI 落地，可先將這兩條移入 `ignore` 並在本文件註明，**不要為了讓 CI 變綠而把它們永久關掉**。
  2. `ruff check --fix` ＋ `ruff format` 全專案跑一次。
  3. **格式化的改動要單獨一個 commit**，且該 commit 不含任何邏輯變更——否則日後 `git blame` 與 code review 都會被洗掉。
- **產出**：`pyproject.toml`；全專案格式化 commit。
- **驗證方式**：
  1. `ruff check .` 與 `ruff format --check .` 皆通過。
  2. **格式化 commit 前後，LONG 回歸 1,889 筆逐筆相同**（純格式化理應零影響，但這是最便宜的保險）。
- **相依**：S1。
- **注意**：`ignore` 清單裡的每一條都要註明對應的 `CLAUDE.md` 章節，避免日後有人「順手」打開而與專案規範打架。

## S3. GitHub Actions CI ✅

- **目的**：讓 lint 與快速測試在每次 push 自動執行，重構時不必依賴人工記得跑。
- **做法**：新增 `.github/workflows/ci.yml`：
  ```yaml
  on: [push, pull_request]
  jobs:
    check:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-python@v5
          with:
            python-version: "3.12"
        - run: pip install -e ".[dev]"
        - run: ruff check .
        - run: ruff format --check .
        - run: pytest -m "not slow" --timeout=300
  ```
  - **只跑 `-m "not slow"`**：`test_long_regression_snapshot` 需要 `stock.db`（未進版控，`.gitignore` 有 `*.db`），CI 環境沒有資料庫。
  - 因此需先確認 `pytest.ini` 的 `slow` marker **已正確標註在所有需要 DB 的測試上**；未標註者會在 CI 失敗，這也正好是一次盤點。
  - `shioaji`／`finmind` 等需要 API 金鑰的測試同樣須標記為 `slow` 或另加 marker。
- **產出**：`.github/workflows/ci.yml`；必要時補標 `@pytest.mark.slow`。
- **驗證方式**：push 後 workflow 全綠；故意引入一個格式錯誤，CI 應變紅。
- **相依**：S1、S2。
- **後續**：[多市場回測引擎架構](../docs/backtest/multi-market-engine.md) Phase0-1 的 SHORT 快照是**純記憶體、秒級**，完成後應納入 CI，讓引擎重構在 CI 就有一條回歸線把關。

## S4. 移除 `core/config.py` 的 import 副作用 ✅

- **目的**：`import core.config` 不應該產生任何檔案系統與環境變數的副作用。現況下任何 import 該模組的測試都會被動建立 `core/logs/`，且 CI 無 `.env` 時 `load_dotenv()` 的行為不明確。
- **做法**：
  1. 移除 `core/config.py:32` 的 `LOGS_DIR_PATH.mkdir(parents=True, exist_ok=True)`，改由 `core/utils/log_manager.py` 的 `LogManager.setup_logger()` 在**實際要寫檔時**惰性建立。
  2. `load_dotenv()`（:9）保留，但確認 `.env` 不存在時不會拋錯（`python-dotenv` 預設行為即為靜默略過，需實測確認），並在 CI 用環境變數而非檔案提供必要設定。
  3. 檢查其他 module-level 副作用：`DDB_PORT: int = int(os.getenv("DDB_PORT") or "0")`（:186）在環境變數為非數字字串時會於 import 期拋 `ValueError`，應加防護。
- **產出**：`core/config.py`、`core/utils/log_manager.py`。
- **驗證方式**：
  1. 刪除 `core/logs/` 後執行 `python -c "import core.config"`，目錄**不應**被建立；執行任一會寫 log 的流程後，目錄應被建立。
  2. LONG 回歸 1,889 筆逐筆相同。
- **相依**：S1。

## S5. `pre-commit` 掛鉤 ✅

- **目的**：讓 S2 的格式成果在本機就被維持，而不是等到 CI 才發現。
- **做法**：新增 `.pre-commit-config.yaml`，掛 `ruff` 與 `ruff-format`（官方 `astral-sh/ruff-pre-commit`），另加 `check-added-large-files`（`tick.png` 有 512KB，雖已被 `.gitignore` 排除，但此掛鉤能防止類似檔案再進來）。
- **產出**：`.pre-commit-config.yaml`；`README.md` 補一行安裝說明（`pre-commit install`）。
- **驗證方式**：`pre-commit run --all-files` 通過；故意提交格式錯誤的檔案會被擋下。
- **相依**：S2。**此步驟為可選**，未做不影響 S1~S4 的驗收。

## S6. 測試覆蓋率基線 ✅

- **目的**：知道目前測到哪、沒測到哪，作為後續補測試的依據。**不設門檻**——在覆蓋率明顯偏低時設門檻只會鼓勵寫無效測試。
- **做法**：
  1. `pyproject.toml` 新增 `[tool.coverage.run]`（`source = ["core"]`、`omit` 掉測試與腳本目錄（原本要 omit 的 `core/api/old api/` 已於 2026-08-07 由多市場抽象 Phase4-3 刪除））。
  2. 在 CI 加一步 `pytest --cov=core --cov-report=term-missing -m "not slow"`，允許失敗不阻擋（`continue-on-error`）。
  3. 把首次量測結果記入本文件 §覆蓋率基線，含**分模組**數字。
- **產出**：`pyproject.toml`、`.github/workflows/ci.yml`、本文件 §覆蓋率基線。
- **驗證方式**：報告可產出；數字記入文件。
- **相依**：S3。
- **預期發現**（撰寫本文件時的觀察，待 S6 量測確認）：`core/backtest/` 有 8 個測試檔、覆蓋較完整；`core/strategies/`、`core/api/`、`frontend/` 幾乎為 0。補測試本身不在本份範圍，應依基線結果另開 backlog。

---

## 例外處理現況

**量測日期：2026-08-16**（`ruff 0.16.3`，全專案 219 個 Python 檔）

套上 `select = ["E", "F", "I", "UP", "B", "BLE", "TRY"]` 的第一次全量結果為 **1495 條**：

| 規則 | 數量 | 處置 |
|------|-----:|------|
| E501 line-too-long | 597 | ignore（formatter 已管行寬，但拆不了中文註解與 docstring） |
| UP045 non-pep604-annotation-optional | 391 | ignore（與 `CLAUDE.md` §2.4 的 `Optional[T]` 直接衝突） |
| F401 unused-import | 134 | 自動修；`__init__.py` 的 re-export 改用 per-file-ignore |
| F541 f-string-missing-placeholders | 112 | 自動修 |
| **BLE001 blind-except** | **85** | **ignore，待收斂（見下）** |
| E402 module-import-not-at-top | 26 | per-file-ignore（`tests/`、`strategy_lab/` 的腳本式 bootstrap） |
| I001 unsorted-imports | 25 | 自動修 |
| UP042 replace-str-enum | 24 | ignore（與 §2.7 的 `class X(str, Enum)` 衝突） |
| TRY300 / TRY201 / TRY301 / TRY004 | 29 | ignore，待收斂 |
| UP007 non-pep604-annotation-union | 18 | ignore（同 §2.4） |
| B905 zip-without-explicit-strict | 15 | ignore（補 `strict=` 屬行為變更） |
| B027 empty-method-without-abstract | 9 | ignore（刻意的 no-op 掛點，例如股票的 `settle_daily`） |
| 其餘 | 30 | 見下方待收斂清單 |

**盲捕實測 85 條，與 2026-08-07 的粗估 81 條相符。** 決策：**另開 backlog 逐一收斂，不在本份修**——理由與原規劃一致（本份不改 `core/` 業務邏輯），且 85 條分散在 ETL 各層，逐一判斷「該重試還是該中止」需要領域知識。

### 待收斂清單（21 條，已於 `pyproject.toml` 的 `ignore` 標記）

**這些不是樣式問題，是真的可能出錯的地方**，修掉後要把對應規則從 `ignore` 移除：

| 規則 | 位置 | 為什麼要緊 |
|------|------|-----------|
| B008 ×5 | `stock_chip_updater.py:61`、`stock_dividend_updater.py:61`、`stock_margin_updater.py:65`、`stock_price_updater.py:63`、`stock_tick_updater.py:99` | 全部是 `end_date: datetime.date = datetime.date.today()`。**預設值在 import 當下就固定**，長駐排程跑過午夜後仍會拿到啟動那天的日期，等於靜默漏掉最新一天 |
| B006 ×4 | `financial_statement_crawler.py:269`、`data_utils.py:101/106/107` | 可變預設參數，跨呼叫共用同一個物件 |
| B904 ×3 | `finmind_updater.py:157`、`time.py:36/45` | `raise` 未帶 `from`，原始例外被吃掉，debug 時看不到根因 |
| E722 ×3 | `stock_tick_cleaner.py:155/163`、`stock_tick_utils.py:209` | 裸 `except`，會連 `KeyboardInterrupt` 一起吞 |
| F841 ×3 | `stock_price_crawler.py:39/40`、`generate_docs.py:65` | **`crawl_price()` 把 `crawl_twse_price()` / `crawl_tpex_price()` 的回傳值指派後完全沒用**，看起來像未完成的函式，需確認是否為缺陷 |
| F811 ×1 | `callback.py:7` | `OrderState` 同時從 `shioaji.constant` 與 `.constant` import，後者覆蓋前者 |
| B007 ×2 | `test_db_tables.py:129/142` | 未使用的迴圈變數，無害 |

### 施作過程的兩個發現

1. **`UP045`／`UP007`／`UP042` 差點造成大規模違規改寫**：原規劃只列了 `UP006`／`UP035` 要關掉，但 `UP` 家族實際上還有三條會把 `Optional[T]` → `T | None`（391 處）、`Union` → `|`（18 處）、`(str, Enum)` → `StrEnum`（24 處），**共 433 處與 `CLAUDE.md` §2.4／§2.7 直接衝突**。若照原規劃只關兩條就跑 `--fix`，會一次改掉 433 個違反專案規範的地方。
2. **`# noqa` 與 formatter 會打架**：原本想用「規則保持啟用 ＋ 逐點 noqa」的 ratchet 作法，但加上中文理由後該行超過 88 字元，`ruff format` 就把運算式拆成多行，noqa 註解跟著跑到別行、失去效力（實測 21 處全部失效）。最後改回「ignore ＋ 本文件逐條記錄」。

---

## S1~S6 施作紀錄補充（2026-08-16）

除了各步驟章節已寫的內容，另有三項與原規劃不同的發現：

1. **`core.managers` 等目錄沒有 `__init__.py`**，靠 PEP 420 namespace package 運作。`[tool.setuptools.packages.find]` 必須設 `namespaces = true`，否則 editable 安裝後 `core.managers.*` 會 import 不到——而且**不會在安裝時報錯，是執行期才炸**。
2. **`dolphindb` 與 `streamlit` 既未安裝也不在 `requirements.txt`**，但程式碼有 import（前者為惰性 import）。已改列為 optional extras（`tick`／`frontend`），不放進主 `dependencies`。
3. **本機 `.venv` 的 `.pth` 從未生效**：`.venv` 內所有檔案帶著 macOS 的 `UF_HIDDEN` 旗標，而 CPython ≥3.11 的 `site.addpackage()` 會**靜默略過隱藏的 `.pth`**。連 setuptools 自己的 `distutils-precedence.pth`（2026-05-05 起）也一直沒被執行。這是**環境問題不是專案問題**——S1 的驗收改在乾淨 venv 完成並通過。要讓本機的 editable 安裝生效，需執行：
   ```bash
   chflags nohidden .venv/lib/python3.12/site-packages/*.pth
   ```
   （本次已對兩個 `.pth` 執行過；若日後重建 venv 需再做一次。反向操作為 `chflags hidden`。）

---

## 覆蓋率基線

**量測日期：2026-08-16**　指令：`pytest -m "not slow" --cov=core --cov-report=term`
（207 passed / 9 deselected）

**整體：7112 行中未覆蓋 4269 行 → 40%**

| 模組 | 覆蓋率 | 說明 |
|------|-------:|------|
| `core/models/` | 77~100% | 資料骨架，回歸線會走到 |
| `core/backtest/models/` | 90~96% | `cost_model` 90、`fill_model` 92、`settlement_model` 90、`sizing` 96 |
| `core/managers/` | 88~97% | `stock/position_manager` 97 |
| `core/backtest/factory.py` | 97% | |
| `core/backtest/backtester.py` | 76% | |
| `core/backtest/report/reporter.py` | 38% | 出圖與報表產出未被回歸線覆蓋（呼應〈6.2 回歸雙線不經過 reporter〉） |
| `core/api/` | 32~87% | `stock_price_api` 87、`stock_dividend_api` 74、`stock_margin_api` 32、`stock_tick_api` 33 |
| `core/utils/` | 37~100% | `constant` 100、`instrument` 81、`path` 0 |
| `core/strategies/` | 0~89% | `base` 89、`stock/base` 84，但 `momentum_strategy_1` 26、`overnight_lead_event_strategy` 0、`strategy_loader` 0 |
| **`core/pipeline/`** | **0~39%** | **多數 loader／updater 為 0%**：`monthly_revenue_report_*`、`stock_chip_*`、`stock_dividend_*`、`stock_margin_*`、`stock_price_*`、`financial_statement_updater` 全部 0 |

**與原預期的差異**：撰寫本文件時預估「`core/strategies/` 幾乎為 0」，實測基底類別其實有 84~89%（被回測測試帶到），真正是 0 的是**具體策略**與 `strategy_loader`。`core/pipeline/` 則如預期，是最大的空白區。

**不設門檻**（見 §範圍界線）。補測試本身不在本份範圍，應依本基線另開 backlog；優先順序建議為 `core/pipeline/` 的 loader → `strategy_loader` → `reporter`。

---

## 關聯與狀態

- **優先級**：P1（與所有重構正交，重要性未變）
- **狀態**：**6 / 6 項全部完成（2026-08-16）**。整份可依 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理) 移出 `backlog/`，惟本文件的 §例外處理現況 與 §覆蓋率基線 具長期參考價值，建議改寫進 `docs/` 後再移出（尚未執行）
- **相依**：無（2026-08-07 確認，理由與取捨見上方〈時機〉）；建議不晚於期貨／美股／PostgreSQL 動工前完成
- **相關程式**：`pyproject.toml`（新增）、`core/config.py`、`core/utils/log_manager.py`、`tests/backtest/conftest.py`、`core/Dockerfile`、`frontend/Dockerfile`
- **相關 backlog**：
  - [多市場回測引擎架構](../docs/backtest/multi-market-engine.md)（Phase0-1 的 SHORT 快照完成後應納入 S3 的 CI；Phase4-3 的死碼清除與本份 §現況盤點 的 repo 衛生項可一併處理）
  - ~~`策略層資料欄位抽象化`~~（2026-08-08 完成並移出 `backlog/`）、~~`部位控管下沉引擎`~~（2026-08-09 完成並移出）——兩份原被列為本份的下游受益者，其完成反證了「沒有 CI 也做得完」，本份 `∥` 的判斷因此成立
