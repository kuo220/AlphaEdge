# 健檢第五輪收斂

## Abstract

- **背景／問題**：2026-09-15 對全 repo 做第五輪掃描（前四輪皆已結案，紀錄在 git 歷史）。**機器護欄全綠**——`pytest` 1071 passed、`ruff check`／`ruff format --check` 零違規、`check_layer_deps.py`／`check_doc_paths.py`／`check_api_orphan_methods.py` 三支皆零違規——所以本輪的發現全部來自逐檔閱讀，每條都以讀程式碼、唯讀查資料庫或假資料實跑驗證過。**本輪與前四輪最大的不同**：前四輪的 A／B 級幾乎都是「錯誤被吞成正常」，本輪的高嚴重度則集中在**回測做多路徑的記帳口徑**與**已經入庫的歷史資料錯誤**，兩者都不會觸發任何例外或護欄，只會讓數字安靜地偏掉。
- **目標**：做多與放空兩條路徑在公司行動、漲跌停成交、無報價出場、當沖稅上的處理口徑一致；已入庫的錯誤歷史資料修正並有護欄擋住復發；前端容器照 README 啟動可用；CI 上被跳過的回歸測試真的會跑；文件與規則回到 `manage-backlog` skill 與 `CLAUDE.md` 的規範。
- **範圍界線**：**不做**覆蓋率數字追求（第四輪已裁示，未被測試引用的模組只列參考、不開步驟）；**不動** [爬蟲缺口回補與非交易日批次清理](爬蟲缺口回補與非交易日批次清理.md) 已登記的項目（S8 股期、財報 crawler 單一市場失敗只 `continue`、`price` updater 補不回週六補行交易日）；**不動** DolphinDB 的去留決策（本輪只盤點殘留）；**不做** [PostgreSQL遷移計畫](PostgreSQL遷移計畫.md) 範圍內的欄位 Enum 與 `core/api` → `core/pipeline` 相依移除。
- **驗收標準**：進度表每一列 ✅ 或以使用者裁示轉 ⏸；`pytest -m "not slow"` 全綠且條數不減；`./scripts/run_regression.sh` 雙線通過，**會改變數值的步驟（進度表備註欄標「重產基準」者）須先說明差異來源再重產**；三支護欄腳本結束碼 0；每個修正資料的步驟，其驗證查詢在修正前實測會命中、修正後為 0。

## 進度追蹤表

嚴重度：**高**＝會算錯結果、遺失或污染資料、照文件做會壞；**中**＝特定情境出錯或維護風險大；**低**＝清理與一致性。

| 編號 | 步驟名稱 | 產出檔案 | 驗證方式 | 狀態 | 備註／中斷點 |
|------|----------|----------|----------|:----:|--------------|
| Phase1-1 | 做多部位的公司行動記帳 | `core/backtest/models/settlement_model.py`、`core/backtest/backtester.py`、`tests/backtest/` | 做多跨除息收到現金股利、跨配股／分割股數與成本調整；權益曲線在除權息日不再跳空 | ⬜ | **高**。重產基準 |
| Phase1-2 | 買進開倉在全日鎖漲停時拒單 | `core/backtest/models/fill_model.py`、`tests/backtest/` | 前收 100、OHLC 皆 110 的買單被拒並計入事件 | ⬜ | **高**。重產基準 |
| Phase1-3 | 當沖轉融券留倉不檢查帳戶餘額 | `core/backtest/models/settlement_model.py`、`tests/backtest/` | 餘額不足時依追繳政策處理並計數，餘額不再變負 | ⬜ | 中 |
| Phase1-4 | 做多平倉沿用放空策略的當沖稅設定 | `core/managers/stock/position_manager.py`、`tests/` | 隔夜賣出的做多部位稅率為一般稅率 | ⬜ | 中 |
| Phase1-5 | 做多部位無報價時永不出場 | `core/backtest/models/settlement_model.py`、`tests/backtest/` | 做多部位連續無報價達上限時強制出場並計數 | ⬜ | 中。可能重產基準 |
| Phase1-6 | 停券期間仍可開融券空單 | `core/backtest/datafeed/tw/`、`core/backtest/models/fill_model.py`、`tests/backtest/` | 回補日至除權息交易日之間的融券放空開倉被拒並計數 | ⬜ | 中 |
| Phase1-7 | `OvernightLeadEventStrategy` 成交時點晚訊號一天，研究端向量化版本有未來資料 | `core/strategies/stock/overnight_lead_event_strategy.py`、`strategy_lab/strategies/tsmc_overnight_signal/pipeline.py` | 成交價與模型目標報酬的區間對齊；研究端兩種回測口徑一致 | ⬜ | 中。先與使用者確認策略意圖 |
| Phase1-8 | 回測口徑小項：借券費不進每日權益、強制出場繞過 `FillModel`、`max_holdings` 算筆數 | `core/backtest/models/settlement_model.py`、`core/models/base/account.py` | 各一條會失敗的測試 | ⬜ | 低。後兩項重產基準 |
| Phase1-9 | 回測層死程式碼與重複實作 | `core/models/stock/`、`core/managers/futures/position_manager.py`、`core/backtest/models/settlement_model.py` | 零引用方法刪除；重複公式收斂到成本模型；`pytest` 全綠 | ⬜ | 低 |
| Phase2-1 | 日頻與公司行動 cleaner 回 `None` 時仍記為完成 | `core/pipeline/shared/base_updater.py`、`core/pipeline/tw/cleaners/{stock_margin,stock_dividend,corporate_action}_cleaner.py`、對應 updater | 版面不符的假資料讓當天記為失敗、下次重試、結束碼非 0 | ⬜ | **高**。半份資料入庫防線的漏洞 |
| Phase2-2 | `price` 2020-04-14 的 TWSE 半邊是 2020-12-18 的重播 | 資料修正、`core/pipeline/tw/cleaners/`、`tests/test_trading_calendar_guard.py` | 重播護欄擴及 `price` 並以修正前資料實測會失敗 | ⬜ | **高**。修資料，重產基準 |
| Phase2-3 | TWSE 籌碼 2014-12-01 前自營商買進／賣出股數被覆寫成 0 | `core/pipeline/tw/cleaners/stock_chip_cleaner.py`、資料重爬 | 2014-12-01 前「買賣皆 0 但買賣超非 0」的列數為 0 | ⬜ | **高**。修程式＋重爬約 23 個月 |
| Phase2-4 | TWSE `漲跌價差` 正負號被丟掉 | `core/pipeline/tw/cleaners/stock_price_cleaner.py`、資料修正 | TWSE 下跌日的 `漲跌價差` 為負 | ⬜ | 中。目前無程式讀此欄 |
| Phase2-5 | 月營收續跑改為差集，且半個市場有資料時判失敗 | `core/pipeline/tw/updaters/monthly_revenue_report_updater.py`、`core/pipeline/tw/crawlers/monthly_revenue_report_crawler.py` | 一邊 NO_DATA、一邊 OK 的月份不入庫；中間缺月會被補 | ⬜ | 中 |
| Phase2-6 | 台股線 3 處把 sqlite 錯誤吞成預設值 | `core/pipeline/tw/updaters/{monthly_revenue_report,financial_statement}_updater.py`、`tests/test_sqlite_error_semantics.py` | 資料庫被鎖時上拋；表不存在時行為不變 | ⬜ | 中。與第四輪收斂的期貨線同型 |
| Phase2-7 | price／chip 的 `stock_id` 前導 0 遺失 | `core/pipeline/tw/{crawlers,loaders}/stock_{price,chip}_*.py`、資料修正 | `chip`／`price` 無純數字且長度不足 4 碼的 `stock_id` | ⬜ | 中 |
| Phase3-1 | 日夜盤整併取錯夜盤（用了前一交易日那段） | `core/backtest/datafeed/tw/futures_datafeed.py`、`core/adapters/tw/futures_quote_adapter.py`、`tests/test_futures_session_combine.py` | slow 測試斷言「同日期夜盤開盤 ≈ 前一交易日日盤收盤」 | ⬜ | **高**。僅影響 `session=COMBINED` |
| Phase3-2 | 期貨日行情只拿到一個時段就算當天完成 | `core/pipeline/tw/updaters/futures_price_updater.py`、`docs/pipeline/etl-ingestion.md`、資料重爬 | 只回夜盤的假 crawler 讓當天記為未完成；全表時段缺口盤點零命中 | ⬜ | **高**。MTX 2026-09-02 缺日盤 |
| Phase3-3 | 期貨線與 FinMind 仍有 6 處把資料庫錯誤吞成「沒有資料」 | `core/api/tw/futures_{price,chip}_api.py`、`core/pipeline/tw/updaters/{futures_chip,futures_price}_updater.py`、`core/pipeline/tw/updaters/finmind/common.py` | 資料庫被鎖時上拋；表不存在時行為不變 | ⬜ | 中。與 Phase2-6 同批 |
| Phase3-4 | `futures_continuous` 整段重建不清舊列 | `core/pipeline/tw/loaders/futures_continuous_loader.py`、`core/pipeline/tw/updaters/futures_continuous_updater.py` | 晚起日重建後無殘留舊列；TX 預設重建位元級不變 | ⬜ | 中 |
| Phase3-5 | `FuturesChipAPI` 傳入 `datetime` 會拿到當天的籌碼 | `core/api/tw/futures_chip_api.py`、`tests/test_futures_chip.py` | 傳 `datetime` 與 `date` 結果一致 | ⬜ | 中。前視偏差，目前無呼叫端 |
| Phase3-6 | 股期契約單位有兩套取值規則 | `core/api/tw/futures_margin_api.py`、`core/api/tw/futures_stock_universe_api.py` | 同一檔同一天兩條路徑乘數相同 | ⬜ | 低 |
| Phase3-7 | DolphinDB 殘留盤點 | 依裁示 | 依裁示（見該節） | ⬜ | 低。需使用者裁示去留 |
| Phase4-1 | 前端容器 `streamlit run` 時 import 不到 `core` | `frontend/Dockerfile`、`docs/deployment/` | 實際 `docker compose up frontend` 後開頁不崩 | ⬜ | **高**（待實際容器重現） |
| Phase4-2 | 前端回歸測試在 CI 永遠整組 skip | `tests/test_frontend_report_loader.py`、`tests/fixtures/`、`.github/workflows/ci.yml` | CI log 顯示這 14 條實際執行 | ⬜ | 中 |
| Phase4-3 | 危險腳本與排程工具的預設行為與結束碼 | `scripts/fix_single_market_batches.py`、`tasks/delete_price_data.py`、`docker-compose.yml` | 不帶參數不刪資料；失敗結束碼非 0；compose 無誤導鍵 | ⬜ | 中 |
| Phase5-1 | 過程紀錄移出 `docs/` | `docs/dev/health-check-round4-2026-09.md`（刪）、`docs/dev/{code-quality,naming-axes}.md` | `docs/` 無進度表、步驟編號、健檢編號與施作日期 | ⬜ | 中 |
| Phase5-2 | 程式碼註解引用步驟與健檢編號，並補機器護欄 | `core/**`、`scripts/`、`tests/**`、`.pre-commit-config.yaml` | 新增的 pygrep hook 對全 repo 零命中 | ⬜ | 中 |
| Phase5-3 | `backlog/index.md` 檔頭的過程紀錄 | `backlog/index.md` | 檔頭只剩圖例、優先級與規範指標 | ⬜ | 低。相依欄膨脹與 S6 殘節已由同日 backlog 全份複查處理（未 commit） |
| Phase5-4 | README 與文件的小處漂移 | `README.md`、`README_zh.md`、`docs/dev/code-quality.md`、`docs/commands/command-usage.md`、`core/backtest/README.md`、`.claude/skills/develop-strategy/SKILL.md`、`.cursor/rules/strategy-development-sdd.mdc`、`CLAUDE.md` | 逐條比對與程式碼一致 | ⬜ | 中／低 |

## Phase1：回測引擎、部位管理與策略

### Phase1-1. 做多部位的公司行動記帳 ⬜

- **目的**：做多部位跨除權息時完全不處理，做多績效系統性偏低；放空（SBL）跨配股則反向憑空獲利。
  - `core/backtest/backtester.py:474-481`：只有允許 SHORT 時才去查股利表（註解明寫「純做多策略不可能有空單，故連查都不查」）。
  - `core/backtest/models/settlement_model.py:457-461`：收盤結算沒有空單就直接 `return`；`compensate_cash_dividend()`（`:528` 起）只迭代 `PositionType.SHORT`。
  - 盯市與平倉用的是原始收盤價（`core/adapters/tw/stock_quote_adapter.py` 的 `close=data.收盤價`）；全 `core/backtest`、`core/managers`、`core/models` 找不到依配股、分割、減資調整股數的程式。
  - **情境**：做多持有某檔跨過除息日（每股配 3 元）→ 收盤價跳空 −3 元，權益與 `realized_pnl` 各少 3 元 × 股數，帳戶沒有收到現金。配股或分割時張數不變、價格砍半，帳面憑空虧一半。
  - `core/backtest/README.md` 的「價格口徑」說還原價解決了除權息跳空被當成漲跌的問題——那只解決了**訊號面**，記帳面仍然憑空虧損。
- **做法**：
  1. `apply_cash_dividends()` 不再限定 SHORT 策略才餵資料；結算模型對所有部位處理：做多入帳現金股利、空單維持既有補償。
  2. 配股、分割、減資由 `corporate_action`／`stock_dividend` 表推導股數倍率，做多調整持股數與每股成本；空單補上配股的補償。
  3. 事件計數比照既有的 `dividend_compensation_*`，做多另開 `dividend_received_*`、`share_adjustment_*`。
- **產出**：`core/backtest/models/settlement_model.py`、`core/backtest/backtester.py`、`core/backtest/datafeed/tw/`、`core/backtest/README.md`、`tests/backtest/`。
- **驗證方式**：合成資料測試三個情境（現金除息、配股、分割），修正前實測權益在除權息日跳空、修正後連續；做多回歸基準重產，並在 commit 內說明差異來自股利入帳。
- **相依**：無。與 Phase1-5 同改 `settle` 的做多分支，建議同批施作。

### Phase1-2. 買進開倉在全日鎖漲停時拒單 ⬜

- **目的**：`TwStockFillModel.validate()`（`core/backtest/models/fill_model.py:205-244`）只檢查成交價落在 `[low, high]` 與 `[跌停, 漲停]`（含邊界）且有成交量。放空回補有 `TwStockSettlementModel.check_limit_up_locked()` 擋「漲停鎖死回補不了」，**買進開倉卻沒有對應判定**。前收 100、OHLC 全為 110 時買在 110 → `validate()` 回 `True`。`MomentumStrategy1` 的訊號正是「當日漲幅 ≥ 9%、以收盤價買進」，這類標的大量是鎖漲停、實務上排隊買不到，做多基準因此偏樂觀且無任何事件計數。
- **做法**：`validate()` 對「`Action.BUY` 開倉、且 `open == high == low == close == 漲停價`」拒單，計入新事件 `rejected_limit_up_locked`；對稱地，「賣出開倉（放空）遇全日鎖跌停」一併拒單。判定函式抽成 `InstrumentSpec` 或 `FillModel` 的共用方法，讓 `check_limit_up_locked()` 共用同一份。
- **產出**：`core/backtest/models/fill_model.py`、`core/backtest/models/settlement_model.py`、`core/backtest/README.md`（事件表）、`tests/backtest/`。
- **驗證方式**：上述合成情境修正前通過、修正後被拒；做多回歸基準重產並記錄被拒筆數。
- **相依**：無。

### Phase1-3. 當沖轉融券留倉不檢查帳戶餘額 ⬜

- **目的**：`convert_to_margin_position()`（`core/backtest/models/settlement_model.py:341-381`）把當沖空單轉為融券留倉時，直接從餘額扣保證金與費用，不檢查是否足夠。實跑：帳戶 10,000 元、當沖放空 500 元 × 1 張 → 轉融券後餘額 **−442,113**、`margin_used` 450,000，無拒絕、無計數。之後的維持率追繳只看單一部位的擔保維持率、不看帳戶現金，負餘額會一路留著。
- **做法**：保證金＋費用超過餘額時，依既有追繳政策強制回補（以收盤價），計入 `forced_cover_insufficient_margin`；若政策設定為不處理，至少計數並 `logger.warning`。
- **產出**：`core/backtest/models/settlement_model.py`、`tests/backtest/`。
- **驗證方式**：上述情境修正前餘額為負、修正後部位被回補且餘額 ≥ 0。
- **相依**：無。

### Phase1-4. 做多平倉沿用放空策略的當沖稅設定 ⬜

- **目的**：`close_long_position()`（`core/managers/stock/position_manager.py:292-297`）呼叫 `cost_model.tax()` 時沒傳 `is_day_trade`，`TwStockCostModel.tax()`（`core/backtest/models/cost_model.py:414-416`）遇 `None` 取 `config.is_day_trade`。放空且開啟當沖的策略，`build_cost_config()` 會把它設為 True；若該策略的 `allowed_directions` 也含 LONG，做多部位**隔夜**賣出也吃到減半稅。實跑：100 元 × 1 張、持有 7 天後賣出，稅 150（應為 300）。
- **做法**：做多平倉依「開倉日 == 平倉日」自行判定 `is_day_trade` 並明確傳入，不再沿用設定值。
- **產出**：`core/managers/stock/position_manager.py`、`tests/`。
- **驗證方式**：隔夜與當日兩個情境各一條測試；既有兩條回歸基準預期零變動（現有 SHORT 策略未允許 LONG）。
- **相依**：無。

### Phase1-5. 做多部位無報價時永不出場 ⬜

- **目的**：`max_no_quote_days` 與 `update_no_quote_days()` 只對空單生效（結算沒有空單就 `return`）。做多持有的股票一旦不再出現在 `price` 表（下市、長期停牌），`Backtester.execute_close_signal()`（`core/backtest/backtester.py:586-588`）不會把無報價標的交給策略，引擎也不強制出場；權益永遠以最後收盤價計算（下市損失不反映，屬存活者偏差），並持續佔用 `max_holdings` 名額。
- **做法**：無報價天數計數與強制出場擴及做多部位，與空單共用同一個上限與事件計數（`forced_exit_no_quote`）。出場價沿用空單路徑的口徑（最後可得收盤價），並在 `core/backtest/README.md` 註明這仍高估下市股的回收價。
- **產出**：`core/backtest/models/settlement_model.py`、`core/backtest/README.md`、`tests/backtest/`。
- **驗證方式**：合成「持有中的股票自某日起無報價」，修正前部位永久留存、修正後在上限日出場並計數。
- **相依**：與 Phase1-1 建議同批施作。

### Phase1-6. 停券期間仍可開融券空單 ⬜

- **目的**：引擎只在推導出的「融券最後回補日」那一天強制回補（`check_force_cover`），但**從回補日到除權息交易日這段停券期間**，開倉路徑（`TwStockFillModel.check_short_borrowable()` 只看券源餘額、`StockPositionManager` 開空單時也不查）沒有擋板。留倉放空策略在這段期間可開融券空單並持有跨過除權息。
- **做法**：DataFeed 除了回補日外，同時提供停券區間（回補日至除權息交易日）；`FillModel` 對 `ShortMethod.MARGIN` 的放空開倉在區間內拒單並計入 `rejected_short_suspended`。SBL 借券不受停券限制，不擋。
- **產出**：`core/backtest/datafeed/base.py`、`core/backtest/datafeed/tw/`、`core/backtest/models/fill_model.py`、`tests/backtest/`。
- **驗證方式**：停券區間內的融券放空被拒、區間外與 SBL 不受影響。
- **相依**：無。

### Phase1-7. `OvernightLeadEventStrategy` 成交時點晚訊號一天，研究端向量化版本有未來資料 ⬜

- **目的**：
  1. **正式策略**：模型目標 `r_2330(d)` 是 d−1 收盤到 d 收盤的報酬，特徵是 d 日台股開盤前可得的前一個美股交易日報酬（`core/strategies/stock/overnight_lead_event_strategy.py:127-160`）。但 `check_open_signal()`／`check_close_signal()` 以 d 日 `cur_price`（收盤價）成交，d 日訊號實際賺到的是 d+1 的報酬——**回測沒有在測模型真正預測的那段**。研究端 `strategy_lab/strategies/tsmc_overnight_signal/pipeline.py` 的 `run_backtest_with_signal()` 同樣以收盤成交，兩邊一致但都錯位。
  2. **研究端向量化版本**（同檔 `:295` 起）：`wealth *= 1.0 + sig[i] * r[i]` 等於在 d−1 收盤就依 d 日訊號進場，而美股收盤在台北時間 d 日清晨才出現，**屬未來資料洩漏**。其 docstring 只寫「理想化、可能高估」，沒有指出這一點。
- **做法**：**先與使用者確認策略意圖**（隔夜事件預期是「開盤跳空」還是「整日報酬」）。若為前者，模型目標改為 d−1 收盤到 d 開盤、成交改用 `quote.open`；若為後者，成交改用 d 日開盤價。研究端兩個回測函式同步修正，向量化版本不得使用 d−1 收盤進場。
- **產出**：`core/strategies/stock/overnight_lead_event_strategy.py`、`strategy_lab/strategies/tsmc_overnight_signal/pipeline.py`、該主題的 `reports/` 重產。
- **驗證方式**：`tests/test_research_production_parity.py` 擴充一條「訊號日與報酬區間對齊」的斷言；研究端兩種回測的年化報酬差距收斂到只剩手續費與整張的差異。
- **相依**：使用者對策略意圖的裁示。

### Phase1-8. 回測口徑小項 ⬜

- **目的**：三個彼此獨立、各自影響小的口徑不一致：
  1. **SBL 借券費不進每日權益**：`accrued_borrow_fee` 逐日計提在部位上但 `mark_position()`（`core/backtest/models/settlement_model.py:180-195`）的空單部位價值沒扣它，股利補償卻是除息當日就扣餘額。權益曲線持有期間偏高、回補日一次掉下，MDD 與日報酬失真。
  2. **強制出場繞過 `FillModel`**：`force_cover_position()`（`:416-434`）與期貨 `close_position_at()`（`:1132-1153`）以收盤價全量成交。`ForeignSellShortDayTradeStrategy` 設了雙邊 10 bps 滑價，策略自己送的回補單吃滑價、引擎的當沖日終／追繳／無報價強制回補不吃，同一策略兩種口徑。
  3. **`max_holdings` 算的是部位筆數**：`core/models/base/account.py:47-49`，引擎（`backtester.py:555`）與 `EqualWeightSizer`（`core/backtest/models/sizing.py:82`）都用它；`MomentumStrategy1` 允許加碼，同一檔加碼兩次佔兩個名額，但 docstring 與 `BaseStrategy` 都寫「檔數」。
- **做法**：(1) 空單部位價值扣除 `accrued_borrow_fee`；(2) 強制出場經 `fill_model.fill()` 套滑價（平倉不拒單）並計數；(3) 改數不重複 symbol，或把文件改成「部位筆數」——**先問使用者要哪個口徑**。
- **產出**：`core/backtest/models/settlement_model.py`、`core/models/base/account.py`、`core/backtest/models/sizing.py`、`tests/backtest/`。
- **驗證方式**：三項各一條會失敗的測試；(2)(3) 會改變 SHORT／LONG 基準，重產時分開 commit 以便歸因。
- **相依**：(3) 需使用者裁示口徑。

### Phase1-9. 回測層死程式碼與重複實作 ⬜

- **目的**：
  - 無正式呼叫端（只有測試或完全沒有）：`StockAccount.get_short_market_value`、`StockAccount.remove_positions_by_stock_id`、`StockAccount.get_last_open_position`、`TwFuturesCostModel.breakeven_points`。
  - 重複公式：`FuturesPositionManager.calculate_roi`（`core/managers/futures/position_manager.py:574-585`）與 `TwFuturesCostModel.roi` 相同，違反同檔 `calculate_pnl` 註解說的「一律走成本模型」。
  - 基底類別介面不實：`BaseSettlementModel.update_no_quote_days`（`settlement_model.py:78-102`）宣告為抽象卻帶股票型別的實作，`TwStockSettlementModel` 又逐字重寫；`get_mark_price` 不是抽象方法、本體 `pass`；`Backtester.execute_close_signal` 無部位時 `return`（`backtester.py:590-591`），標註卻是 `List`。
- **做法**：刪除零正式呼叫端的方法（連同只測它們的測試）；ROI 收斂到成本模型；基底方法改為真正的 `@abstractmethod`；`return` 改 `return []`。
- **產出**：上列檔案、`tests/`。
- **驗證方式**：`grep` 零引用；`pytest -m "not slow"` 全綠；回歸基準零變動。
- **相依**：Phase1-5 會改 `update_no_quote_days`，建議排在其後。

## Phase2：台股現股 ETL

### Phase2-1. 日頻與公司行動 cleaner 回 `None` 時仍記為完成 ⬜

- **目的**：2026-09-15 上線的「一邊沒拿到就整天不入庫」防線（`BaseDataUpdater.record_market_day()`）只看 crawler 結果，**cleaner 那一層有洞**：
  - `StockMarginCleaner.clean_margin()`（`core/pipeline/tw/cleaners/stock_margin_cleaner.py:187-192`、`:201-203`）欄位數不符或過濾後無有效列時回 `None` 而非拋例外；`BaseDataUpdater.clean_one()`（`core/pipeline/shared/base_updater.py:157-160`）遇 `None`／空表只記 warning、仍回 `True`，當天維持 OK、日期進入入庫批次並被 `record_complete()`。假資料實測（TWSE 17 欄、TPEX 正常）→ `day_status OK`，只入庫上櫃半邊；之後差集判定表內已有這天，**永遠不回頭補**。
  - 同類：`stock_price_updater.py:145-153` 某市場列數 ≤ `MIN_DF_ROWS_AFTER_CLEAN` 時跳過清洗，當天照樣入庫另一邊。
  - 同類但較輕：`StockDividendCleaner`（`:141-146`、`:164-169`）與 `CorporateActionCleaner`（`:126-131`）版面不符回 `None`，updater 只記「Cleaned … empty」、`UpdateStats` 仍算 OK、結束碼 0。這兩者每輪重掃整個區間，資料不會永久缺漏，但會**無聲地停止更新**——而除權息正是還原價的輸入。同檔 `parse_roc_date`／`parse_roc_slash_date`（`stock_dividend_cleaner.py:327`、`:338`）仍是 `except Exception`。
- **做法**：
  1. `clean_margin()`、兩支公司行動 cleaner 的版面不符改拋 `ColumnLayoutError`，比照 price／chip。
  2. `clean_one()` 把 `None`／空表算成失敗（回 `False`），當天記 FAILED、下次重試。
  3. `stock_price_updater` 列數門檻不足時當天記 FAILED。
  4. 日期解析的例外收窄到 `(ValueError, TypeError)`。
- **產出**：上列 cleaner／updater、`tests/test_stock_margin_cleaner.py`、`tests/test_partial_market_guard.py`、`tests/test_stock_dividend_cleaner.py`。
- **驗證方式**：以「一邊版面改制」的假資料跑 updater，修正前兩邊皆記完成、修正後當天 FAILED 且不入庫；公司行動 updater 在版面不符時結束碼非 0。
- **相依**：無。

### Phase2-2. `price` 2020-04-14 的 TWSE 半邊是 2020-12-18 的重播 ⬜

- **目的**：2026-09-15 唯讀查詢確認：`price` 在 `2020-04-14` 的 TWSE 部分與 `2020-12-18` 完全相同——2330 兩天皆為開 508、收 510、成交股數 40,625,502，而前後兩日收盤分別是 278.5 與 287.5。回測的交易日曆與還原價都讀 `price`，這天的上市收盤價整批錯誤（約 +80%），次日則是反向的假跳空。
  - `core/pipeline/tw/cleaners/corporate_action_detector.py` 的 `KNOWN_BAD_PRICE_DATES` 已知道這天、註解寫「資料本身要修」，但沒有對應待辦。
  - 爬蟲端的修復只擋住**非交易日**回傳的舊頁面；**交易日回了別天的頁面**攔不到——三支 cleaner（`stock_price_cleaner.py:83`、`stock_chip_cleaner.py:81`、`stock_margin_cleaner.py:205`）都蓋上請求日期，不比對來源表頭內的日期。
  - 重播護欄 `tests/test_trading_calendar_guard.py` 的 `test_no_stale_replayed_batches` 只比對 `chip`，不看 `price`。
- **做法**：
  1. 刪除 `price` 的 `2020-04-14`（先匯出 CSV 備份）後以 `--target price` 重爬；確認 `KNOWN_BAD_PRICE_DATES` 可以移除。
  2. 重播護欄擴及 `price`（比照 `chip` 的探針做法）。
  3. **待確認後施作**：TWSE 表頭第一層推測含「民國 X 年 M 月 D 日」，目前被 `droplevel(0)` 丟掉。先取一份真實 TWSE HTML 存成 fixture 確認格式，再讓 cleaner 比對來源日期與請求日期、不符判 FAILED。
- **產出**：資料修正、`tests/test_trading_calendar_guard.py`、`core/pipeline/tw/cleaners/`（第 3 點）、`core/pipeline/tw/cleaners/corporate_action_detector.py`。
- **驗證方式**：擴充後的護欄以修正前資料實測會失敗、修正後通過；重爬後 2330 在 2020-04-14 收盤與鄰日同量級；做多基準若涵蓋 2020-04 須重產。
- **相依**：無。與 Phase2-1 同批最省（同一組 cleaner）。

### Phase2-3. TWSE 籌碼 2014-12-01 前自營商買進／賣出股數被覆寫成 0 ⬜

- **目的**：`core/pipeline/tw/cleaners/stock_chip_cleaner.py:84-89` 不分日期一律執行 `df["自營商買進股數"] = df.get("…(自行買賣)", 0) + df.get("…(避險)", 0)`。第一次改制前來源沒有拆分欄、而是直接給 `自營商買進股數`，原值被 `0 + 0` 蓋掉，賣出同理。2026-09-15 唯讀查詢：2330 在 `2013-06-03` 為買進 0、賣出 0、買賣超 +307,000。
- **做法**：只在拆分欄存在時才相加（或以 `TWSE_FIRST_REFORM_DATE` 判斷）；修正後重爬 2013-01 ~ 2014-11 的 TWSE 籌碼（先匯出備份）。
- **產出**：`core/pipeline/tw/cleaners/stock_chip_cleaner.py`、新增 `tests/test_stock_chip_cleaner.py`（目前 `StockChipCleaner` 沒有任何測試）、資料重爬。
- **驗證方式**：`SELECT COUNT(*) FROM chip WHERE date < '2014-12-01' AND 自營商買進股數 = 0 AND 自營商賣出股數 = 0 AND 自營商買賣超股數 != 0` 修正前約 20.8 萬、修正後 0；以改制前版面的假資料測試 cleaner。
- **相依**：無。重爬期間與 [爬蟲缺口回補](爬蟲缺口回補與非交易日批次清理.md) 共用 TWSE 請求節奏，不要同時跑。

### Phase2-4. TWSE `漲跌價差` 正負號被丟掉 ⬜

- **目的**：`core/pipeline/tw/cleaners/stock_price_cleaner.py:75` 直接 `drop(columns=["漲跌(+/-)"])`。TWSE 的正負號只在這一欄、`漲跌價差` 本身是絕對值；TPEX 的 `漲跌價差` 則自帶正負號。同一張表上市、上櫃語意不同。2026-09-15 唯讀查詢：2330 在 `2025-04-07` 收盤 848（前一日 942），庫內 `漲跌價差 = 94.0`；2013~2016 全表沒有任何負值。目前沒有程式讀這一欄，所以列為中。
- **做法**：清洗時把 `漲跌(+/-)` 的正負號併進 `漲跌價差` 再刪欄；歷史資料以「本日收盤 − 前一交易日收盤」重算 TWSE 部分（不必重爬）。
- **產出**：`core/pipeline/tw/cleaners/stock_price_cleaner.py`、`tests/`、一次性資料修正腳本（scratchpad，不進版控）。
- **驗證方式**：TWSE 下跌日 `漲跌價差 < 0`；抽樣比對重算值與來源一致（除權息日例外需註明）。
- **相依**：建議排在 Phase2-2 之後（重算依賴正確的前一日收盤）。

### Phase2-5. 月營收續跑改為差集，且半個市場有資料時判失敗 ⬜

- **目的**：
  - `MonthlyRevenueReportUpdater` 的起點來自 `get_actual_update_start_year_month()`＝表內最新年月 +1（`core/pipeline/tw/updaters/monthly_revenue_report_updater.py:85-88`、`:157-184`），但 `docs/pipeline/etl-ingestion.md` 的對照表寫的是「年 × 月的差集」，文件與程式不符；中間缺月永遠不會補。
  - `combine_results()`（`core/pipeline/tw/crawlers/monthly_revenue_report_crawler.py:90-105`）只在有 FAILED 時判失敗；TWSE 兩次 NO_DATA、TPEX 一次 OK → `CrawlStatus.OK`，入庫半個市場。[爬蟲缺口回補](爬蟲缺口回補與非交易日批次清理.md) S3 補回的 2026/04「只有 26 檔」即此成因，當時只修資料、程式未改。財報三表已改成差集，月營收沒有跟進。
- **做法**：比照 `plan_pending_year_seasons()` 改為差集；任一市場 NO_DATA、另一市場 OK 時判 FAILED（與日頻三表 `record_market_day()` 同語意）。當月尚未公布的「兩邊皆 NO_DATA」維持不入庫、不算失敗。
- **產出**：上列兩檔、新增 `tests/test_monthly_revenue_report_updater.py`（月營收整條鏈目前沒有測試）、`docs/pipeline/etl-ingestion.md`（若語意有調整）。
- **驗證方式**：半市場情境修正前 OK、修正後 FAILED；表內人為挖掉中間一個月，修正後會被排入。
- **相依**：無。與 Phase2-6 同檔，建議同批。

### Phase2-6. 台股線 3 處把 sqlite 錯誤吞成預設值 ⬜

- **目的**：第四輪只收斂了期貨線，台股線還有 3 處同型：
  - `monthly_revenue_report_updater.py:176`：`except Exception` 後回預設起點 2013/1，資料庫被鎖時靜默從頭重爬約 164 個月 × 4 次請求。
  - `financial_statement_updater.py` 的 `get_target_stock_ids()`（約 `:1127`）例外時回 `[]`，外層印「No target stocks, skipped」就 return，權益變動表整段沒更新但結束碼 0。
  - 同檔 `get_crawled_stock_ids()`（約 `:1150`）例外時回空集合，整季兩千多檔全部重打，`is_season_filed()` 也失去判據。
- **做法**：比照 `SQLiteUtils.check_table_exist()`，只在表不存在時回空值，其餘錯誤上拋。
- **產出**：上列兩檔、`tests/test_sqlite_error_semantics.py`。
- **驗證方式**：對被另一連線鎖住的資料庫呼叫這 3 個方法，修正前靜默回預設、修正後拋出；表不存在時行為不變。
- **相依**：無。

### Phase2-7. price／chip 的 `stock_id` 前導 0 遺失 ⬜

- **目的**：`stock_chip_loader.py:122`、`stock_price_loader.py:149` 的 `pd.read_csv` 沒指定 dtype，`stock_chip_crawler.py:62`、`stock_price_crawler.py:59` 的 `read_html` 沒加 converters；只要某份檔案的代號全是數字，pandas 就推斷成整數。margin 兩層都有防護（`stock_margin_crawler.py:66`、`stock_margin_loader.py:121`），price／chip 沒有。2026-09-15 唯讀查詢：`chip` 在 `2014-03-11` 有 `stock_id='50'`（元大台灣50）、`'51'`（元大中型100）等，該日沒有 `0050`。
- **做法**：price／chip 兩層補 `dtype={"stock_id": str}` 與 `converters`；盤點全表純數字且長度 < 4 的 `stock_id`，以左補 0 修正（先確認補完後不與既有列衝突主鍵）。
- **產出**：上列 4 檔、`tests/`、一次性資料修正。
- **驗證方式**：`SELECT COUNT(*) FROM chip WHERE length(stock_id) < 4 AND stock_id GLOB '[0-9]*'`（`price` 同）修正後為 0；以全數字代號的假 CSV 測試 loader。
- **相依**：無。

## Phase3：期貨 ETL、FinMind 與資料 API

### Phase3-1. 日夜盤整併取錯夜盤 ⬜

- **目的**：`TwFuturesDataFeed.get_night_session_date()`（`core/backtest/datafeed/tw/futures_datafeed.py:330-351`）回傳**前一交易日**，docstring 的前提是「資料表把夜盤存在它開始的那個日曆日」。**庫內資料正好相反**——日期為 D 的夜盤列是前一交易日 15:00 開盤、制度上屬於交易日 D 的那一段。2026-09-15 唯讀查詢 `tw_futures.db`：
  - 夜盤制度 2017-05-15 晚上上線，但 TX 第一筆 `night` 列是 `2017-05-16`；`2017-05-15` 只有 `day`。
  - 2023 年起 TX 近月樣本：夜盤開盤與**前一交易日日盤收盤**的差距中位數 14 點（839 筆），與**同日期日盤收盤**的差距中位數 156 點（883 筆）。
  - `core/pipeline/tw/crawlers/futures_price_crawler.py:38-39` 的註解「日盤表列的盤後成交量與夜盤成交量一致」也與此相符。
  - 後果：`session=COMBINED` 的 bar D 整併的是屬於 D−1 的夜盤，真正緊接在 D 日盤之前的那段被挪到 D+1——隔夜跳空的 open、high/low、volume 全部錯一天（不是前視偏差，但序列是錯的）。預設 `DAY` 不受影響。`tests/test_futures_session_combine.py:19-22`、`:237-260` 的 fixture 把錯的假設固化成了測試。
- **做法**：`get_night_session_date()` 改回傳 `date` 本身（仍保留 2017-05-15 前回 `None`）；同步更正 `core/adapters/tw/futures_quote_adapter.py:114-119`、`core/backtest/datafeed/tw/futures_calendar.py:294` 的 docstring 與測試 fixture；新增一條 slow 測試斷言「同日期夜盤開盤 ≈ 前一交易日日盤收盤」，以真實資料固定這個口徑。
- **產出**：上列檔案、`tests/test_futures_session_combine.py`、`docs/futures/tw-futures-platform.md`（若有描述整併口徑）。
- **驗證方式**：新 slow 測試修正前失敗、修正後通過；盤點是否有策略或基準使用 `COMBINED`，有則重產。
- **相依**：無。

### Phase3-2. 期貨日行情只拿到一個時段就算當天完成 ⬜

- **目的**：`FuturesPriceUpdater.crawl_and_clean_date()`（`core/pipeline/tw/updaters/futures_price_updater.py:368-403`）「任一時段有資料即回傳 True」，續跑取 `MAX(date)+1`（`:108-142`），缺的那一段永遠不再被請求。TAIFEX 被擋時回 HTTP 200 加一頁 HTML，與查無資料分不開，只被擋一段時完全不會重試。2026-09-15 唯讀查詢：
  - **MTX `2026-09-02` 只有 `night` 8 列、沒有 `day`**，而 MTX 的 `MAX(date)` 已推進到之後；`MTX_night_20260902.csv` 寫入時間換算台北為 09-02 11:15（日盤尚未收盤），`MTX_day_20260902.csv` 不存在。`futures_continuous` 的 MTX 也因此停在 09-01。
  - TE 在 `2024-06-18`、`2024-07-03` 有日盤、沒有夜盤，也沒有夜盤 CSV。
  - `docs/pipeline/etl-ingestion.md` §3.5 的「一邊沒拿到就整天不入庫」只涵蓋台股日頻三表與財報三表，期貨的時段層完整性沒有規範。
- **做法**：
  1. 該日為有夜盤制度的交易日而任一時段缺 → 不算完成、記 `incomplete`、下次重試（比照台股 `record_market_day()` 的語意）；至少「只有夜盤、沒有日盤」不可推進續跑起點。
  2. 盤點全表「有 `day` 無 `night`」與「有 `night` 無 `day`」的交易日（排除 2017-05-15 前與已知無夜盤日），列出後重爬；補完 MTX 後以 `--target futures_continuous` 重建。
  3. `etl-ingestion.md` §3.5 補上期貨時段層的規範。
- **產出**：`core/pipeline/tw/updaters/futures_price_updater.py`、`tests/test_futures_price_updater.py`、`docs/pipeline/etl-ingestion.md`、資料重爬。
- **驗證方式**：只回夜盤的假 crawler 讓當天記為未完成；盤點查詢修正後零命中（已知例外寫進測試）；MTX 連續契約追到最新交易日。
- **相依**：Phase3-1 的「同日期夜盤」口徑決定盤點查詢怎麼配對，建議排在其後。

### Phase3-3. 期貨線與 FinMind 仍有 6 處把資料庫錯誤吞成「沒有資料」 ⬜

- **目的**：第四輪只 `grep` 了 `except sqlite3.OperationalError`，下列位置用的是別的例外名稱。資料庫被鎖時 `pd.read_sql_query` 拋的是 `pd.errors.DatabaseError: ... database is locked`（已實測）。
  - `core/api/tw/futures_price_api.py:238-252`：`get_trading_days()` 攔 `DatabaseError` 回 `[]`；`TwFuturesDataFeed.build_calendar()` 因此在 DB 被鎖時拿到空日曆，整場回測沒有交易日也不報錯。
  - `core/api/tw/futures_chip_api.py:125-134`：`get_on_date()` 同樣寫法。
  - `core/pipeline/tw/updaters/futures_chip_updater.py:342-355`：`has_trading_days()` 的 docstring 說「行情表不存在時回 True」，但上游已回 `[]`，實際回 False，自己的 `except: return True` 是死碼；被擋的月份不列入 blocked、結束碼 0，之後 `MAX+1` 越過它。
  - `core/pipeline/tw/updaters/futures_price_updater.py:128-136`：`get_actual_update_start_date()` 攔 `sqlite3.Error` 退回預設起日，等於從 2015 整段重爬。
  - 同檔 `:165-177`：`get_traded_weekend_dates()` 攔 `Exception` 一律跳過週末，補行交易日沒爬到、之後不回頭。
  - `core/pipeline/tw/updaters/finmind/common.py:198-226`：`get_stock_list()`／`get_securities_trader_list()` 攔 `Exception` 回 `[]`，`broker_trading_updater.py:89-111` 只記 warning 就 return，結束碼 0。
- **做法**：比照第四輪對期貨線的收斂——先 `check_table_exist()` 判斷表是否存在，其餘錯誤一律上拋；`has_trading_days()` 的「表不存在」語意改由明確判斷實現並刪掉死碼。
- **產出**：上列檔案、`tests/test_sqlite_error_semantics.py`。
- **驗證方式**：對被另一連線鎖住的資料庫逐一呼叫，修正前靜默回空、修正後拋出；表不存在時行為不變。收尾以 `grep -rnE "except (sqlite3\.|pd\.errors\.|DatabaseError|Exception)" core/api core/pipeline` 逐筆確認剩下的都有正當理由。
- **相依**：與 Phase2-6 同型，建議同批施作並共用測試骨架。

### Phase3-4. `futures_continuous` 整段重建不清舊列 ⬜

- **目的**：`FuturesContinuousLoader.add_to_db()`（`core/pipeline/tw/loaders/futures_continuous_loader.py:115-147`）只做 `INSERT OR REPLACE`，`FuturesContinuousUpdater`（`futures_continuous_updater.py:80-143`）號稱整段重建，但只有主鍵相同的列會被覆寫：以較晚的 `start_date` 重建時，起日之前的舊列保留上一代的 `adj_factor`，兩代交界那次換月的價差沒有調整；日後刪掉 `futures_price_daily` 的壞日，對應的連續契約列也會一直留著——正是該檔 docstring 說要避免的「混著兩代基準」。目前 `tasks/update_db.py` 呼叫 `update()` 不帶參數，日常更新不會觸發，但公開參數允許。
- **做法**：重建前在同一個 transaction 內 `DELETE` 該 `(product, session, method, roll_rule)` 的所有列，再寫入新序列。
- **產出**：上列兩檔、`tests/test_futures_continuous.py`。
- **驗證方式**：先以早起日建、再以晚起日重建，修正前表內殘留起日之前的列、修正後沒有；TX 以預設參數重建後 sha256 仍與重建前位元級相同。
- **相依**：無。

### Phase3-5. `FuturesChipAPI` 傳入 `datetime` 會拿到當天的籌碼 ⬜

- **目的**：`get_latest_available_date()`（`core/api/tw/futures_chip_api.py:106-108`）以 `str(date)` 組參數、做**嚴格小於**比較，沒有走 `sql_params()`。傳 `datetime.date(2026, 8, 28)` 回 `'2026-08-27'`（正確）；傳 `datetime.datetime(2026, 8, 28, 9, 0)` 回 `'2026-08-28'`，因為字串比較 `'2026-08-28' < '2026-08-28 09:00:00'` 成立，`get_available()` 因此回傳當天盤後才公布的籌碼。目前回測沒有呼叫端，但這支 API 存在的理由就是防前視。保證金與標的池 API 同樣用 `str(date)`，但它們是 `<=` 比較，不受影響。
- **做法**：改用 `self.sql_params(date)`；順手把保證金與標的池 API 的 `str(date)` 一併改掉，避免日後改成嚴格比較時重演。
- **產出**：`core/api/tw/futures_chip_api.py`、`core/api/tw/futures_margin_api.py`、`core/api/tw/futures_stock_universe_api.py`、`tests/test_futures_chip.py`。
- **驗證方式**：傳 `datetime` 與傳 `date` 的結果一致，修正前該測試失敗。
- **相依**：無。

### Phase3-6. 股期契約單位有兩套取值規則 ⬜

- **目的**：`core/api/tw/futures_margin_api.py:221-258` 以該商品自己的 `snapshot_date <= date` 取值、查不到退回最早一份；`core/api/tw/futures_stock_universe_api.py:140-169` 先解出**全表**適用的快照日再查該商品，商品若在那份快照之後才掛牌就回 `None`，`FuturesDataFeed.resolve_multiplier()` 隨即拋 `KeyError`。保證金計算（`calculate_stock_futures_margin()`）與 PnL 乘數可能不一致；保證金 API 的版本也沒有檢查表是否存在，全新環境直接拋 `OperationalError`，與同檔其他方法回 `None` 的語意不同。目前沒有策略交易股票期貨，故列為低。
- **做法**：只留標的池 API 一個實作，保證金 API 改為委派；取值規則統一並寫進 docstring。
- **產出**：上列兩檔、`tests/test_futures_stock_universe_api.py`、`tests/test_futures_margin.py`。
- **驗證方式**：同一檔同一天兩條路徑取得相同乘數；「快照後才掛牌」情境有明確測試。
- **相依**：無。可待 [爬蟲缺口回補](爬蟲缺口回補與非交易日批次清理.md) S8 解除（開始開發股期策略）時再做。

### Phase3-7. DolphinDB 殘留盤點 ⬜

- **目的**：2026-09-14 使用者裁示不再使用 DolphinDB（台股 tick 不回補、期貨 tick 不做），但專案內殘留大量相關程式、設定、文件與測試。本步驟**只提供清單、等使用者裁示去留**，不預設要刪。
  - **程式**：`core/api/tw/stock_tick_api.py`；`core/pipeline/tw/crawlers/{stock,futures}_tick_crawler.py`、`cleaners/{stock,futures}_tick_cleaner.py`、`loaders/{stock,futures}_tick_loader.py`、`updaters/{stock,futures}_tick_updater.py`、`core/pipeline/tw/utils/stock_tick_utils.py`；`core/backtest/datafeed/tw/stock_datafeed.py` 的 `Scale.TICK` 分支；`tasks/update_db.py` 的 `tick`／`futures_tick` target；`scripts/manual/manual_tick_{crawler,updater}.py`、`manual_init_tick_metadata.py`。
  - **設定**：`core/config/settings.py`（`TICK_UPDATE_START_DATE`、`DDB_*`、tick 爬蟲多帳號 `API_KEYS`）、`core/config/schema.py`（`TICK_DB_*`、`require_tick_db_path()`、`TICK_TABLE_NAME`、`FUTURES_TICK_TABLE_NAME`）、`core/config/__init__.py`、`core/pipeline/utils/constant.py`、`pyproject.toml`（`tick` extra 與 per-file-ignores）、`.env.example`。
  - **測試**：`tests/test_futures_tick.py`、`tests/test_entrypoint_and_logging.py`、`tests/test_strategy_data_access.py`、`tests/test_api_public_interfaces.py`、`tests/test_config_consistency.py` 的 tick 相關段落。
  - **文件**：兩份 README、`strategy_lab/README.md`、`scripts/manual/README.md`，以及 `docs/` 下 `pipeline/etl-ingestion.md`、`futures/tw-futures-platform.md`、`setup/dev-setup.md`、`exchanges/data_coverage.md`、`backtest/module-map.md`、`deployment/dev-deployment.md`、`dev/runtime-artifacts.md`、`dev/code-quality.md`、兩份 `commands/command-usage*.md`。
  - **本機資料**（不在版控）：`data/downloads/tw_stock/tick/` 541 個 CSV 與 `tick_metadata.json`、`data/downloads/tw_futures/tick/` 1 個 CSV。
- **做法**：使用者裁示後二選一——(a) 整條移除，程式、設定、測試、文件同一個 commit 收掉，`Scale.TICK` 回測分支一併移除；(b) 保留程式但從 `--target all` 與文件主流程拿掉，並在 `README` 註明「已停用」。
- **產出**：依裁示。
- **驗證方式**：(a) `grep -rniE "dolphindb|ddb_|tick_db" --include='*.py' --include='*.md' --include='*.toml' .` 零命中（歷史紀錄除外）且 `pytest` 全綠；(b) `--target all` 不再觸及 tick。
- **相依**：使用者裁示。

## Phase4：前端、測試、CI 與腳本

### Phase4-1. 前端容器 `streamlit run` 時 import 不到 `core` ⬜

- **目的**：`frontend/Dockerfile` 的 `WORKDIR /app`、`CMD ["streamlit", "run", "frontend/app.py", ...]`，映像不安裝本專案、也沒設 `PYTHONPATH`（`core/Dockerfile` 改 editable 安裝時拿掉了 `PYTHONPATH=/app`，前端映像沒有對應替代）。Streamlit 只把主程式所在目錄（`/app/frontend`）加進 `sys.path`，`frontend/services/metrics.py` 的 `from core.backtest.analysis.risk_metrics import ...` 會 `ModuleNotFoundError`，`app.py` 的 fallback `from frontend.config import ...` 同樣找不到。以 Dockerfile 的 COPY 清單在本機模擬 `sys.path`，兩個 import 都失敗。本機不出事是因為 editable 安裝讓 `core` 可 import；容器驗收用 Streamlit AppTest 以 `python` 啟動時 cwd 在 `sys.path` 裡，所以測不出這條路徑（推測成因）。
- **做法**：
  1. **先以實際容器重現**（`docker compose up frontend` 後開頁）；2026-09-15 掃描時本機 Docker daemon 未啟動，未能重現。
  2. 重現後在 `frontend/Dockerfile` 加 `ENV PYTHONPATH=/app`。
  3. 容器驗收改為真的 `streamlit run` 後以 `curl` 或 headless 瀏覽器開頁確認無錯誤，不只用 AppTest。
- **產出**：`frontend/Dockerfile`、`docs/deployment/`。
- **驗證方式**：容器啟動後首頁 HTTP 200 且頁面無 `ModuleNotFoundError`。
- **相依**：本機 Docker 可用。若第 1 步無法重現，本步驟改記「偏離原規格」並寫明實際原因。

### Phase4-2. 前端回歸測試在 CI 永遠整組 skip ⬜

- **目的**：`tests/test_frontend_report_loader.py:43-55` 的 fixture 是 `results/Foreign-Sell-Short-Day-Trade/`，被 `.gitignore:14 results/` 忽略、未進版控，`pytestmark = skipif(not _FIXTURE_DIR.is_dir())` 在 CI 必定成立。被跳過的 14 條正是防止「平均 ROI 乘 100」「起始資金取錯」「依 `Sell Date` 排序」「MDD 用已實現口徑」四個舊 bug 復發的測試。CI 的 `pytest -m "not slow" --timeout=300`（`.github/workflows/ci.yml:69`）沒有 `-rs`，skip 只顯示為一個 `s`，看起來是綠的。
- **做法**：從該報表目錄抽一份縮小版 CSV（trading_report、daily_equity、direction_summary、event_report）放進 `tests/fixtures/frontend_report/` 並納入版控，期望值改為對縮小版重算；CI 的主測試步驟加 `-rs`。
- **產出**：`tests/test_frontend_report_loader.py`、`tests/fixtures/frontend_report/`、`.github/workflows/ci.yml`。
- **驗證方式**：在沒有 `results/` 的乾淨 clone 跑該檔，14 條實際執行且通過；把 ROI 計算故意改回乘 100，測試會失敗。
- **相依**：無。

### Phase4-3. 危險腳本與排程工具的預設行為與結束碼 ⬜

- **目的**：
  1. `scripts/fix_single_market_batches.py:166-227` **不帶參數就直接 `DELETE` 並 `commit`**，`--dry-run` 要自己加；同類工具 `tasks/delete_price_data.py` 是預設預覽、`--apply` 才刪、還要互動確認。它服務的 [爬蟲缺口回補](爬蟲缺口回補與非交易日批次清理.md) S10 已完成，且半份資料入庫防線已上線，留著只剩誤跑風險。`--backup-dir` 預設是相對路徑，不跟隨 `ALPHAEDGE_DATA_DIR`。
  2. `tasks/delete_price_data.py` 失敗一律結束碼 0：實測 `--date 2025-13-45` 印出「日期解析失敗」後結束碼 0；DB 不存在時 `sqlite3.connect()` 建出 0 byte 的 `tw_stock.db`。docstring 說 `--yes` 是給排程用的，排程端看不出失敗。
  3. `docker-compose.yml:7-17` 的 `core` 服務 `environment: MODE/STRATEGY` 不會影響 `command` 裡的 `${MODE:-backtest}`（那是 compose 解析階段由 shell 環境或 `.env` 替換）；有人改 `environment` 想換策略時會靜默仍跑 `MomentumStrategy1`。
- **做法**：(1) 使用者決定刪除該腳本，或改為預設預覽＋`--apply`＋確認；(2) 錯誤路徑 `sys.exit(1)`，連線改 `file:{path}?mode=rw` URI；(3) 刪掉兩個誤導的 `environment` 鍵，改以註解說明覆寫方式。
- **產出**：上列三檔。
- **驗證方式**：(1) 不帶參數執行不寫入；(2) 兩個錯誤情境結束碼 1 且不產生空 DB；(3) `docker compose config` 輸出不再含該兩鍵。
- **相依**：(1) 需使用者裁示刪除或改寫。

## Phase5：文件與規則

### Phase5-1. 過程紀錄移出 `docs/` ⬜

- **目的**：`manage-backlog` skill §5 規定 `docs/` 只放現行說明，「進度表、完成紀錄、驗收數字、事故經過、日期與 commit 一律不帶過去」。但：
  - `docs/dev/health-check-round4-2026-09.md`（333 行）是第四輪的完整完成紀錄，含進度追蹤表與驗收結果表，兩份 README 的文件索引都沒列它。commit `c6bab3e` 訂下規範後 2.5 小時，`21d3722` 仍把它搬進 `docs/`，之後 `7f44fec` 還回頭改它的內文——代表這份「紀錄」得跟著現況維護。
  - 殘留片段：`docs/dev/code-quality.md:96-98`（「於 2026-09-13 納入 `select`（健檢第四輪 S4）……累積 181 處缺漏」）、`docs/dev/naming-axes.md:4-6`、`:53`、`:98`、`:111`、`:115`（實作日期、Phase 編號、F 編號）。
- **做法**：刪除 `health-check-round4-2026-09.md`（留在 git 歷史），其中仍成立的設計理由（`.ruff_cache` 的判斷、抽象方法不能自動補標註等）併進 `docs/dev/code-quality.md`；兩份文件的殘留片段只保留定案內容與理由。
- **產出**：上列檔案。
- **驗證方式**：`grep -rnE '健檢第|Phase[0-9]-[0-9]|F-0[0-9]{2}|20[0-9]{2}-[0-9]{2}-[0-9]{2}' docs/dev/` 只剩合理用法（例如制度生效日）；`check_doc_paths.py` 結束碼 0。
- **相依**：無。

### Phase5-2. 程式碼註解引用步驟與健檢編號，並補機器護欄 ⬜

- **目的**：`CLAUDE.md` §2.1 第 4 點規定註解不引用 backlog 步驟編號與健檢發現編號。2026-09-15 以 `grep` 查得 `core/` 11 處、`scripts/` 1 處、`tests/` 約 19 處，代表位置：`core/api/base.py:62`（「健檢第四輪 S1，與 F-056 同型」）、`:66`（F-007）、`core/api/tw/futures_chip_api.py:102`、`futures_stock_universe_api.py:73`、`futures_margin_api.py:99`（「（S1）」）、`core/pipeline/tw/loaders/futures_chip_loader.py:167`、`core/pipeline/tw/cleaners/corporate_action_detector.py:49`（直接放 backlog 相對連結）、`core/pipeline/utils/data_utils.py:74`（「健檢 F-037」）、`scripts/fix_single_market_batches.py:1`。其中數處是規範訂下**之後**才新增的，這條規則沒有機器護欄。
- **做法**：逐處改寫為自足的理由；`.pre-commit-config.yaml` 比照既有的 `no-stdlib-exc-info` 加一條 pygrep hook（例如比對 `健檢|F-0\d\d|backlog/|（S\d+[）、]`），`tests/` 若刻意保留歷史脈絡須在 hook 內明列排除並寫理由。
- **產出**：上列 `.py`、`.pre-commit-config.yaml`。
- **驗證方式**：`pre-commit run --all-files` 通過，且在任一檔案加入「（S1）」註解時該 hook 失敗。
- **相依**：Phase4-3 若刪除 `fix_single_market_batches.py`，該處自然消失。

### Phase5-3. `backlog/index.md` 檔頭的過程紀錄 ⬜

- **目的**：`backlog/index.md` 維護規則第 6 點規定本檔只放通則，施作紀錄寫在各文件自身。檔頭仍有兩段過程紀錄：「2026-08-15：『梯次』欄已移除」（說明一個已不存在的欄位為何被拿掉）與「2026-09-15：全份複查」。
  - 掃描當下（2026-09-15）發現的另兩個問題——爬蟲缺口列「相依」欄膨脹到約 3,000 字且自相矛盾、`爬蟲缺口回補與非交易日批次清理.md` 已移除的 S6 仍留有完整待做章節——**已由同日進行中的 backlog 全份複查處理**（撰寫本文時該複查尚未 commit；複查後各列相依欄 138~364 字、S6 章節已移除），故不列入本步驟。若該複查未 commit 即被丟棄，須把這兩項加回本步驟。
- **做法**：刪除兩段檔頭說明；「相依」欄仍保留的判斷日期維持現狀（維護規則第 5 點允許寫「該判斷的日期與理由」）。
- **產出**：`backlog/index.md`。
- **驗證方式**：檔頭只剩圖例、優先級與規範指標。
- **相依**：先確認 2026-09-15 的 backlog 全份複查已 commit。

### Phase5-4. README 與文件的小處漂移 ⬜

- **目的**：
  1. **（中）CI 步驟描述落後**：`.github/workflows/ci.yml` 已加文件路徑檢查與 API 死介面檢查、`.pre-commit-config.yaml` 已加 `check-doc-paths`，但 `README.md:201-205`、`README_zh.md` 對應段落、`docs/dev/code-quality.md:14` 與其「CI 跑得到與跑不到的護欄」表都沒列這兩支。
  2. **（低）英文版指令教學少期貨指引**：`docs/commands/command-usage.zh-TW.md:111` 有導向台期貨平台〈指令〉的段落，英文版 `docs/commands/command-usage.md` 沒有，看英文版的人找不到 `futures_continuous`／`futures_margin`／`futures_chip`／`futures_tick` 的說明。
  3. **（低）`core/backtest/README.md` 過時**：`:29` 引用不存在的 `core/strategies/stock/simple_long_strategy.py`；`:311` 寫策略只從 `stock/` 載入，實際 `strategy_loader` 也掃 `futures/`；參數說明沒有 `--show`／`--no-show`。`develop-strategy` skill 會把讀者導到這份。
  4. **（低）策略開發 skill 的觸發描述只涵蓋台股**：`.claude/skills/develop-strategy/SKILL.md:2` 與 `.cursor/rules/strategy-development-sdd.mdc:2` 只寫 `core/strategies/stock/` 與 `BaseStockStrategy`，但 `CLAUDE.md` 的 skill 表與 skill 本文都涵蓋期貨，開發期貨策略時可能不會觸發。
  5. **（低）`CLAUDE.md` 小處不一致**：skill 表 `manage-backlog` 列的「對應 Cursor Rule」為「—」，但 `.cursor/skills/manage-backlog/SKILL.md` 存在且確實是指標；§2.6 命名表的範例 `_ridge_fit_predict()` 在全專案不存在（現存的是無底線的 `ridge_fit_predict`）。
- **做法**：逐條修正；第 4、5 條改的是規則權威檔，**`.cursor/` 側只改指標描述、不複製本文**。
- **產出**：上列檔案。
- **驗證方式**：逐條比對程式碼；`check_doc_paths.py` 結束碼 0。
- **相依**：無。

## 附錄：本輪未列為步驟的觀察

- **測試涵蓋缺口（僅供參考，依第四輪裁示不追覆蓋率）**：以類別名比對，下列 ETL 模組在 `tests/` 完全沒有被引用——`StockChipCrawler`／`StockChipCleaner`／`StockChipLoader`、`StockMarginCrawler`、月營收整條鏈（Cleaner／Loader／Updater／API）、`FinancialStatementLoader`、`BrokerTradingUpdater`、`FuturesStockUniverseUpdater`、`StockDividendCrawler`／`StockDividendUpdater`、`CorporateActionCrawler`／`CorporateActionUpdater`、`FuturesMarginCrawler`、`FuturesContinuousLoader`、`URLManager`、tick 叢集。Phase2-3、Phase2-5 會順帶補上其中兩條鏈的第一條測試。
- **本輪確認沒有問題、下一輪可跳過**：兩份 README 文件索引連結全部存在；`--target`／`--mode`／`--strategy` 與文件一致；`.cursor/` 6 個檔全是指標；`strategy_lab/` 目錄規範無違規；`.gitignore` 對 `.env`、`.coverage`、`shioaji.log`、`*.egg-info/`、`data/`、`logs/`、`results/` 皆命中；版控內無 `.env`、`.db`、金鑰或 log，歷史中也未加入過；`requirements.txt` 鎖定版本皆滿足 `pyproject.toml` 下限，ruff 版本與 pre-commit 一致；15 個爬蟲／ETL 測試檔在阻斷網路下 271 passed；非 slow 測試不觸碰真實 DB；前端與 reporter 的欄名、檔名一致；漲停價浮點相等比較隨機 2 萬個前收全部相等。
