# 還原價缺非除權息的公司行動

> **📌 2026-09-05：S1 調查完成，本文件的範圍被推翻並改寫。**
> 原標題是「還原價缺分割事件」，起因是 0050 一拆四。S1 實查後發現**分割只是最小的那一塊**——
> 真正的大宗是**減資**：光上市就有 355 筆可算倍率的事件、影響 243 檔，
> 且**沒有任何一筆出現在 `dividend` 表**。檔名與標題已隨之改寫。

## Abstract

- **背景／問題**：`StockPriceAPI.get_adjusted_close_series()` 的還原係數來自 `dividend` 表，
  而該表只記除權息（`權息別` 只有「息」「權」「權息」，來源是 TWSE／TPEx 的除權息公告）。
  **所有不走除權息流程的公司行動都不在裡面**——減資（彌補虧損／退還股款）、面額變更、
  股票分割、ETF 受益權單位分割、槓桿反向 ETF 的反向分割。這些事件同樣會讓價格在一天之內
  跳動數倍，而還原價完全沒有處理，序列在那一天留下一個假跳空。
- **目標**：`get_adjusted_close_series()` 對上述所有事件都不再留假跳空，且不需要呼叫端各自記得補；
  `core/api/tw/stock_split.py` 那份只認得 0050 的過渡表可以刪掉。
- **範圍界線**：**不動除權息還原邏輯**（`還原係數` 那條路徑已驗證正確）；不改 `dividend` 表既有
  欄位語意；不碰 reporter 與 analyzer 的呼叫端（它們現在走 `stock_split.py`，資料源補上後才一併收斂）。
- **驗收標準**：全市場掃描「非除權息日、單日變動 > 15%」的 2,480 筆候選中，屬公司行動者都能由
  事件表解釋；0050 在 2025-06-18 的還原價日報酬絕對值 < 5%；不再需要 `stock_split.py`；
  `pytest -m "not slow"` 全綠、`./scripts/run_regression.sh` 通過。

## 進度追蹤表

| 編號 | 步驟名稱 | 產出檔案 | 驗證方式 | 狀態 | 備註／中斷點 |
|------|----------|----------|----------|:----:|--------------|
| S1 | 找出資料源並盤點歷史檔數 | 本文件（調查結論） | 能取得 0050 的 2025-06-18 事件；列出全市場歷年事件清單 | ✅ | **2026-09-05 完成**。結論：**走 ETL**。上市 360 筆／243 檔、上櫃 283 筆／193 檔，兩邊端點皆完整 |
| S2 | 公司行動事件入庫 | `core/pipeline/tw/{crawlers,cleaners,loaders,updaters}/corporate_action_*.py`、`url_manager.py`、`core/config/{schema,paths,settings}.py`、`tasks/update_db.py`、`tests/test_corporate_action.py` | 重跑冪等；0050 的 2025-06-18 與南亞科 2014-09-09 兩筆存在且倍率正確 | ✅ | **2026-09-10 完成**（`df54046`）：**632 筆／435 檔**入庫，兩個驗收案例倍率皆正確（9.99／0.25），重跑仍 637 列。過程抓到三個事前不知道的坑，見完成紀錄 |
| S3 | 還原係數納入公司行動，移除過渡表 | `core/api/tw/stock_dividend_api.py`、`core/api/tw/stock_split.py`（已刪）、`core/backtest/{report/reporter,analysis/analyzer}.py`、`tests/backtest/test_reporting.py` | 0050 分割日的還原價日報酬 < 5%；回歸雙線通過 | ✅ | **2026-09-13 完成**（`f5a0389`＋`960140f`）：0050 分割日 **−74.8% → 1.82%**；**回歸基準不需重產**，理由見完成紀錄 |
| S4 | 假跳空偵測護欄 | `tests/test_corporate_action_guard.py`（新增）、`corporate_action_detector.py` | 以事件表解釋不掉的 > 15% 跳動即紅燈 | ✅ | **2026-09-12 完成**（`0a9d1de`）：6 條測試，**已依規格實測刪掉 0050 那筆即紅**。過程另外抓到 `2020-04-14` 整批是 `2020-12-18` 的資料，見完成紀錄 |

## 步驟詳述

### S1. 找出資料源並盤點歷史檔數 ✅

- **目的**：先確定事件從哪裡取得、涵蓋到哪一年、欄位長什麼樣，並**盤點檔數**——
  檔數決定這件事要不要走 ETL：若只有個位數，寫死表加護欄也許就夠。
- **產出**：本節的調查結論。
- **驗證方式**：能取得 0050 的 2025-06-18 事件（已知答案，用來驗端點對不對）。
- **相依**：無。

> **✅ 完成紀錄（2026-09-05）**
>
> **結論一句話：走 ETL，而且問題比原本以為的大兩個數量級——主角是減資，不是分割。**
>
> #### 1. 規模盤點：`dividend` 漏掉的是 400 檔以上，不是個位數
>
> | 來源 | 端點 | 期間 | 事件數 | 檔數 | 出現在 `dividend` 的比例 |
> |------|------|------|-------:|-----:|-------------------------:|
> | 上市 | `www.twse.com.tw/rwd/zh/reducation/TWTAUU` | 2013~2026 | 360（355 可算倍率） | 243 | **0 / 355** |
> | 上櫃 | `www.tpex.org.tw/www/zh-tw/bulletin/revivt` | 2013~2026 | 283 | 193 | 未逐筆比對，欄位語意與上市相同 |
>
> 上市的 355 筆裡，**倍率偏離 1 超過 5% 的有 305 筆、超過 20% 的有 200 筆**，
> 最極端的是南亞科 2014-09-09：停止買賣前收盤 8.10 → 恢復參考價 80.93，**倍率 9.99×**。
> 減資原因分布：退還股款 203、彌補虧損 157。
>
> #### 2. 資料源齊備，兩邊都給得出精確倍率
>
> 兩個端點都直接提供「停止買賣前收盤價格」與「恢復買賣參考價」，相除即為調整倍率，
> 不需要另外推算。TPEx 還多給「每壹仟股換發新股票」（例如 550 股 ＝ 1 股變 0.55 股），
> 是倍率的獨立佐證。
>
> - TWSE 欄位：`恢復買賣日期／股票代號／名稱／停止買賣前收盤價格／恢復買賣參考價／漲停價格／跌停價格／開盤競價基準／除權參考價／減資原因／詳細資料`
> - TPEx 欄位：同上，第 4 欄名為「最後交易日之收盤價格」，`詳細資料` 是一段 HTML，內含停止買賣日期與換股比例
> - **實測注意**：TPEx 端點會間歇回 **HTTP 520**（2019 與 2024 兩年首輪失敗，重試即成功），
>   crawler 必須有重試層，並且**不可把 520 當成「該年沒有事件」**——那正是
>   [ETL 入庫約定](../docs/pipeline/etl-ingestion.md) §4.2 記錄過的事故樣式。
> - TWSE 的日期參數是西元 `YYYYMMDD`、TPEx 是 `YYYY/MM/DD`，回傳日期則都是民國年。
>
> #### 3. 分割：只有 6 檔，而且 **0050 不在任何結構化端點裡**
>
> 以「非除權息日、單日跌幅使倍率接近 1/N、且停牌 ≥ 3 日」掃全市場 6,372,450 列日行情，
> 全歷史只命中 6 筆：
>
> | 日期 | 代號 | 名稱 | 前收 → 收盤 | 比例 | 停牌 | 成交股數變化（獨立佐證） |
> |------|------|------|-------------|:----:|:----:|-------------------------|
> | 2021-10-18 | 6531 | 愛普* | 750.00 → 377.50 | 1拆2 | 12 日 | 2.86× |
> | 2022-09-19 | 5536 | 聖暉* | 206.00 → 106.00 | 1拆2 | 13 日 | 2.25× |
> | 2024-09-09 | 8932 | 智通* | 169.50 → 82.80 | 1拆2 | 12 日 | **1.01×（不符，待人工判讀）** |
> | 2024-10-14 | 00887 | 永豐中國科技50大 | 22.15 → 10.86 | 1拆2 | 3 日 | 19.12× |
> | 2024-11-11 | 8476 | 台境* | 58.80 → 29.85 | 1拆2 | 12 日 | 2.44× |
> | 2025-06-18 | 0050 | 元大台灣50 | 188.65 → 47.57 | 1拆4 | 8 日 | 6.67× |
>
> **`0050` 這一筆查遍了都不在結構化端點裡**：`TWTB8U`（變更交易）2025-06 全月只有
> 4763 材料-KY 一筆（885.00 → 88.50，面額變更 1 拆 10）；TWSE OpenAPI 143 個端點中
> 沒有任何「受益權單位分割」相關者。ETF 分割走的是公告系統而非結構化查詢。
> **因此 S2 一定要保留「由行情偵測 ＋ 人工確認」這條補漏路徑，不能只靠端點。**
>
> #### 4. 反向分割也在，且集中在槓桿反向 ETF
>
> 同一套方法反向掃（價格上跳、停牌 ≥ 3 日、倍率接近整數）命中 41 筆／39 檔，
> 其中 00632R 元大台灣50反1（2024-12-11，3.28 → 23.12，約 7×）、00673R、00674R、00706L
> 是槓桿反向 ETF 的反向分割，其餘多為減資（例如 2321 東訊 2020-10-19，3.79 → 11.40，約 3×，
> 該筆 `dividend` 零列）。**調整方向有正有負**，這也是「寫死一張分割表」行不通的原因之一。
>
> #### 5. 偵測方法本身值得留下來當護欄（→ 新增 S4）
>
> 台股單日漲跌幅上限 ±10%，所以**非除權息日出現超過 15% 的單日變動，必然是公司行動或資料錯誤**，
> 沒有第三種可能。全期間共 2,480 筆候選、涵蓋 1,113 檔。這個判準不需要任何外部資料，
> 且事件表補齊後應該要能解釋掉其中屬於公司行動的每一筆——解釋不掉的就是新事件或資料錯誤。
> 故新增 S4：把它寫成測試，事件表從此不會靜默過期。
>
> #### 6. 對 S2／S3 的影響（本節推翻了原規劃）
>
> - 原 S2 寫「與 `dividend` 同表或另立 `stock_split` 表，由 S1 的資料形狀決定」——
>   **定案為另立 `corporate_action` 表**。不併入 `dividend` 的理由：`dividend.還原係數` 的語意是
>   「除權息參考價 ÷ 除權息前收盤價」，而減資的倍率是**大於 1** 的（價格上調），
>   塞進同一欄會讓所有既有讀取端的假設反向。
> - 表名不叫 `stock_split`：分割只有 6 筆，減資有 600 多筆，用分割命名會讓下一個人找不到。
> - 原驗收標準只看 0050 一檔，已改為「2,480 筆候選都要能被解釋」。

### S2. 公司行動事件入庫 ✅

- **目的**：把減資／面額變更／分割變成可查詢的資料，而不是散在程式碼裡的常數。
- **做法**：
  1. `url_manager.py` 新增 `TWSE_CAPITAL_REDUCTION_URL`（`reducation/TWTAUU`）與
     `TPEX_CAPITAL_REDUCTION_URL`（`bulletin/revivt`），比照既有除權息端點的寫法。
  2. 另立 `corporate_action` 表，主鍵 `(date, stock_id)`，欄位至少：
     `恢復買賣日期`（＝生效日）、`stock_id`、`停止買賣前收盤價`、`恢復買賣參考價`、
     `調整倍率`（＝後者 ÷ 前者）、`事件類型`（減資／面額變更／分割／反向分割）、
     `原因`（退還股款／彌補虧損／…）、`資料來源`（twse／tpex／detected）。
  3. **保留 `detected` 這個來源**：0050 那類 ETF 分割不在任何端點，只能由 S1 的偵測方法
     產出候選再人工確認。這不是妥協，是 S1 查證後的事實。
  4. TPEx 的 520 要有重試層，且**失敗不可當成「該年沒有事件」**。
- **驗證方式**：重跑冪等；0050 的 2025-06-18（倍率 0.25）與南亞科 2014-09-09（倍率 9.99）
  兩筆存在且倍率正確。
- **相依**：S1。

> **✅ 完成紀錄（2026-09-10，commit `df54046`）**
>
> `--target corporate_action` 上線，全期間 **632 筆事件、435 檔**入庫
> （twse 350／tpex 282／detected 5），倍率偏離 1 超過 20% 的有 **403 筆**。
>
> | 驗收案例 | 結果 |
> |---|---|
> | 南亞科 2014-09-09 | 8.10 → 80.93，倍率 **9.99** ✅ |
> | 0050 2025-06-18 | 188.65 → 47.16，倍率 **0.25** ✅（`detected` 來源）|
> | 重跑冪等 | 連跑兩次皆 637 列 ✅ |
> | 年度統計 | 14 requested / 14 ok / 0 unreachable ✅ |
>
> #### 三個事前不知道的坑（都已固化為測試）
>
> 1. ⚠️ **不可用倍率方向判斷分割**。第一版寫「倍率 < 1 即分割」，結果 632 筆裡
>    有 **7 筆**原因欄明寫「退還股款」「現金減資」卻被判成分割
>    （協禧 0.922、無敵 0.934、中環 0.950…）。成因是**退還的現金比股數合併的
>    效果大時，參考價會低於前收盤價**。這兩個端點本身就是減資端點，
>    分割不會出現在裡面；改為端點來源一律看 `原因` 欄。
> 2. ⚠️ **TPEX 交叉驗證要含「每股退還股款」**：站方公式是
>    `(前收 − 退還股款) × 1000 ÷ 換發股數`。S1 只記了換股比例那一半，
>    於是「退還股款」型整批對不上，一度被我誤判成資料有問題
>    （3290 東浦推得 40.93 vs 站方 36.64）。補上之後 2019／2024／2025 三年
>    共 46 筆**零不符**。
> 3. ⚠️ **站方會給重複列**：公司改名期間同一筆事件出現兩次（南亞科／南科），
>    2013~2026 上市共 5 筆這種多餘列，主鍵 `(date, stock_id)` 會撞。
>    cleaner 先去重，價格不同才警告（那才是真衝突，不是改名）。
>
> #### 對 S4 的重要修正：S1 的判準對槓桿反向 ETF 不成立
>
> ⚠️ S1 寫「台股 ±10% 漲跌幅，非除權息日 > 15% 必為公司行動或資料錯誤，
> 沒有第三種可能」——**槓桿與反向 ETF 是第三種可能，它們的漲跌幅上限是 ±20%**。
> 實測 2025-04-07 關稅重挫那天，00631L／00640L／00647L／00650L／00663L
> 一整批同時跌到 −20%（00631L 由 200.30 到 160.25，倍率 0.80），
> **那天它們都沒有任何公司行動**。
>
> 新增的 `corporate_action_detector.py` 因此**標記而不濾掉**（`疑似槓桿反向ETF`
> 欄）：直接濾會連真正的反向分割一起藏起來，而 S1 正好查到 00632R 在
> 2024-12-11 做過反向分割。判讀時先看這一欄，再看**停牌日數**——公司行動幾乎
> 都伴隨停止買賣數日（0050 停 8 日），跌停不會。**S4 寫護欄時要沿用這個修正**，
> 否則每逢重挫日就會整批紅燈。
>
> #### 偵測事件的兩個決定
>
> - **8932 智通刻意不寫入**：S1 標為「待人工判讀」，本次複查成交量佐證只有
>   1.60×，不是分割該有的量級。寧可少一筆，也不要把不確定的事件寫進調整表——
>   錯的調整會直接汙染價格序列。S1 的 6 筆分割因此只入庫 5 筆。
> - **倍率用理論值 1/N，不用當日實際漲跌**：0050 實際是 188.65 → 47.57
>   （0.2522），但入庫的是 0.25。參考價是**算出來的**，當天的真實走勢在那之後
>   才發生；把 0.2522 當調整倍率會連那 0.9% 的真實漲跌一起抹掉。
>
> #### 待辦（相依另一條線）
>
> `docs/pipeline/etl-ingestion.md` §二的 updater 對照表要登記本表。該檔仍有
> 另一條線的未提交變更，但**這不再構成封鎖**——
> [健檢第三輪收斂](../docs/dev/health-check-round3-2026-09.md) S4 於 2026-09-13
> 證明了作法：只暫存「HEAD ＋ 本次修正」這個版本（`git hash-object -w` ＋
> `git update-index --cacheinfo`），對方的工作目錄內容原封不動。
> 「同一個檔案有別人的未提交變更」不等於「不能提交我的修改」，只是不能用 `git add`。

### S3. 還原係數納入公司行動，移除過渡表 ✅

- **目的**：讓 `get_adjusted_close_series()` 一次把除權息與公司行動都處理掉，
  呼叫端不需要各自記得補。
- **做法**：`StockDividendAPI.get_cumulative_factor()` 的累乘序列納入 `corporate_action`；
  確認後刪除 `core/api/tw/stock_split.py`，並把 `StockBacktestReporter._get_adjusted_price()`
  與 `StockBacktestAnalyzer.compute_benchmark_daily_returns()` 的呼叫改掉。
- **注意**：`stock_split.py` 是 2026-09-04 為了讓 Information Ratio 算得出來而建立的**過渡方案**，
  它只認得 0050 這一檔。刪除它時務必先確認 S2 的資料涵蓋範圍包含那一筆。
- **驗證方式**：0050 在 2025-06-18 的還原價日報酬絕對值 < 5%（目前走 `stock_split.py` 是 +0.86%，
  資料源接上後應相同）；南亞科 2014-09-09 的還原價日報酬回到常態；
  `./scripts/run_regression.sh` 通過。**回歸雙線預期會變動**——還原價是策略訊號的輸入，
  基準需重產，這點與前三份工作的「零數值變動」不同，動手前先確認。
- **相依**：S2。

> **✅ 完成紀錄（2026-09-13，commit `f5a0389` ＋ `960140f`）**
>
> `corporate_action.調整倍率` 疊進原本只吃 `dividend.還原係數` 的累乘係數。
> 兩張表的語意本來就一致（都是「參考價 ÷ 前收盤價」），所以同一套累乘直接吃得下；
> 累的是倒數，方向由數值自己決定——減資倍率 9.99 的倒數 0.1 會把事件後的價格
> 往下調回來，分割 0.25 的倒數 4 會往上調。
>
> | 驗收條件 | 結果 |
> |---|---|
> | 0050 分割日還原價日報酬 | **−74.8% → 1.82%**（門檻 < 5%）✅ |
> | `core/api/tw/stock_split.py` | 已刪除 ✅ |
> | 回歸雙線 | 通過、無 skip ✅ |
>
> #### ⚠️ 拆過渡表時必須同時拆掉兩處重複調整
>
> reporter 與 analyzer 原本各自再套一次 `apply_split_adjustment()`（因為當時的
> 還原係數不含分割）。分割進了累乘係數之後，**再套一次就是重複調整**——
> 實測 0050 的分割日會由 1.82% 變成 **303%**。這是刪表時最容易漏的一步。
>
> #### 回歸基準**不需要**重產（與原規劃不同）
>
> 本文件與 `index.md` 原本都寫「S3 完成後回歸雙線基準需重產」。實查後**不需要**：
>
> - 2024 回歸區間內有 27 筆公司行動，baseline 交易到其中 **11 檔**；
> - 那 11 檔 2024 全年有 **57.3% 的還原價確實改變**（2363 於 2024-01-02
>   由 70.77 變成 64.64）；
> - **但 `MomentumStrategy1` 的訊號是「當日收盤 ÷ 前一交易日收盤」的比值**，
>   後復權係數對相鄰兩日是同一個常數會約掉，只有事件正好落在那兩日之間才會變，
>   而那 11 檔的事件日都不在 baseline 的持倉區間內。
>
> 也就是說：還原價本身大幅改變了，但這支策略的訊號不受影響。
> **下一支用到跨期價格水準（而非相鄰比值）的策略就會受影響**，屆時基準要重產。

### S4. 假跳空偵測護欄 ✅

- **目的**：事件表一定會過期——每年都有新的減資與分割。沒有護欄的話，
  下一次漏掉某檔的方式與這次一模一樣：靜默、無錯誤、數字看起來合理。
- **做法**：把 S1 的判準寫成測試——掃 `price`，找出「非除權息日、單日變動 > 15%」的列，
  逐筆嘗試以 `corporate_action` 解釋；解釋不掉且未列入已知例外清單者即失敗。
  需標 `@pytest.mark.slow`（要讀 `tw_stock.db`）。
- **驗證方式**：故意從事件表刪掉 0050 那筆，測試要紅。
- **相依**：S2。

> **✅ 完成紀錄（2026-09-12，commit `0a9d1de`）**
>
> `tests/test_corporate_action_guard.py` 6 條、標 `slow`。**核心測試是「已知事件
> 必須被解釋」**（0050 2025-06-18、南亞科 2014-09-09、大同 2025-06-23），
> 已依本步驟的驗證方式實測：**把 0050 那筆從事件表刪掉，該條立刻紅**，還原後綠。
>
> #### S1 的判準要再修三處，否則護欄會被誤報淹掉
>
> 1. ⚠️ **`2020-04-14` 整批是錯的資料**——這是本步驟最大的意外發現。
>    台積電當天顯示開 508／高 512／低 507／收 510、成交股數 40,625,502，
>    與 **`2020-12-18` 位元級相同**，而前後兩日分別是 278.5 與 287.5。
>    這是[爬蟲缺口回補](爬蟲缺口回補與非交易日批次清理.md) S1 記載過的樣式
>    （清洗端蓋的是請求參數的日期而非來源頁面內的日期）。該日 715 檔被判跳空、
>    次日再 630 檔（被前一日的壞收盤拖累），**兩天佔了全部候選的 1,412 筆**。
>    已列入 `KNOWN_BAD_PRICE_DATES` 排除，並附一條測試：資料修好那天它會變紅，
>    提醒把日期刪掉，不讓臨時措施長住。**這筆資料本身要修，屬爬蟲那條線。**
> 2. ⚠️ **權證等 5 碼以上代號沒有漲跌幅限制**（248 筆），與槓桿反向 ETF
>    （±20%，153 筆）同屬「判準本來就不適用」，護欄分類統計而非混在總數。
> 3. ⚠️ **比對不可用 `(date, stock_id)` 完全相等**：端點給的是官方
>    `恢復買賣日期`，而實際有成交的第一天可能因假日更晚——1529 的事件日是
>    2018-12-22（週六）、跳空在 12-24；5455 差了 8 天。偵測器改為
>    「事件日落在**停牌區間**內即視為解釋」，解釋不掉的由 361 降為 337 筆。
>
> #### 為什麼用基準數而不是「必須為 0」
>
> 排除上述三類後仍有 **337 筆一般個股／ETF** 解釋不掉。抽樣判讀後分兩種：
> 多數是**不在減資端點裡的公司行動**（合併換股、私募、股權轉換——那是 S2 的
> 涵蓋範圍限制），少數是單日離群值（2408 在 2015-12-14 由 38.0 跳到 52.9、
> 隔日回到 42.5，那是資料錯誤不是事件）。
>
> 逐筆列成例外清單會變成一份沒人維護的 337 行名單，且新事件與舊殘留混在一起
> 反而看不出差別。改記**基準數**：只准降不准升，升即代表事件表過期。
> 一般個股／ETF 另外單獨設一條基準，因為槓桿 ETF 與權證的筆數會隨市場波動
> 起伏（重挫日整批跌停就多幾十筆），混在總數裡會把該注意的那一類稀釋掉。
>
> #### ⚠️ CI 跑不到這條護欄
>
> CI 只跑 `-m "not slow"`，且沒有 `tw_stock.db`（`*.db` 未進版控）。本護欄與
> LONG 回歸線同屬**本機護欄**，建議跑完 `--target corporate_action` 後手動執行：
> `pytest tests/test_corporate_action_guard.py -m slow`。

## 附：目前的影響範圍

| 呼叫端 | 現況 |
|------|------|
| `StockBacktestReporter`（淨值圖／MDD 圖的 benchmark 曲線） | 走 `stock_split.py`，**只有 0050 正確** |
| `StockBacktestAnalyzer.compute_information_ratio()` | 走 `stock_split.py`，**只有 0050 正確** |
| **策略訊號**（任何用 `get_adjusted_close_series()`／`get_adjusted_close_map()` 的地方） | **未補**；遇到 400 檔以上曾減資或分割的個股會拿到假跳空 |
