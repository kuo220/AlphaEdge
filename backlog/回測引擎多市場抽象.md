# 回測引擎多市場抽象（單一引擎 ＋ 可插拔 model）

## Abstract

- **背景／問題**：`core/backtest/backtester.py`（838 行）是純台股實作——`StockAccount`、`StockPositionManager`、`StockCostModel`、五支 `Stock*API`、`StockQuoteAdapter` 全部在 `__init__` 內直接 new 出來，`stock_id` 貫穿五個 model 與所有 log 字串，漲跌停與檔位規則寫死在 `validate_fill_price()`。既有規劃（[台期貨ETL與回測架構規劃.md](台期貨ETL與回測架構規劃.md) Phase1-6）是「平行複製一支 `FuturesBacktester`」，但那會複製掉其中約 4 成真正市場無關的骨架，兩邊永久漂移。
- **目標**：把引擎收斂為**單一 `Backtester`**，市場差異全部下沉為可插拔的 model（`InstrumentSpec` / `FillModel` / `CostModel` / `SettlementModel` / `DataFeed`），對齊 Backtrader `Cerebro`、Zipline `run_algorithm`、QuantConnect Lean `Engine`、Nautilus `BacktestEngine` 的一致做法：**引擎唯一，行為注入**。同時完成 `core/` 的解耦收尾（領域模組移出 `utils/`、`BasePositionManager` 抽取、死碼清除）。
- **範圍界線**：**行為必須零改變**——只搬移程式碼與抽取介面，不改任何費用公式、不改記帳口徑、不改報表輸出欄位名。**不做**：期貨／美股的 model 實作與 ETL（屬 [台期貨ETL與回測架構規劃.md](台期貨ETL與回測架構規劃.md)、[美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md)）、事件驅動 order queue 與 T+1 延遲成交、per-instrument 粒度的 model 掛載（見 §四）、`--mode live` 實盤路徑。
- **驗收標準**：每一步驟完成後，`test_long_regression_snapshot`（915 筆 × 13 欄）與新增的 SHORT 端到端快照皆逐筆相同，且 `tests/` 全數通過；`Backtester` 內不再 import 任何 `Stock*` 具體類別；新增一個市場所需的既有檔案改動為 **0 行**。

> **完成後的判準**：`grep -c "Stock" core/backtest/backtester.py` 應為 0。

---

## 進度追蹤表

> 實作分支建議：`refactor/multi-market-engine`。
>
> **每一格的驗證方式一律包含「回歸雙線」**：`test_long_regression_snapshot`（需 `stock.db`，約 54 秒）＋ `test_short_regression_snapshot`（純記憶體，秒級）。任一破線即停止並記錄中斷點，不得往下推進。

| 編號 | 階段 | 步驟名稱 | 產出檔案 | 驗證方式 | 狀態 | 備註／中斷點 |
|------|------|----------|----------|----------|:----:|--------------|
| Phase0-1 | Phase 0 | SHORT 端到端回歸快照 | `tests/backtest/make_short_baseline.py`、`snapshots/short_regression_baseline.csv`、`test_short_regression.py` | 快照可重現產生；重跑逐筆相同 | ⬜ | **動任何程式碼前的第一步**；不連 DB，用既有 `ScriptedStrategy` |
| Phase1-1 | Phase 1 | `core/models/base/` 抽象與 `symbol` 欄位 | `core/models/base/*.py`、`core/models/stock/*.py` | 回歸雙線 ＋ 既有 67 項測試 | ⬜ | `Stock*` 保留 `stock_id` property 別名，策略層零改動 |
| Phase1-2 | Phase 1 | reporter／analyzer 內部改用 `symbol` | `core/backtest/report/reporter.py`、`analysis/analyzer.py` | 回歸雙線（CSV 欄位名不變是關鍵） | ⬜ | **輸出欄位名維持 `Stock ID`**，否則 baseline 失效、回歸線中斷 |
| Phase1-3 | Phase 1 | 抽出 `BaseStrategy` | `core/strategies/base.py`、`core/strategies/stock/base.py` | 既有 6 支策略未修改即通過回歸 | ⬜ | 市場無關欄位上移；`market` 欄位自此成為 factory 分派鍵 |
| Phase2-1 | Phase 2 | `InstrumentSpec` 介面 ＋ `TwStockSpec` | `core/backtest/models/instrument_spec.py` | `test_instrument_spec`；回歸雙線 | ⬜ | 吸收 `convert_lot_to_share`、`round_to_tick`、漲跌停比率 |
| Phase2-2 | Phase 2 | `BaseFillModel` ＋ `TwStockFillModel` | `core/backtest/models/fill_model.py` | 既有 3 項成交價測試改為打 model；回歸雙線 | ⬜ | 搬 `validate_fill_price` / `get_price_range` / `update_intraday_range` |
| Phase2-3 | Phase 2 | `BaseSettlementModel` ＋ `TwStockSettlementModel` | `core/backtest/models/settlement_model.py` | 既有 7 項每日檢查測試；回歸雙線 | ⬜ | **本階段最大一塊**：搬當沖強制回補、借券費計提、維持率追繳 |
| Phase2-4 | Phase 2 | `BaseCostModel` 介面化 | `core/backtest/models/cost_model.py` | `test_cost_model` 24 項；回歸雙線 | ⬜ | `StockCostModel` 只加繼承，公式一行不改 |
| Phase2-5 | Phase 2 | `BaseDataFeed` ＋ `TwStockDataFeed` | `core/backtest/datafeed/*.py` | 回歸雙線 | ⬜ | 吸收 `load_datasets()`、`QuoteAdapter`、`check_stock_market_open` |
| Phase2-6 | Phase 2 | `Backtester` 改為注入式，引擎瘦身 | `core/backtest/backtester.py` | `grep -c "Stock" backtester.py == 0`；回歸雙線 | ⬜ | 相依 Phase2-1~2-5；預計 838 → 約 400 行 |
| Phase3-1 | Phase 3 | `build_backtester()` factory | `core/backtest/factory.py` | 台股策略經 factory 產出的引擎跑出相同結果 | ⬜ | 全專案唯一的 `if market ==` |
| Phase3-2 | Phase 3 | `StrategyLoader` 多市場 ＋ `run.py` 接 factory | `core/strategies/strategy_loader.py`、`run.py` | `python run.py --strategy MomentumStrategy1` 結果不變 | ⬜ | CLI 介面不變，不新增 `--market` |
| Phase4-1 | Phase 4 | 領域模組移出 `core/utils/` | `core/backtest/models/`、`core/utils/` | 回歸雙線 ＋ 全專案 import 掃描 | ⬜ | `cost_model.py`、`instrument.py`、`market_calendar.py` |
| Phase4-2 | Phase 4 | `BasePositionManager` 抽取 ＋ `settle_daily()` 掛點 | `core/managers/base/position_manager.py`、`core/managers/stock/position_manager.py` | 回歸雙線 | ⬜ | 股票 `settle_daily()` 為 no-op；期貨每日結算的預留掛點 |
| Phase4-3 | Phase 4 | 刪除死碼 | `core/archive/`、`core/api/old api/` | 全專案 import 掃描無引用 | ⬜ | 兩者皆零引用；`old api` 目錄名含空格 |
| Phase4-4 | Phase 4 | 同步更新受影響的 backlog 文件 | `backlog/*.md`、`backlog/index.md` | 人工檢視：檔案路徑與掛點名稱一致 | ⬜ | 清單見 §五 |

---

## 一、為什麼不是「平行複製 FuturesBacktester」

把現有 838 行逐段分類的結果：

| 性質 | 內容 | 佔比 |
|------|------|------|
| **市場無關**（複製會浪費、且會漂移） | `run()` 日期迴圈、`execute_bar()` 執行順序、`validate_orders()` 方向白名單、`resolve_open/close_action()`、`execute_open/close_signal()`、`snapshot_daily_equity()` 骨架、`event_counts`、`generate_backtest_report()` | 約 4 成 |
| **市場規則**（介面共用、實作不同） | `validate_fill_price()`、`get_price_range()`、`check_stock_market_open()` | 約 2 成 |
| **台股信用交易專屬** | `build_cost_config()`、`enrich_orders()`、`enforce_day_trade_cover()`、`convert_to_margin_position()`、`accrue_holding_cost()`、`check_margin_call()` | 約 4 成 |

第三類看似「非分家不可」，但逐一檢查後**全部都能對應到 model 掛點**，沒有一項需要靠繼承分支：

| 現有方法（`backtester.py` 行號） | 下沉到 | 業界對應（Lean） |
|------|--------|------|
| `validate_fill_price` / `get_price_range`（257、307） | `FillModel` | `FillModel` |
| `build_cost_config` / `enrich_orders`（133、243） | `CostModel` ＋ `InstrumentSpec` | `FeeModel` ＋ `SymbolProperties` |
| `check_margin_call`（688） | `SettlementModel` | `BuyingPowerModel` / `MarginCallModel` |
| `accrue_holding_cost`（662） | `SettlementModel` | `MarginInterestRateModel` |
| `enforce_day_trade_cover` / `convert_to_margin_position`（522、595） | `SettlementModel` | `SettlementModel` |
| `snapshot_daily_equity` 的 `convert_lot_to_share`（794） | `InstrumentSpec.multiplier` | `SymbolProperties.ContractMultiplier` |

**關鍵洞察**：台股的「當沖日終強制回補」與期貨的「每日結算」，在架構上是**同一個掛點的兩種實作**——「一根 bar 收盤後，市場規則強制對部位做的事」。看出這點之後，切兩個引擎就沒有理由了。

---

## 二、目標架構

```
Backtester                      ← 唯一引擎，市場無關，無子類
  ├─ InstrumentSpec             ← 乘數、tick size、漲跌停規則、報價單位換算
  ├─ FillModel                  ← 成交價可信度（前視偏誤與不可能成交的擋板）
  ├─ CostModel                  ← 手續費／稅／借券費（期貨為期交稅）
  ├─ SettlementModel            ← 每根 bar 收盤後的強制動作
  └─ DataFeed                   ← 資料載入、報價轉換、交易日判定
```

引擎的建構子從「自己 new 具體類別」改為「收 model」：

```python
class Backtester:
    def __init__(
        self,
        strategy: BaseStrategy,
        instrument: InstrumentSpec,
        fill_model: BaseFillModel,
        cost_model: BaseCostModel,
        settlement: BaseSettlementModel,
        data_feed: BaseDataFeed,
    ) -> None:
```

`execute_bar()` 收斂為市場無關的流程：

```python
def execute_bar(self, date: datetime.date, quotes: List[BaseQuote]) -> None:
    if self.get_execution_order() == BarExecutionOrder.OPEN_THEN_CLOSE:
        self.execute_open_signal(quotes)
        self.execute_close_signal(quotes)
    else:
        self.execute_close_signal(quotes)
        self.execute_open_signal(quotes)

    # 台股：當沖強制回補 ＋ 借券費計提 ＋ 維持率追繳
    # 期貨：每日結算 ＋ 保證金追繳 ＋ 到期換月
    self.settlement.on_bar_close(date, quotes, self.account, self.event_counts)

    self.snapshot_daily_equity(date, quotes)
```

**方向（LONG／SHORT）與市場（股票／期貨）是兩條獨立的軸**，本次只動「市場」那條。`validate_orders()`、`resolve_open_action()`、`resolve_close_action()` 本來就與市場無關（期貨的多空語意與股票相同），一律留在引擎內不動。[放空回測框架建置.md](放空回測框架建置.md) §1 原則 2「方向來自訂單，策略只做白名單」是本次重構的**基礎**，不是被取代的對象。

---

## 三、為什麼先做 3+2 個 model 而不是 Lean 的五件套

Lean 把 `FeeModel` / `FillModel` / `BuyingPowerModel` / `MarginCallModel` / `SettlementModel` / `SlippageModel` 拆得很細，是因為它要支撐十幾種資產類別與多家券商。本專案只有兩個市場，**先合成 `SettlementModel` 一個**（吸收保證金與追繳），等期貨的原始／維持保證金真的塞不進去再拆第四個。少即是多。

`DataFeed` 與 `InstrumentSpec` 不算「行為 model」，但必須一起抽，否則 `Backtester.load_datasets()` 仍會 import 五支 `Stock*API`，`grep -c "Stock" == 0` 的驗收標準過不了。

---

## 四、已知簡化（明確不做，但要記錄）

| 項目 | 影響 | 為何不做 |
|------|------|----------|
| **per-instrument 粒度的 model 掛載** | 無法在同一次回測同時持有台股與台指期（跨市場組合／避險） | 業界（Lean 掛在 `Security`、Nautilus 掛在 `Instrument`）確實是這個粒度，本次採 per-run 簡化。[台期貨ETL與回測架構規劃.md](台期貨ETL與回測架構規劃.md) 的範圍界線本來就寫明「不含跨市場組合回測」。升級路徑乾淨：把 model 從 `Backtester` 移到 `InstrumentSpec` 物件上，引擎迴圈不動 |
| 事件驅動 order queue（T+1 延遲成交、限價單未成交、部分成交） | 追繳仍只能以觸發當日收盤價回補 | 本質是引擎典範轉移，非本次範圍。既有紀錄見 [放空回測框架建置.md](放空回測框架建置.md) §7.2 與 [回測引擎當沖執行順序重構.md](回測引擎當沖執行順序重構.md) S4 |
| 報表輸出欄位改名（`Stock ID` → `Symbol`） | 期貨報表的欄位名仍是股票語意 | 改名會讓 915 筆 baseline 失效、回歸線中斷。**等期貨真的要出報表時再處理**，屆時 baseline 本來就要重產 |
| `--mode live` 實盤路徑 | 實盤仍是空實作 | `run.py` 的 live 分支目前是 `pass`，不在本次範圍；但 factory 已預留讓實盤共用同一組 model |

---

## 五、受本次重構影響的既有 backlog

| 文件 | 受影響處 | 處理方式 |
|------|----------|----------|
| [台期貨ETL與回測架構規劃.md](台期貨ETL與回測架構規劃.md) | Phase1-6「平行新增 `FuturesBacktester`」整條作廢 | 改為「實作期貨 model 組」，相依本文件 |
| [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | Phase2-2 `engine/fee_models.py`、Phase3-3「多市場共用介面」 | 路徑對齊本文件的 `core/backtest/models/`；Phase3-3 部分由本文件提前完成 |
| [LONG成本模型口徑收斂.md](LONG成本模型口徑收斂.md) | S3、S4 的產出檔案路徑（`cost_model.py` 移出 `utils/`、manager 目錄扁平化） | 更新路徑；工作內容不變 |
| [回測滑價與執行係數.md](回測滑價與執行係數.md) | S3「`Backtester` 掛點」 | 掛點改為 `FillModel`，語意更貼切 |
| [回測權益曲線改用逐日權益.md](回測權益曲線改用逐日權益.md) | S1、S5 動 `generate_backtest_report()` | 該方法留在引擎內，僅確認行號漂移 |
| [回測引擎當沖執行順序重構.md](回測引擎當沖執行順序重構.md) | S1、S3 動 `execute_bar()` | 該方法留在引擎內，工作內容不變 |
| [放空框架Phase5延伸.md](放空框架Phase5延伸.md) | S2 券源檢核的落點 | 由 `Backtester` 改為開倉前的 constraint 檢查，可放 `FillModel` 或 `CostModel` |
| [放空回測框架建置.md](放空回測框架建置.md) | §十一「與其他 backlog 的關係」 | 補一條指向本文件 |

---

## Phase0-1. SHORT 端到端回歸快照 ⬜

- **目的**：建立本次重構的第二條回歸線。LONG 有 915 筆逐筆 baseline，**SHORT 目前只有 61 項單元／整合測試，缺少「整條路徑的完整帳」**。Phase 2 要把放空記帳從 `Backtester` 搬進 `SettlementModel`，若某個攤提比例走鐘，現有測試不保證抓得到。
- **做法**：
  - 用既有的 `ScriptedStrategy`（`tests/backtest/conftest.py:76`，以「日期 → 訂單清單」腳本驅動，不連 DB）建構一組確定性情境，至少涵蓋：

    | 情境 | 驗的是什麼 |
    |------|------------|
    | 當沖放空同日回補 | `DAY_TRADE` 稅率減半、`OPEN_THEN_CLOSE` |
    | 融券留倉 10 天後回補 | 保證金佔用、融券利息於平倉一次算 |
    | 開兩筆、只回補一部分 | FIFO 方向篩選、等比例攤提 |
    | 持有中股價上漲觸發維持率 | 斷頭強制回補 |
    | 當沖日終鎖漲停 | 轉融券留倉、事件計數 |
    | `SBL` 借券費逐日計提 | 只有 SBL 累加，`MARGIN` 恆為 0（不重複計費） |

  - 把 `StockTradeRecord` **全欄位**存成 `tests/backtest/snapshots/short_regression_baseline.csv`，欄位至少包含：`entry/exit date`、`entry/exit price`、`volume`、`borrow_fee`、`interest`、`margin`、`holding_days`、`short_method`、`realized_pnl`、`roi`、`roi_on_capital`。
  - 另存 `account.balance`、`account.margin_used` 終值與 `event_counts` 六個計數（可放同一 CSV 的附加區塊或第二個檔案）。
  - 比照 `make_baseline.py` / `test_long_regression.py` 的既有寫法，產生器與測試分開兩支檔案。
- **產出**：`tests/backtest/make_short_baseline.py`、`tests/backtest/snapshots/short_regression_baseline.csv`、`tests/backtest/test_short_regression.py`。
- **驗證方式**：快照產生後立即重跑測試須通過；刻意把 `close_short_position()` 的攤提比例改錯一個字元，測試必須 fail（護欄有效性自檢）。
- **相依**：無。**必須是本文件的第一個動作**，在動任何生產程式碼之前完成並單獨 commit。

---

## Phase1-1. `core/models/base/` 抽象與 `symbol` 欄位 ⬜

- **目的**：`BaseBacktester` 的骨架不可能寫 `order.stock_id`。目前 `stock_id` 散在 `StockOrder` / `StockPosition` / `StockTradeRecord` / `StockQuote` / `StockAccount` 五個 model，加上所有 warning 字串。
- **做法**：
  - 新增 `core/models/base/`：`BaseQuote`、`BaseOrder`、`BasePosition`、`BaseTradeRecord`、`BaseAccount`，識別欄位一律命名 `symbol`。
  - `Stock*` 改為繼承對應的 `Base*`，並保留 `stock_id` 為 **property 別名**（`@property def stock_id(self) -> str: return self.symbol`），既有 6 支策略與 `core/strategies/README.md` 的範例一行都不用改。
  - `StockAccount.check_has_position(stock_id=...)` 等既有簽章維持關鍵字相容。
- **產出**：`core/models/base/*.py`（5 檔）、`core/models/stock/*.py`（5 檔修改）、`core/models/__init__.py`。
- **驗證方式**：回歸雙線 ＋ 既有 67 項測試；`python run.py --strategy MomentumStrategy1` 可跑完。
- **相依**：Phase0-1。

## Phase1-2. reporter／analyzer 內部改用 `symbol` ⬜

- **目的**：報表層是 `stock_id` 的第二大散布地（`reporter.py` 以 `pd.DataFrame` 組表，欄位名寫死）。
- **做法**：內部取值改讀 `record.symbol`；**輸出的 CSV 欄位名一律維持現狀**（`Stock ID`、`Buy Date`、`Exit Date`…）。
- **產出**：`core/backtest/report/reporter.py`、`core/backtest/analysis/analyzer.py`。
- **驗證方式**：回歸雙線——欄位名不變是這一步能安全驗證的唯一理由，一旦改名 baseline 立刻失效、後續所有步驟失去保護。
- **相依**：Phase1-1。
- **註**：欄位改名列入 §四「已知簡化」，等期貨要出報表時連同 baseline 重產一起做。

## Phase1-3. 抽出 `BaseStrategy` ⬜

- **目的**：`StrategyLoader` 與 factory 都需要一個市場無關的策略基底。
- **做法**：
  - 新增 `core/strategies/base.py`，上移市場無關欄位：`strategy_name`、`market`、`init_capital`、`max_holdings`、`scale`、`start_date`／`end_date`、`allowed_directions`、`position_type`、`bar_execution_order`、`is_backtest`，以及三個訊號抽象方法。
  - 台股專屬欄位（`short_method`、`cost_config`、`short_constraint`、`max_holding_days`、`day_trade_uncovered_policy`、`margin_call_policy`、五個 `Stock*API` 資料集）留在 `BaseStockStrategy`。
  - `self.market`（`core/strategies/stock/base.py:33`，目前全專案零引用的死欄位）自此成為 factory 的分派鍵。
- **產出**：`core/strategies/base.py`、`core/strategies/stock/base.py`、`core/strategies/__init__.py`。
- **驗證方式**：既有 6 支策略未修改即通過回歸雙線。
- **相依**：Phase1-1。

---

## Phase2-1. `InstrumentSpec` ＋ `TwStockSpec` ⬜

- **目的**：商品規格（乘數、跳動點、漲跌停）是市場差異最集中的地方，且被 `snapshot_daily_equity` 與 `validate_fill_price` 兩處使用。
- **做法**：

  ```python
  class InstrumentSpec(ABC):
      """商品規格：報價單位換算、跳動點、漲跌停規則"""

      @abstractmethod
      def to_units(self, volume: int) -> int:
          """下單數量 → 計價單位（台股：張 → 股 ×1000；期貨：口 → 契約乘數）"""

      @abstractmethod
      def round_to_tick(self, price: float, direction: str) -> float: ...

      @abstractmethod
      def get_price_limits(self, prev_close: float) -> Tuple[Optional[float], Optional[float]]:
          """漲跌停區間；無此制度時回傳 (None, None)"""
  ```

  `TwStockSpec` 的實作直接沿用 `StockUtils.convert_lot_to_share`、`StockUtils.round_to_tick` 與 `PRICE_LIMIT_RATIO`，公式不改。
- **產出**：`core/backtest/models/instrument_spec.py`。
- **驗證方式**：`test_instrument_spec`（六段檔位邊界沿用既有 15 組測資）＋ 回歸雙線。
- **相依**：Phase1-3。

## Phase2-2. `BaseFillModel` ＋ `TwStockFillModel` ⬜

- **目的**：成交價可信度是市場規則，不是引擎邏輯。
- **做法**：搬 `validate_fill_price()`、`get_price_range()`、`update_intraday_range()`、`update_prev_close()`（`backtester.py:257-337`）。model 自行持有 `intraday_range` 與 `prev_close` 兩個狀態字典。介面：

  ```python
  def validate(self, order: BaseOrder, quote: BaseQuote) -> bool: ...
  def on_bar_open(self, quotes: List[BaseQuote]) -> None: ...   # 更新盤中累計高低
  def on_bar_close(self, quotes: List[BaseQuote]) -> None: ...  # 記錄前收
  ```
- **產出**：`core/backtest/models/fill_model.py`。
- **驗證方式**：`test_fill_price_out_of_range_rejected`、`test_fill_price_limit_rejected`、`test_fill_price_tick_scale` 三項改為直接打 model；回歸雙線。
- **相依**：Phase2-1。

## Phase2-3. `BaseSettlementModel` ＋ `TwStockSettlementModel` ⬜

- **目的**：本次重構最大的一塊，也是「台股當沖強制回補」與「期貨每日結算」共用的掛點。
- **做法**：搬 `enforce_day_trade_cover()`、`check_limit_up_locked()`、`convert_to_margin_position()`、`force_cover_position()`、`execute_daily_position_check()`、`accrue_holding_cost()`、`check_margin_call()`、`get_mark_price()`（`backtester.py:522-767`，共約 240 行）。介面：

  ```python
  def on_bar_close(
      self,
      date: datetime.date,
      quotes: List[BaseQuote],
      account: BaseAccount,
      event_counts: Dict[str, int],
  ) -> None:
      """一根 bar 收盤後由市場規則強制執行的動作"""
  ```

  `event_counts` 的六個 key 維持不變（報表相容）。model 需要 `position_manager` 才能執行強制回補，於建構時注入。
- **產出**：`core/backtest/models/settlement_model.py`。
- **驗證方式**：`test_margin_call_force_cover`、`test_margin_call_warn_only`、`test_borrow_fee_not_double_counted`、`test_sbl_borrow_fee_accrued_daily`、`test_max_holding_days_force_cover`、`test_uncovered_day_trade_forced`、`test_limit_up_cannot_cover` 七項；**SHORT 快照逐筆相同是本步驟的主要護欄**。
- **相依**：Phase2-1。

## Phase2-4. `BaseCostModel` 介面化 ⬜

- **目的**：讓期貨的期交稅／保證金能替換進來，同時不動既有公式。
- **做法**：新增 `BaseCostModel` 抽象（`commission`、`tax`、`realized_pnl`、`roi`、`roi_on_capital` 為必要方法；`borrow_fee`、`margin_required`、`short_interest` 等放空專屬方法給預設 `return 0` 實作），`StockCostModel` 加上繼承，**公式一行不改**。`build_cost_config()` 與 `enrich_orders()` 一併移入台股實作。
- **產出**：`core/backtest/models/cost_model.py`。
- **驗證方式**：`test_cost_model.py` 24 項 ＋ 回歸雙線。
- **相依**：Phase1-3。
- **註**：與 [LONG成本模型口徑收斂.md](LONG成本模型口徑收斂.md) 無衝突——該文件是在 `StockCostModel` **內部**統一 LONG／SHORT 口徑，介面不受影響，兩者可任意先後。

## Phase2-5. `BaseDataFeed` ＋ `TwStockDataFeed` ⬜

- **目的**：`load_datasets()` 硬接五支 `Stock*API`，`run()` 硬呼叫 `MarketCalendar.check_stock_market_open()`，這兩處不抽走則驗收標準過不了。
- **做法**：

  ```python
  class BaseDataFeed(ABC):
      @abstractmethod
      def setup(self, strategy: BaseStrategy) -> None: ...
      @abstractmethod
      def is_market_open(self, date: datetime.date) -> bool: ...
      @abstractmethod
      def get_quotes(self, date: datetime.date, scale: Scale) -> List[BaseQuote]: ...
  ```

  `TwStockDataFeed` 吸收 `StockPriceAPI` / `StockChipAPI` / `StockTickAPI` / `MonthlyRevenueReportAPI` / `FinancialStatementAPI` 的建立、`StockQuoteAdapter` 的轉換，以及 `MarketCalendar.check_stock_market_open()`。
- **產出**：`core/backtest/datafeed/base.py`、`core/backtest/datafeed/tw_stock_datafeed.py`。
- **驗證方式**：回歸雙線（LONG 回歸會實際讀 `stock.db`，這是最有效的驗證）。
- **相依**：Phase1-3。

## Phase2-6. `Backtester` 改為注入式 ⬜

- **目的**：組裝前五步的成果，完成引擎瘦身。
- **做法**：`__init__` 改收六個參數（見 §二）；刪除所有 `Stock*` import；`run()`／`execute_bar()`／`execute_open_signal()`／`execute_close_signal()`／`snapshot_daily_equity()`／`generate_backtest_report()` 改呼叫注入的 model。`snapshot_daily_equity` 的 `StockUtils.convert_lot_to_share` 換成 `self.instrument.to_units()`。
- **產出**：`core/backtest/backtester.py`（838 → 約 400 行）。
- **驗證方式**：`grep -c "Stock" core/backtest/backtester.py` 為 0；回歸雙線；全測試通過。
- **相依**：Phase2-1 ~ Phase2-5。

---

## Phase3-1. `build_backtester()` factory ⬜

- **目的**：全專案唯一一處依市場分派的地方。
- **做法**：

  ```python
  def build_backtester(strategy: BaseStrategy) -> Backtester:
      """依策略宣告的市場組裝對應的 model 組合"""

      if strategy.market == Market.STOCK:
          return Backtester(
              strategy,
              instrument=TwStockSpec(),
              fill_model=TwStockFillModel(),
              cost_model=StockCostModel(...),
              settlement=TwStockSettlementModel(...),
              data_feed=TwStockDataFeed(),
          )
      raise ValueError(f"尚未支援的市場：{strategy.market}")
  ```
- **產出**：`core/backtest/factory.py`、`core/backtest/__init__.py`。
- **驗證方式**：經 factory 產出的引擎跑 `MomentumStrategy1` 結果與 Phase2-6 相同。
- **相依**：Phase2-6。

## Phase3-2. `StrategyLoader` 多市場 ＋ `run.py` 接 factory ⬜

- **目的**：入口只保留一支，不新增 `run_futures.py`（Backtrader `Cerebro`、Zipline `run_algorithm`、Lean `Engine`、Nautilus `BacktestEngine` 皆為單一入口，市場由設定決定而非由入口決定）。
- **做法**：
  - `StrategyLoader.load_strategies()` 掃描 `core/strategies/` 下的所有市場子套件（現況寫死 `stock_strategies_pkg`，`strategy_loader.py:16`），回傳 `Dict[str, Type[BaseStrategy]]`；保留 `load_stock_strategies()` 為相容別名。
  - `run.py` 的 `Backtester(strategy)` 改為 `build_backtester(strategy)`，**CLI 介面完全不變**，不新增 `--market`（市場由策略類別自己宣告）。
- **產出**：`core/strategies/strategy_loader.py`、`run.py`。
- **驗證方式**：`python run.py --strategy MomentumStrategy1` 產出的報表與重構前逐檔相同。
- **相依**：Phase3-1。

---

## Phase4-1. 領域模組移出 `core/utils/` ⬜

- **目的**：`core/utils/` 目前是雜物櫃——`cost_model.py`（回測領域核心）、`instrument.py`、`market_calendar.py` 與 `log_manager.py`、`decorators.py`、`notify.py` 混在一起，導致 `backtester.py` 同時出現 `from core.utils import ...` 與 `from core.utils.cost_model import ...` 兩種 import 形式。
- **做法**：`cost_model.py` → `core/backtest/models/`；`instrument.py` 的計算函式併入 `InstrumentSpec` 或移至 `core/backtest/models/`；`market_calendar.py` → `core/backtest/datafeed/`。`core/utils/` 只留真正的通用工具（`time.py`、`log_manager.py`、`decorators.py`、`notify.py`、`constant.py`、`path.py`）。
- **產出**：`core/backtest/models/`、`core/backtest/datafeed/`、`core/utils/`、`core/utils/__init__.py`。
- **驗證方式**：全專案 `grep -rn "from core.utils.cost_model\|from core.utils.instrument"` 無殘留；回歸雙線。
- **相依**：Phase2-6。

## Phase4-2. `BasePositionManager` 抽取 ＋ `settle_daily()` 掛點 ⬜

- **目的**：FIFO 拆單與等比例攤提是市場無關的，應該上移；同時為期貨的「每日結算」預留掛點。
- **做法**：
  - `core/managers/base/position_manager.py`：抽出 FIFO 迴圈、方向篩選、等比例攤提骨架。
  - 新增抽象方法 `settle_daily(position: BasePosition, settle_price: float) -> None`：**股票實作為 no-op**（維持現狀，LONG 回歸不受影響）；期貨日後實作為「結算損益進 balance、`position.price` 重設為結算價」。這是股票（開倉→持有→平倉才實現）與期貨（每日結算、成本基礎重設）語意差異的收容處，避免日後動到 FIFO 主幹。
  - 目錄扁平化：`core/managers/stock/position/position_manager.py` → `core/managers/stock/position_manager.py`（四層目錄只裝一個檔）。
- **產出**：`core/managers/base/position_manager.py`、`core/managers/stock/position_manager.py`。
- **驗證方式**：回歸雙線 ＋ `test_position_manager.py` 10 項。
- **相依**：Phase2-6。

## Phase4-3. 刪除死碼 ⬜

- **目的**：降低後續重構的搜尋噪音。
- **做法**：刪除 `core/archive/`（空目錄）與 `core/api/old api/`（`data.py`、`qx_data.py`，**目錄名含空格**）。兩者經 `grep -rn` 確認全專案零引用。
- **產出**：刪除上述目錄。
- **驗證方式**：`grep -rn "archive\|old api" --include="*.py"` 無結果；全測試通過。
- **相依**：無（可與任何步驟並行）。

## Phase4-4. 同步更新受影響的 backlog 文件 ⬜

- **目的**：依 [`CLAUDE.md` §3.4](../CLAUDE.md#34-索引維護規範)，路徑與掛點名稱變動必須同步回文件，否則後續實作者會照著失效的路徑找檔案。
- **做法**：依 §五 的清單逐份更新「產出檔案」欄與掛點敘述，並更新 `backlog/index.md` 的進度。
- **產出**：§五 列出的 8 份 `.md` ＋ `backlog/index.md`。
- **驗證方式**：人工檢視——文件中提到的每個檔案路徑都能在重構後的樹中找到。
- **相依**：Phase4-1、Phase4-2。

---

## 拆分原則與中斷處理

- 每個步驟都是**可獨立 commit、可獨立驗證**的最小單位，驗收一律是「回歸雙線 ＋ 全測試」。
- **Phase 1 與 Phase 2 的所有步驟都必須行為零改變**。任何一步若發現「不改行為就搬不動」，代表該處藏有設計問題——**停下來記中斷點、另開 backlog，不要順手改公式**。這是本次重構唯一的紅線。
- 中斷時依 [`CLAUDE.md` §3.3](../CLAUDE.md#33-實作過程的狀態更新) 在該步驟章節末尾補「⛔ 中斷紀錄」，寫明已完成／未完成／原因／恢復下一步。

---

## 關聯與狀態

- **優先級**：P0（最高，阻塞期貨與美股的回測落地）
- **狀態**：⬜ 未開始
- **相關程式**：`core/backtest/backtester.py`、`core/backtest/report/reporter.py`、`core/backtest/analysis/analyzer.py`、`core/managers/stock/position/position_manager.py`、`core/models/stock/*`、`core/strategies/stock/base.py`、`core/strategies/strategy_loader.py`、`core/utils/cost_model.py`、`core/utils/instrument.py`、`core/utils/market_calendar.py`、`core/adapters/stock_quote_adapter.py`、`run.py`
- **實作前必讀**：本文件 §一（為什麼不平行複製）、§四（已知簡化）、Phase0-1（護欄必須先建）
- **相關 backlog**：見 §五
