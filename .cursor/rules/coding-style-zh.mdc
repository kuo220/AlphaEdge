---
description: Python coding style 與中文註解規範（由 core/、strategy_lab/ 現有程式碼歸納）
globs: **/*.py
alwaysApply: false
---

# Coding Style 與註解規範

[`CLAUDE.md`](../../CLAUDE.md) §1 通用原則與 §2 Coding Style 是本專案程式碼風格的**唯一權威來源**，
完整定義了：註解語言、docstring 格式（含專案自訂的 `- Description:` / `- Parameter:` / `- Return:` 結構）、
模組層級說明字串的位置、type hints 慣例、import 排序、命名表、常數與 Enum、區塊分隔註解、
日誌（`loguru`）與格式化工具（行寬 88）。

本檔只是指標，**不重複任何規則內容**——風格規範一旦兩邊各存一份就會漂移。

## 執行步驟

1. 一律先完整讀取 [`CLAUDE.md`](../../CLAUDE.md) 的 §1 與 §2，再開始新增或修改 `**/*.py`，
   不要只憑記憶或猜測風格。
2. 既有程式碼的寫法優先於個人偏好；沿用現有風格，不要引入新風格。

> 規則需要修改時，改 `CLAUDE.md`，不要改本檔。
> 本檔僅在指標失效（例如章節重編號）時才需要更新。
