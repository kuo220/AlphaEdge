---
description: 規範 commit 與 push 的中文格式
alwaysApply: true
---

# Commit 與 Push 回覆規格

[`CLAUDE.md`](../../CLAUDE.md) §4 Commit 與 Push 是本專案 commit 規範的**唯一權威來源**，
定義了 commit message 的中文格式、編號條列的內文結構、多主題變更的分組方式，
以及回覆使用者時必須列出的欄位（分支名稱、commit hash、push 目標、變更檔案清單）。
回覆語言的預設規則見 `CLAUDE.md` §1。

本檔只是指標，**不重複任何規則內容**——規範一旦兩邊各存一份就會漂移。

## 執行步驟

1. 使用者要求「commit + push」時，先讀 [`CLAUDE.md`](../../CLAUDE.md) §4，再依該節格式撰寫與回覆。

> 規則需要修改時，改 `CLAUDE.md`，不要改本檔。
> 本檔僅在指標失效（例如章節重編號）時才需要更新。
